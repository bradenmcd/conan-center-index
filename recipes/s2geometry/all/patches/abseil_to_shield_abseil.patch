diff -u -r '--exclude=.git' a/CMakeLists.txt b/CMakeLists.txt
--- a/CMakeLists.txt	2024-07-25 16:38:02.879739431 -0400
+++ b/CMakeLists.txt	2024-07-25 16:22:23.176416495 -0400
@@ -50,8 +50,8 @@
 # If we already have abseil we don't need to find it, for an example:
 # add_subdirectory(absl-submbodule)
 # add_subdirectory(s2-submodule)
-if (NOT TARGET absl::base)
-    find_package(absl REQUIRED)
+if (NOT TARGET shield_shield_absl::base)
+    find_package(shield_absl REQUIRED)
 endif()
 find_package(OpenSSL REQUIRED)
 # pthreads isn't used directly, but this is still required for std::thread.
@@ -204,39 +204,39 @@
 target_link_libraries(
     s2
     ${OPENSSL_LIBRARIES}
-    absl::base
-    absl::btree
-    absl::check
-    absl::config
-    absl::core_headers
-    absl::dynamic_annotations
-    absl::endian
-    absl::fixed_array
-    absl::flags
-    absl::flat_hash_map
-    absl::flat_hash_set
-    absl::hash
-    absl::inlined_vector
-    absl::int128
-    absl::log
-    absl::log_severity
-    absl::memory
-    absl::span
-    absl::status
-    absl::str_format
-    absl::strings
-    absl::type_traits
-    absl::utility
+    shield_absl::base
+    shield_absl::btree
+    shield_absl::check
+    shield_absl::config
+    shield_absl::core_headers
+    shield_absl::dynamic_annotations
+    shield_absl::endian
+    shield_absl::fixed_array
+    shield_absl::flags
+    shield_absl::flat_hash_map
+    shield_absl::flat_hash_set
+    shield_absl::hash
+    shield_absl::inlined_vector
+    shield_absl::int128
+    shield_absl::log
+    shield_absl::log_severity
+    shield_absl::memory
+    shield_absl::span
+    shield_absl::status
+    shield_absl::str_format
+    shield_absl::strings
+    shield_absl::type_traits
+    shield_absl::utility
     ${CMAKE_THREAD_LIBS_INIT})
 
 if (GOOGLETEST_ROOT)
   target_link_libraries(
       s2testing
-      absl::check
-      absl::flags
-      absl::log
-      absl::memory
-      absl::strings)
+      shield_absl::check
+      shield_absl::flags
+      shield_absl::log
+      shield_absl::memory
+      shield_absl::strings)
 endif()
 
 # Allow other CMake projects to use this one with:
@@ -575,18 +575,18 @@
     target_link_libraries(
         ${test}
         s2testing s2
-        absl::base
-        absl::btree
-        absl::check
-        absl::core_headers
-        absl::flags
-        absl::flags_reflection
-        absl::log
-        absl::memory
-        absl::span
-        absl::status
-        absl::strings
-        absl::synchronization
+        shield_absl::base
+        shield_absl::btree
+        shield_absl::check
+        shield_absl::core_headers
+        shield_absl::flags
+        shield_absl::flags_reflection
+        shield_absl::log
+        shield_absl::memory
+        shield_absl::span
+        shield_absl::status
+        shield_absl::strings
+        shield_absl::synchronization
         gmock_main)
     add_test(${test} ${test})
   endforeach()
diff -u -r '--exclude=.git' a/Config.cmake.in b/Config.cmake.in
--- a/Config.cmake.in	2024-07-25 16:37:56.171717268 -0400
+++ b/Config.cmake.in	2024-07-25 16:21:30.656206118 -0400
@@ -1,6 +1,6 @@
 @PACKAGE_INIT@
 
 include(CMakeFindDependencyMacro)
-find_dependency(absl CONFIG)
+find_dependency(shield_absl CONFIG)
 
 include("${CMAKE_CURRENT_LIST_DIR}/s2Targets.cmake")
diff -u -r '--exclude=.git' a/doc/examples/point_index.cc b/doc/examples/point_index.cc
--- a/doc/examples/point_index.cc	2024-07-25 16:38:02.879739431 -0400
+++ b/doc/examples/point_index.cc	2024-07-25 16:23:15.404621412 -0400
@@ -24,24 +24,24 @@
 int main(int argc, char **argv) {
   // Build an index containing random points anywhere on the Earth.
   S2PointIndex<int> index;
-  for (int i = 0; i < absl::GetFlag(FLAGS_num_index_points); ++i) {
+  for (int i = 0; i < shield_absl::GetFlag(FLAGS_num_index_points); ++i) {
     index.Add(S2Testing::RandomPoint(), i);
   }
 
   // Create a query to search within the given radius of a target point.
   S2ClosestPointQuery<int> query(&index);
   query.mutable_options()->set_max_distance(S1Angle::Radians(
-      S2Earth::KmToRadians(absl::GetFlag(FLAGS_query_radius_km))));
+      S2Earth::KmToRadians(shield_absl::GetFlag(FLAGS_query_radius_km))));
 
   // Repeatedly choose a random target point, and count how many index points
   // are within the given radius of that point.
   int64_t num_found = 0;
-  for (int i = 0; i < absl::GetFlag(FLAGS_num_queries); ++i) {
+  for (int i = 0; i < shield_absl::GetFlag(FLAGS_num_queries); ++i) {
     S2ClosestPointQuery<int>::PointTarget target(S2Testing::RandomPoint());
     num_found += query.FindClosestPoints(&target).size();
   }
 
   std::printf("Found %" PRId64 " points in %d queries\n", num_found,
-              absl::GetFlag(FLAGS_num_queries));
+              shield_absl::GetFlag(FLAGS_num_queries));
   return 0;
 }
diff -u -r '--exclude=.git' a/doc/examples/term_index.cc b/doc/examples/term_index.cc
--- a/doc/examples/term_index.cc	2024-07-25 16:38:02.879739431 -0400
+++ b/doc/examples/term_index.cc	2024-07-25 16:23:15.404621412 -0400
@@ -44,14 +44,14 @@
   // region types include polygons, polylines, rectangles, discs, buffered
   // geometry, etc.)
   std::vector<S2Point> documents;
-  documents.reserve(absl::GetFlag(FLAGS_num_documents));
-  for (int docid = 0; docid < absl::GetFlag(FLAGS_num_documents); ++docid) {
+  documents.reserve(shield_absl::GetFlag(FLAGS_num_documents));
+  for (int docid = 0; docid < shield_absl::GetFlag(FLAGS_num_documents); ++docid) {
     documents.push_back(S2Testing::RandomPoint());
   }
 
   // We use a hash map as our inverted index.  The key is an index term, and
   // the value is the set of "document ids" where this index term is present.
-  absl::flat_hash_map<std::string, std::vector<int>> index;
+  shield_absl::flat_hash_map<std::string, std::vector<int>> index;
 
   // Create an indexer suitable for an index that contains points only.
   // (You may also want to adjust min_level() or max_level() if you plan
@@ -70,18 +70,18 @@
 
   // Convert the query radius to an angle representation.
   S1Angle radius = S1Angle::Radians(
-      S2Earth::KmToRadians(absl::GetFlag(FLAGS_query_radius_km)));
+      S2Earth::KmToRadians(shield_absl::GetFlag(FLAGS_query_radius_km)));
 
   // Count the number of documents (points) found in all queries.
   int64_t num_found = 0;
-  for (int i = 0; i < absl::GetFlag(FLAGS_num_queries); ++i) {
+  for (int i = 0; i < shield_absl::GetFlag(FLAGS_num_queries); ++i) {
     // Choose a random center for query.
     S2Cap query_region(S2Testing::RandomPoint(), radius);
 
     // Convert the query region to a set of terms, and compute the union of
     // the document ids associated with those terms.  (An actual information
     // retrieval system would do something more sophisticated.)
-    absl::btree_set<int> candidates;
+    shield_absl::btree_set<int> candidates;
     for (const auto& term : indexer.GetQueryTerms(query_region, kPrefix)) {
       candidates.insert(index[term].begin(), index[term].end());
     }
@@ -99,6 +99,6 @@
     num_found += result.size();
   }
   std::printf("Found %" PRId64 " points in %d queries\n", num_found,
-              absl::GetFlag(FLAGS_num_queries));
+              shield_absl::GetFlag(FLAGS_num_queries));
   return 0;
 }
diff -u -r '--exclude=.git' a/src/python/s2_common.i b/src/python/s2_common.i
--- a/src/python/s2_common.i	2024-07-25 16:38:02.883739446 -0400
+++ b/src/python/s2_common.i	2024-07-25 16:20:50.104040492 -0400
@@ -72,14 +72,14 @@
 %apply std::vector<S2CellId> *OUTPUT {std::vector<S2CellId> *interior};
 %apply std::vector<S2CellId> *OUTPUT {std::vector<S2CellId> *output};
 
-%typemap(in) absl::string_view {
+%typemap(in) shield_absl::string_view {
   if (PyUnicode_Check($input)) {
-    $1 = absl::string_view(PyUnicode_AsUTF8($input));
+    $1 = shield_absl::string_view(PyUnicode_AsUTF8($input));
   } else {
     SWIG_exception(SWIG_TypeError, "string expected");
   }
 }
-%typemap(typecheck) absl::string_view = char *;
+%typemap(typecheck) shield_absl::string_view = char *;
 
 
 %typemap(in, numinputs=0) S2CellId *OUTPUT_ARRAY_4(S2CellId temp[4]) {
@@ -130,15 +130,15 @@
 // SWIG_Python_GetSwigThis, doesn't find the 'this' attribute and gives up.
 // To avoid this problem rename the Polyline::Init methods so they aren't
 // overloaded.  We also need to reimplement them since SWIG doesn't
-// seem to understand absl::Span.
+// seem to understand shield_absl::Span.
 %extend S2Polyline {
  public:
   void InitFromS2LatLngs(const std::vector<S2LatLng>& vertices) {
-    $self->Init(absl::MakeConstSpan(vertices));
+    $self->Init(shield_absl::MakeConstSpan(vertices));
   }
 
   void InitFromS2Points(const std::vector<S2Point>& vertices) {
-    $self->Init(absl::MakeConstSpan(vertices));
+    $self->Init(shield_absl::MakeConstSpan(vertices));
   }
  };
 
@@ -253,15 +253,15 @@
   }
 };
 
-// The extensions below exist to work around the use of absl::Span.
+// The extensions below exist to work around the use of shield_absl::Span.
 %extend S2Loop {
  public:
   explicit S2Loop(const std::vector<S2Point>& vertices) {
-    return new S2Loop(absl::MakeConstSpan(vertices));
+    return new S2Loop(shield_absl::MakeConstSpan(vertices));
   }
 
   void Init(const std::vector<S2Point>& vertices) {
-    $self->Init(absl::MakeConstSpan(vertices));
+    $self->Init(shield_absl::MakeConstSpan(vertices));
   }
 };
 
@@ -633,12 +633,12 @@
 %rename(GetVertexNeighbors) S2CellId::AppendVertexNeighbors(int, std::vector<S2CellId>*) const;
 %unignore S2CellId::Begin;
 %unignore S2CellId::End;
-%unignore S2CellId::FromDebugString(absl::string_view);
+%unignore S2CellId::FromDebugString(shield_absl::string_view);
 %unignore S2CellId::FromFaceIJ(int, int, int);
 %unignore S2CellId::FromFacePosLevel(int, uint64, int);
 %unignore S2CellId::FromLatLng;
 %unignore S2CellId::FromPoint;
-%unignore S2CellId::FromToken(absl::string_view);
+%unignore S2CellId::FromToken(shield_absl::string_view);
 %unignore S2CellId::GetCenterSiTi(int*, int*) const;
 %unignore S2CellId::GetEdgeNeighbors;
 %unignore S2CellId::ToFaceIJOrientation(int*, int*, int*) const;
@@ -874,16 +874,16 @@
 %unignore S2RegionTermIndexer;
 %unignore S2RegionTermIndexer::S2RegionTermIndexer;
 %unignore S2RegionTermIndexer::~S2RegionTermIndexer;
-%unignore S2RegionTermIndexer::GetIndexTerms(const S2Point&, absl::string_view);
+%unignore S2RegionTermIndexer::GetIndexTerms(const S2Point&, shield_absl::string_view);
 %unignore S2RegionTermIndexer::GetIndexTerms(const S2Region&,
-                                             absl::string_view);
+                                             shield_absl::string_view);
 %unignore S2RegionTermIndexer::GetIndexTermsForCanonicalCovering(
-    const S2CellUnion&, absl::string_view);
-%unignore S2RegionTermIndexer::GetQueryTerms(const S2Point&, absl::string_view);
+    const S2CellUnion&, shield_absl::string_view);
+%unignore S2RegionTermIndexer::GetQueryTerms(const S2Point&, shield_absl::string_view);
 %unignore S2RegionTermIndexer::GetQueryTerms(const S2Region&,
-                                             absl::string_view);
+                                             shield_absl::string_view);
 %unignore S2RegionTermIndexer::GetQueryTermsForCanonicalCovering(
-    const S2CellUnion&, absl::string_view);
+    const S2CellUnion&, shield_absl::string_view);
 %unignore S2ShapeIndex;
 
 %include "s2/r1interval.h"
diff -u -r '--exclude=.git' a/src/python/s2.i b/src/python/s2.i
--- a/src/python/s2.i	2024-07-25 16:38:02.879739431 -0400
+++ b/src/python/s2.i	2024-07-25 16:20:50.104040492 -0400
@@ -6,9 +6,9 @@
 #include "absl/strings/string_view.h"
 %}
 
-%typemap(typecheck) absl::string_view = char *;
+%typemap(typecheck) shield_absl::string_view = char *;
 
-%typemap(in) absl::string_view {
+%typemap(in) shield_absl::string_view {
   if ($input != Py_None) {
     Py_ssize_t len;
     if (PyUnicode_Check($input)) {
@@ -17,14 +17,14 @@
       if (buf == nullptr) {
         SWIG_fail;
       }
-      $1 = absl::string_view(buf, len);
+      $1 = shield_absl::string_view(buf, len);
     } else {
       char *buf;
       if (PyBytes_AsStringAndSize($input, &buf, &len) == -1) {
         // Python has raised an error (likely TypeError or UnicodeEncodeError).
         SWIG_fail;
       }
-      $1 = absl::string_view(buf, len);
+      $1 = shield_absl::string_view(buf, len);
     }
   }
 }
diff -u -r '--exclude=.git' a/src/s2/base/logging.h b/src/s2/base/logging.h
--- a/src/s2/base/logging.h	2024-07-25 16:38:02.883739446 -0400
+++ b/src/s2/base/logging.h	2024-07-25 16:20:50.104040492 -0400
@@ -25,7 +25,7 @@
 // The names CHECK, etc. are too common and may conflict with other
 // packages.  We use S2_CHECK to make it easier to switch to
 // something other than abseil-cpp for logging.
-// TODO(user): Remove these or make absl::log optional.
+// TODO(user): Remove these or make shield_absl::log optional.
 
 #define S2_LOG LOG
 #define S2_LOG_IF LOG_IF
diff -u -r '--exclude=.git' a/src/s2/encoded_s2cell_id_vector.cc b/src/s2/encoded_s2cell_id_vector.cc
--- a/src/s2/encoded_s2cell_id_vector.cc	2024-07-25 16:38:02.883739446 -0400
+++ b/src/s2/encoded_s2cell_id_vector.cc	2024-07-25 16:20:50.116040541 -0400
@@ -28,7 +28,7 @@
 #include "s2/encoded_uint_vector.h"
 #include "s2/s2cell_id.h"
 
-using absl::Span;
+using shield_absl::Span;
 using std::max;
 using std::min;
 using std::vector;
@@ -98,7 +98,7 @@
       uint64 t_base = v_min & ~(~0ULL >> (8 * len));
       int t_max_delta_msb = max(
           0,
-          static_cast<int>(absl::bit_width((v_max - t_base) >> e_shift)) - 1);
+          static_cast<int>(shield_absl::bit_width((v_max - t_base) >> e_shift)) - 1);
       uint64 t_bytes = len + v.size() * ((t_max_delta_msb >> 3) + 1);
       if (t_bytes < e_bytes) {
         e_base = t_base;
diff -u -r '--exclude=.git' a/src/s2/encoded_s2cell_id_vector.h b/src/s2/encoded_s2cell_id_vector.h
--- a/src/s2/encoded_s2cell_id_vector.h	2024-07-25 16:38:02.883739446 -0400
+++ b/src/s2/encoded_s2cell_id_vector.h	2024-07-25 16:20:50.140040641 -0400
@@ -35,7 +35,7 @@
 //
 // REQUIRES: "encoder" uses the default constructor, so that its buffer
 //           can be enlarged as necessary by calling Ensure(int).
-void EncodeS2CellIdVector(absl::Span<const S2CellId> v, Encoder* encoder);
+void EncodeS2CellIdVector(shield_absl::Span<const S2CellId> v, Encoder* encoder);
 
 // This class represents an encoded vector of S2CellIds.  Values are decoded
 // only when they are accessed.  This allows for very fast initialization and
diff -u -r '--exclude=.git' a/src/s2/encoded_s2point_vector.cc b/src/s2/encoded_s2point_vector.cc
--- a/src/s2/encoded_s2point_vector.cc	2024-07-25 16:38:02.883739446 -0400
+++ b/src/s2/encoded_s2point_vector.cc	2024-07-25 16:20:50.104040492 -0400
@@ -38,8 +38,8 @@
 #include "s2/s2coords.h"
 #include "s2/s2point.h"
 
-using absl::MakeSpan;
-using absl::Span;
+using shield_absl::MakeSpan;
+using shield_absl::Span;
 using std::max;
 using std::min;
 using std::vector;
@@ -646,7 +646,7 @@
   // 2. The format only allows us to represent up to 7 bytes (56 bits) of
   // "base", so we need to ensure that "base" conforms to this requirement.
   int min_delta_bits = (have_exceptions || values.size() == 1) ? 8 : 4;
-  int excluded_bits = max<int>(absl::bit_width(v_min ^ v_max),
+  int excluded_bits = max<int>(shield_absl::bit_width(v_min ^ v_max),
                                max(min_delta_bits, BaseShift(level, 56)));
   uint64 base = v_min & ~BitMask(excluded_bits);
 
@@ -749,7 +749,7 @@
   // It is possible to show that this last example is the worst case, i.e.  we
   // do not need to consider increasing delta_bits or overlap_bits further.
   int delta_bits =
-      (max(1, static_cast<int>(absl::bit_width(b_max - b_min)) - 1) + 3) & ~3;
+      (max(1, static_cast<int>(shield_absl::bit_width(b_max - b_min)) - 1) + 3) & ~3;
   int overlap_bits = 0;
   if (!CanEncode(b_min, b_max, delta_bits, 0, have_exceptions)) {
     if (CanEncode(b_min, b_max, delta_bits, 4, have_exceptions)) {
diff -u -r '--exclude=.git' a/src/s2/encoded_s2point_vector.h b/src/s2/encoded_s2point_vector.h
--- a/src/s2/encoded_s2point_vector.h	2024-07-25 16:38:02.883739446 -0400
+++ b/src/s2/encoded_s2point_vector.h	2024-07-25 16:20:50.144040657 -0400
@@ -39,7 +39,7 @@
 //
 // REQUIRES: "encoder" uses the default constructor, so that its buffer
 //           can be enlarged as necessary by calling Ensure(int).
-void EncodeS2PointVector(absl::Span<const S2Point> points, CodingHint hint,
+void EncodeS2PointVector(shield_absl::Span<const S2Point> points, CodingHint hint,
                          Encoder* encoder);
 
 // This class represents an encoded vector of S2Points.  Values are decoded
@@ -79,10 +79,10 @@
   // points.  This would save some decoding overhead.
 
  private:
-  friend void EncodeS2PointVector(absl::Span<const S2Point>, CodingHint,
+  friend void EncodeS2PointVector(shield_absl::Span<const S2Point>, CodingHint,
                                   Encoder*);
-  friend void EncodeS2PointVectorFast(absl::Span<const S2Point>, Encoder*);
-  friend void EncodeS2PointVectorCompact(absl::Span<const S2Point>, Encoder*);
+  friend void EncodeS2PointVectorFast(shield_absl::Span<const S2Point>, Encoder*);
+  friend void EncodeS2PointVectorCompact(shield_absl::Span<const S2Point>, Encoder*);
 
   bool InitUncompressedFormat(Decoder* decoder);
   bool InitCellIdsFormat(Decoder* decoder);
diff -u -r '--exclude=.git' a/src/s2/encoded_s2point_vector_test.cc b/src/s2/encoded_s2point_vector_test.cc
--- a/src/s2/encoded_s2point_vector_test.cc	2024-07-25 16:38:02.883739446 -0400
+++ b/src/s2/encoded_s2point_vector_test.cc	2024-07-25 16:20:50.144040657 -0400
@@ -296,7 +296,7 @@
   // the maximum face, si, and ti values).  All such S2CellIds can be encoded in
   // 6 bytes because most of the bits are zero.
   for (int level = 0; level <= S2CellId::kMaxLevel; ++level) {
-    SCOPED_TRACE(absl::StrCat("Level = ", level));
+    SCOPED_TRACE(shield_absl::StrCat("Level = ", level));
     TestEncodedS2PointVector({S2CellId::Begin(level).ToPoint()},
                              CodingHint::COMPACT, 6);
   }
@@ -308,7 +308,7 @@
   // possible si value at that level.  Such S2CellIds can be encoded in 6 to 13
   // bytes depending on the level.
   for (int level = 0; level <= S2CellId::kMaxLevel; ++level) {
-    SCOPED_TRACE(absl::StrCat("Level = ", level));
+    SCOPED_TRACE(shield_absl::StrCat("Level = ", level));
     // Note that 8 bit deltas are used to encode blocks of size 1, which
     // reduces the size of "base" from ((level + 2) / 4) to (level / 4) bytes.
     int expected_size = 6 + level / 4;
@@ -323,7 +323,7 @@
   // S2CellId whose human-readable form is 5/222...22 (0xb555555555555555),
   // however for clarity we consruct it using S2CellId::FromFaceIJ.
   for (int level = 0; level <= S2CellId::kMaxLevel; ++level) {
-    SCOPED_TRACE(absl::StrCat("Level = ", level));
+    SCOPED_TRACE(shield_absl::StrCat("Level = ", level));
     S2CellId id = S2CellId::FromFaceIJ(5, S2::kLimitIJ - 1, S2::kLimitIJ - 1)
                   .parent(level);
 
@@ -342,7 +342,7 @@
 TEST(EncodedS2PointVectorTest, LastTwoPointsAtAllLevels) {
   // Test encoding the last two S2CellIds at each level.
   for (int level = 0; level <= S2CellId::kMaxLevel; ++level) {
-    SCOPED_TRACE(absl::StrCat("Level = ", level));
+    SCOPED_TRACE(shield_absl::StrCat("Level = ", level));
     S2CellId id = S2CellId::End(level).prev();
     // Notice that this costs only 4 bits more than encoding the last S2CellId
     // by itself (see LastAtAllLevels).  This is because encoding a block of
@@ -359,7 +359,7 @@
   // between 27 and 38 bytes depending on the level.  (Note that the encoding
   // can use less than 1 byte per point in this situation.)
   for (int level = 0; level <= S2CellId::kMaxLevel; ++level) {
-    SCOPED_TRACE(absl::StrCat("Level = ", level));
+    SCOPED_TRACE(shield_absl::StrCat("Level = ", level));
     S2CellId id = S2CellId::End(level).prev();
     // Encoding: header (2 bytes), base ((level + 2) / 4 bytes), block count
     // (1 byte), block offsets (2 bytes), block headers (2 bytes), 32 deltas
@@ -374,7 +374,7 @@
 }
 
 TEST(EncodedS2PointVectorTest, SnappedFractalLoops) {
-  S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed));
+  S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed));
   int kMaxPoints = 3 << (google::DEBUG_MODE ? 10 : 14);
   for (int num_points = 3; num_points <= kMaxPoints; num_points *= 4) {
     size_t s2polygon_size = 0, lax_polygon_size = 0;
diff -u -r '--exclude=.git' a/src/s2/encoded_s2shape_index.h b/src/s2/encoded_s2shape_index.h
--- a/src/s2/encoded_s2shape_index.h	2024-07-25 16:38:02.883739446 -0400
+++ b/src/s2/encoded_s2shape_index.h	2024-07-25 16:20:50.156040706 -0400
@@ -122,7 +122,7 @@
 // thread safe, and non-const methods are not thread safe.  The only non-const
 // method is Minimize(), so if you plan to call Minimize() while other threads
 // are actively using the index that you must use an external reader-writer
-// lock such as absl::Mutex to guard access to it.  (There is no global state
+// lock such as shield_absl::Mutex to guard access to it.  (There is no global state
 // and therefore each index can be guarded independently.)
 class EncodedS2ShapeIndex final : public S2ShapeIndex {
  public:
diff -u -r '--exclude=.git' a/src/s2/encoded_s2shape_index_test.cc b/src/s2/encoded_s2shape_index_test.cc
--- a/src/s2/encoded_s2shape_index_test.cc	2024-07-25 16:38:02.883739446 -0400
+++ b/src/s2/encoded_s2shape_index_test.cc	2024-07-25 16:20:50.140040641 -0400
@@ -60,7 +60,7 @@
 #include "s2/s2text_format.h"
 #include "s2/thread_testing.h"
 
-using absl::StrCat;
+using shield_absl::StrCat;
 using s2builderutil::S2CellIdSnapFunction;
 using s2builderutil::S2PolylineLayer;
 using std::make_unique;
@@ -352,7 +352,7 @@
   // bytes is the encoded data of an S2ShapeIndex with a null shape and a
   // polyline with one edge. It was derived by base-16 encoding the buffer of
   // an encoder to which expected was encoded.
-  string bytes = absl::HexStringToBytes(
+  string bytes = shield_absl::HexStringToBytes(
       "100036020102000000B4825F3C81FDEF3F27DCF7C958DE913F1EDD892B0BDF913FFC7FB8"
       "B805F6EF3F28516A6D8FDBA13F27DCF7C958DEA13F28C809010408020010");
   Decoder decoder(bytes.data(), bytes.length());
diff -u -r '--exclude=.git' a/src/s2/encoded_string_vector.cc b/src/s2/encoded_string_vector.cc
--- a/src/s2/encoded_string_vector.cc	2024-07-25 16:38:02.883739446 -0400
+++ b/src/s2/encoded_string_vector.cc	2024-07-25 16:20:50.136040625 -0400
@@ -29,9 +29,9 @@
 #include "s2/util/coding/coder.h"
 #include "s2/encoded_uint_vector.h"
 
-using absl::MakeSpan;
-using absl::Span;
-using absl::string_view;
+using shield_absl::MakeSpan;
+using shield_absl::Span;
+using shield_absl::string_view;
 using std::string;
 using std::vector;
 
diff -u -r '--exclude=.git' a/src/s2/encoded_string_vector.h b/src/s2/encoded_string_vector.h
--- a/src/s2/encoded_string_vector.h	2024-07-25 16:38:02.883739446 -0400
+++ b/src/s2/encoded_string_vector.h	2024-07-25 16:20:50.136040625 -0400
@@ -67,7 +67,7 @@
   //
   // REQUIRES: "encoder" uses the default constructor, so that its buffer
   //           can be enlarged as necessary by calling Ensure(int).
-  static void Encode(absl::Span<const std::string> v, Encoder* encoder);
+  static void Encode(shield_absl::Span<const std::string> v, Encoder* encoder);
 
  private:
   // A vector consisting of the starting offset of each string in the
@@ -105,7 +105,7 @@
   size_t size() const;
 
   // Returns the string at the given index.
-  absl::string_view operator[](int i) const;
+  shield_absl::string_view operator[](int i) const;
 
   // Returns a Decoder initialized with the string at the given index.
   Decoder GetDecoder(int i) const;
@@ -117,7 +117,7 @@
   // Returns the entire vector of original strings.  Requires that the
   // data buffer passed to the constructor persists until the result vector is
   // no longer needed.
-  std::vector<absl::string_view> Decode() const;
+  std::vector<shield_absl::string_view> Decode() const;
 
   void Encode(Encoder* encoder) const;
 
@@ -150,10 +150,10 @@
   return offsets_.size();
 }
 
-inline absl::string_view EncodedStringVector::operator[](int i) const {
+inline shield_absl::string_view EncodedStringVector::operator[](int i) const {
   uint64 start = (i == 0) ? 0 : offsets_[i - 1];
   uint64 limit = offsets_[i];
-  return absl::string_view(data_ + start, limit - start);
+  return shield_absl::string_view(data_ + start, limit - start);
 }
 
 inline Decoder EncodedStringVector::GetDecoder(int i) const {
diff -u -r '--exclude=.git' a/src/s2/encoded_string_vector_test.cc b/src/s2/encoded_string_vector_test.cc
--- a/src/s2/encoded_string_vector_test.cc	2024-07-25 16:37:56.175717283 -0400
+++ b/src/s2/encoded_string_vector_test.cc	2024-07-25 16:20:50.136040625 -0400
@@ -26,7 +26,7 @@
 #include "absl/strings/string_view.h"
 #include "s2/util/coding/coder.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using std::string;
 using std::vector;
 
diff -u -r '--exclude=.git' a/src/s2/encoded_uint_vector.h b/src/s2/encoded_uint_vector.h
--- a/src/s2/encoded_uint_vector.h	2024-07-25 16:38:02.883739446 -0400
+++ b/src/s2/encoded_uint_vector.h	2024-07-25 16:20:50.108040509 -0400
@@ -42,7 +42,7 @@
 // REQUIRES: "encoder" uses the default constructor, so that its buffer
 //           can be enlarged as necessary by calling Ensure(int).
 template <class T>
-void EncodeUintVector(absl::Span<const T> v, Encoder* encoder);
+void EncodeUintVector(shield_absl::Span<const T> v, Encoder* encoder);
 
 // This class represents an encoded vector of unsigned integers of type T.
 // Values are decoded only when they are accessed.  This allows for very fast
@@ -203,7 +203,7 @@
 }
 
 template <class T>
-void EncodeUintVector(absl::Span<const T> v, Encoder* encoder) {
+void EncodeUintVector(shield_absl::Span<const T> v, Encoder* encoder) {
   // The encoding is as follows:
   //
   //   varint64: (v.size() * sizeof(T)) | (len - 1)
diff -u -r '--exclude=.git' a/src/s2/mutable_s2shape_index.cc b/src/s2/mutable_s2shape_index.cc
--- a/src/s2/mutable_s2shape_index.cc	2024-07-25 16:38:02.883739446 -0400
+++ b/src/s2/mutable_s2shape_index.cc	2024-07-25 16:20:50.104040492 -0400
@@ -185,7 +185,7 @@
 
 MutableS2ShapeIndex::Options::Options()
     : max_edges_per_cell_(
-          absl::GetFlag(FLAGS_s2shape_index_default_max_edges_per_cell)) {}
+          shield_absl::GetFlag(FLAGS_s2shape_index_default_max_edges_per_cell)) {}
 
 void MutableS2ShapeIndex::Options::set_max_edges_per_cell(
     int max_edges_per_cell) {
@@ -456,7 +456,7 @@
       shapes_(std::move(b.shapes_)),
       cell_map_(std::move(b.cell_map_)),
       options_(std::move(b.options_)),
-      pending_additions_begin_(absl::exchange(b.pending_additions_begin_, 0)),
+      pending_additions_begin_(shield_absl::exchange(b.pending_additions_begin_, 0)),
       pending_removals_(std::move(b.pending_removals_)),
       index_status_(b.index_status_.exchange(FRESH, std::memory_order_relaxed)),
       mem_tracker_(std::move(b.mem_tracker_)) {}
@@ -469,7 +469,7 @@
   shapes_ = std::move(b.shapes_);
   cell_map_ = std::move(b.cell_map_);
   options_ = std::move(b.options_);
-  pending_additions_begin_ = absl::exchange(b.pending_additions_begin_, 0);
+  pending_additions_begin_ = shield_absl::exchange(b.pending_additions_begin_, 0);
   pending_removals_ = std::move(b.pending_removals_);
   index_status_.store(
       b.index_status_.exchange(FRESH, std::memory_order_relaxed),
@@ -847,7 +847,7 @@
   // Check whether we can update all the edges at once.
   int num_edges_total = num_edges_removed + num_edges_added;
   const double tmp_memory_budget_bytes =
-      absl::GetFlag(FLAGS_s2shape_index_tmp_memory_budget);
+      shield_absl::GetFlag(FLAGS_s2shape_index_tmp_memory_budget);
   if (num_edges_total * kTmpBytesPerEdge <= tmp_memory_budget_bytes) {
     return vector<int>{num_edges_total};
   }
@@ -912,7 +912,7 @@
   // sample the edges to estimate how many edges per face there are, and
   // therefore we generally use a lower threshold.)
   const size_t kMaxCheapBytes =
-      min(absl::GetFlag(FLAGS_s2shape_index_tmp_memory_budget) / 2,
+      min(shield_absl::GetFlag(FLAGS_s2shape_index_tmp_memory_budget) / 2,
           int64{30} << 20 /*30 MB*/);
   int64 face_edge_usage = batch.num_edges * (6 * sizeof(FaceEdge));
   if (static_cast<size_t>(face_edge_usage) <= kMaxCheapBytes) {
@@ -1186,7 +1186,7 @@
   // use Norm() rather than Angle() for speed.
   double max_cell_edge =
       ((edge.v0 - edge.v1).Norm() *
-       absl::GetFlag(FLAGS_s2shape_index_cell_size_to_long_edge_ratio));
+       shield_absl::GetFlag(FLAGS_s2shape_index_cell_size_to_long_edge_ratio));
 
   // Now return the first level encountered during subdivision where the
   // average cell edge length at that level is at most "max_cell_edge".
@@ -1807,7 +1807,7 @@
     int max_short_edges =
         max(options_.max_edges_per_cell(),
             static_cast<int>(
-                absl::GetFlag(FLAGS_s2shape_index_min_short_edge_fraction) *
+                shield_absl::GetFlag(FLAGS_s2shape_index_min_short_edge_fraction) *
                 (edges.size() + tracker->shape_ids().size())));
     int count = 0;
     for (const ClippedEdge* edge : edges) {
diff -u -r '--exclude=.git' a/src/s2/mutable_s2shape_index.h b/src/s2/mutable_s2shape_index.h
--- a/src/s2/mutable_s2shape_index.h	2024-07-25 16:38:02.883739446 -0400
+++ b/src/s2/mutable_s2shape_index.h	2024-07-25 16:20:50.128040591 -0400
@@ -52,7 +52,7 @@
 namespace s2internal {
 // Hack to expose bytes_used.
 template <typename Key, typename Value>
-class BTreeMap : public absl::btree_map<Key, Value> {
+class BTreeMap : public shield_absl::btree_map<Key, Value> {
  public:
   size_t bytes_used() const { return this->tree_.bytes_used(); }
 };
@@ -564,7 +564,7 @@
     // This mutex is used as a condition variable.  It is locked by the
     // updating thread for the entire duration of the update; other threads
     // lock it in order to wait until the update is finished.
-    absl::Mutex wait_mutex;
+    shield_absl::Mutex wait_mutex;
 
     // The number of threads currently waiting on "wait_mutex_".  The
     // UpdateState can only be freed when this number reaches zero.
diff -u -r '--exclude=.git' a/src/s2/mutable_s2shape_index_test.cc b/src/s2/mutable_s2shape_index_test.cc
--- a/src/s2/mutable_s2shape_index_test.cc	2024-07-25 16:38:02.887739458 -0400
+++ b/src/s2/mutable_s2shape_index_test.cc	2024-07-25 16:20:50.108040509 -0400
@@ -69,7 +69,7 @@
 #include "s2/util/coding/coder.h"
 #include "s2/util/math/matrix3x3.h"
 
-using absl::WrapUnique;
+using shield_absl::WrapUnique;
 using s2textformat::MakePolylineOrDie;
 using std::make_unique;
 using std::string;
@@ -187,7 +187,7 @@
     int max_short_edges = std::max(
         index_.options().max_edges_per_cell(),
         static_cast<int>(
-            absl::GetFlag(FLAGS_s2shape_index_min_short_edge_fraction) *
+            shield_absl::GetFlag(FLAGS_s2shape_index_min_short_edge_fraction) *
             (num_edges + num_containing_shapes)));
     EXPECT_LE(num_short_edges, max_short_edges);
     if (it.done()) break;
@@ -233,7 +233,7 @@
   string result;
   for (const auto& batch : batches) {
     if (!result.empty()) result += ", ";
-    absl::StrAppendFormat(&result, "(%d:%d, %d:%d, %d)",
+    shield_absl::StrAppendFormat(&result, "(%d:%d, %d:%d, %d)",
                           batch.begin.shape_id, batch.begin.edge_id,
                           batch.end.shape_id, batch.end.edge_id,
                           batch.num_edges);
@@ -245,8 +245,8 @@
     int num_edges_removed, const vector<int>& shape_edges_added,
     int64 tmp_memory_budget, int shape_id_begin,
     const vector<BatchDescriptor>& expected_batches) {
-  absl::FlagSaver fs;
-  absl::SetFlag(&FLAGS_s2shape_index_tmp_memory_budget, tmp_memory_budget);
+  shield_absl::FlagSaver fs;
+  shield_absl::SetFlag(&FLAGS_s2shape_index_tmp_memory_budget, tmp_memory_budget);
 
   int num_edges_added = 0;
   for (auto n : shape_edges_added) num_edges_added += n;
@@ -567,11 +567,11 @@
   // Set the temporary memory budget such that at least one shape needs to be
   // split into multiple update batches (namely, the "5 concentric rings"
   // polygon below which needs ~25KB of temporary space).
-  absl::FlagSaver fs;
-  absl::SetFlag(&FLAGS_s2shape_index_tmp_memory_budget, 10000);
+  shield_absl::FlagSaver fs;
+  shield_absl::SetFlag(&FLAGS_s2shape_index_tmp_memory_budget, 10000);
 
   // Allow the seed to be varied from the command line.
-  S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed));
+  S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed));
 
   // A few polylines.
   index_.Add(make_unique<S2Polyline::OwningShape>(
diff -u -r '--exclude=.git' a/src/s2/r1interval_test.cc b/src/s2/r1interval_test.cc
--- a/src/s2/r1interval_test.cc	2024-07-25 16:37:56.175717283 -0400
+++ b/src/s2/r1interval_test.cc	2024-07-25 16:20:50.116040541 -0400
@@ -23,7 +23,7 @@
 #include <gtest/gtest.h>
 #include "absl/strings/string_view.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 
 static void TestIntervalOps(const R1Interval& x, const R1Interval& y,
                             string_view expected) {
diff -u -r '--exclude=.git' a/src/s2/r2rect_test.cc b/src/s2/r2rect_test.cc
--- a/src/s2/r2rect_test.cc	2024-07-25 16:37:56.175717283 -0400
+++ b/src/s2/r2rect_test.cc	2024-07-25 16:20:50.124040575 -0400
@@ -27,7 +27,7 @@
 #include "s2/r1interval.h"
 #include "s2/r2.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 
 static void TestIntervalOps(const R2Rect& x, const R2Rect& y,
                             string_view expected_rexion,
diff -u -r '--exclude=.git' a/src/s2/s1angle.h b/src/s2/s1angle.h
--- a/src/s2/s1angle.h	2024-07-25 16:38:02.887739458 -0400
+++ b/src/s2/s1angle.h	2024-07-25 16:20:50.128040591 -0400
@@ -165,7 +165,7 @@
   // Normalize this angle to the range (-180, 180] degrees.
   void Normalize();
 
-  // When S1Angle is used as a key in one of the absl::btree container types,
+  // When S1Angle is used as a key in one of the shield_absl::btree container types,
   // indicate that linear rather than binary search should be used.  This is
   // much faster when the comparison function is cheap.
   typedef std::true_type absl_btree_prefer_linear_node_search;
diff -u -r '--exclude=.git' a/src/s2/s1angle_test.cc b/src/s2/s1angle_test.cc
--- a/src/s2/s1angle_test.cc	2024-07-25 16:38:02.887739458 -0400
+++ b/src/s2/s1angle_test.cc	2024-07-25 16:20:50.132040607 -0400
@@ -164,7 +164,7 @@
 // The current implementation guarantees exact conversions between
 // E6() and E7() when the E6() argument is an integer.
 TEST(S1Angle, E6VsE7) {
-  S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed));
+  S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed));
   for (int iter = 0; iter < 1000; ++iter) {
     int i = S2Testing::rnd.Uniform(180000000);
     EXPECT_EQ(S1Angle::E6(i), S1Angle::E7(10 * i));
diff -u -r '--exclude=.git' a/src/s2/s1chord_angle.h b/src/s2/s1chord_angle.h
--- a/src/s2/s1chord_angle.h	2024-07-25 16:38:02.887739458 -0400
+++ b/src/s2/s1chord_angle.h	2024-07-25 16:20:50.132040607 -0400
@@ -289,7 +289,7 @@
   // Infinity() are both considered valid.
   bool is_valid() const;
 
-  // When S1ChordAngle is used as a key in one of the absl::btree container
+  // When S1ChordAngle is used as a key in one of the shield_absl::btree container
   // types, indicate that linear rather than binary search should be used.
   // This is much faster when the comparison function is cheap.
   typedef std::true_type absl_btree_prefer_linear_node_search;
diff -u -r '--exclude=.git' a/src/s2/s1interval_test.cc b/src/s2/s1interval_test.cc
--- a/src/s2/s1interval_test.cc	2024-07-25 16:37:56.175717283 -0400
+++ b/src/s2/s1interval_test.cc	2024-07-25 16:20:50.156040706 -0400
@@ -24,7 +24,7 @@
 #include <gtest/gtest.h>
 #include "absl/strings/string_view.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using std::fabs;
 
 class S1IntervalTestBase : public testing::Test {
diff -u -r '--exclude=.git' a/src/s2/s2boolean_operation.cc b/src/s2/s2boolean_operation.cc
--- a/src/s2/s2boolean_operation.cc	2024-07-25 16:38:02.887739458 -0400
+++ b/src/s2/s2boolean_operation.cc	2024-07-25 16:20:50.104040492 -0400
@@ -110,8 +110,8 @@
 
 namespace {  // Anonymous namespace for helper classes.
 
-using absl::flat_hash_map;
-using absl::string_view;
+using shield_absl::flat_hash_map;
+using shield_absl::string_view;
 using std::lower_bound;
 using std::make_pair;
 using std::make_unique;
@@ -201,7 +201,7 @@
   bool outgoing;
   VertexId dst;
 };
-using CrossingGraphEdgeVector = absl::InlinedVector<CrossingGraphEdge, 2>;
+using CrossingGraphEdgeVector = shield_absl::InlinedVector<CrossingGraphEdge, 2>;
 
 // Returns a vector of EdgeIds sorted by input edge id.  When more than one
 // output edge has the same input edge id (i.e., the input edge snapped to a
@@ -1364,7 +1364,7 @@
   // A map that translates from SourceId (the (region_id, shape_id, edge_id)
   // triple that identifies an S2ShapeIndex edge) to InputEdgeId (the
   // sequentially increasing numbers assigned to input edges by S2Builder).
-  using SourceIdMap = absl::btree_map<SourceId, InputEdgeId>;
+  using SourceIdMap = shield_absl::btree_map<SourceId, InputEdgeId>;
   SourceIdMap source_id_map_;
 
   // For each edge in region B that defines a degenerate loop (either a point
@@ -2280,7 +2280,7 @@
     // one S2ShapeIndex intersects a non-empty cell of the other S2ShapeIndex.
   }
   vector<ShapeEdgeId> a_starts, b_starts;
-  auto _ = absl::MakeCleanup([&]() {
+  auto _ = shield_absl::MakeCleanup([&]() {
       tracker_.Untally(a_starts);
       tracker_.Untally(b_starts);
     });
diff -u -r '--exclude=.git' a/src/s2/s2boolean_operation.h b/src/s2/s2boolean_operation.h
--- a/src/s2/s2boolean_operation.h	2024-07-25 16:38:02.887739458 -0400
+++ b/src/s2/s2boolean_operation.h	2024-07-25 16:20:50.148040674 -0400
@@ -233,21 +233,21 @@
     SYMMETRIC_DIFFERENCE  // Contained by one region but not the other.
   };
   // Translates OpType to one of the strings above.
-  static absl::string_view OpTypeToString(OpType op_type);
+  static shield_absl::string_view OpTypeToString(OpType op_type);
 
   // Defines whether polygons are considered to contain their vertices and/or
   // edges (see definitions above).
   enum class PolygonModel : uint8 { OPEN, SEMI_OPEN, CLOSED };
 
   // Translates PolygonModel to one of the strings above.
-  static absl::string_view PolygonModelToString(PolygonModel model);
+  static shield_absl::string_view PolygonModelToString(PolygonModel model);
 
   // Defines whether polylines are considered to contain their endpoints
   // (see definitions above).
   enum class PolylineModel : uint8 { OPEN, SEMI_OPEN, CLOSED };
 
   // Translates PolylineModel to one of the strings above.
-  static absl::string_view PolylineModelToString(PolylineModel model);
+  static shield_absl::string_view PolylineModelToString(PolylineModel model);
 
   // With Precision::EXACT, the operation is evaluated using the exact input
   // geometry.  Predicates that use this option will produce exact results;
diff -u -r '--exclude=.git' a/src/s2/s2boolean_operation_test.cc b/src/s2/s2boolean_operation_test.cc
--- a/src/s2/s2boolean_operation_test.cc	2024-07-25 16:38:02.887739458 -0400
+++ b/src/s2/s2boolean_operation_test.cc	2024-07-25 16:20:50.120040559 -0400
@@ -67,11 +67,11 @@
 
 namespace {
 
-using absl::ByAnyChar;
-using absl::SkipEmpty;
-using absl::StrContains;
-using absl::string_view;
-using absl::StrSplit;
+using shield_absl::ByAnyChar;
+using shield_absl::SkipEmpty;
+using shield_absl::StrContains;
+using shield_absl::string_view;
+using shield_absl::StrSplit;
 using s2builderutil::IndexMatchingLayer;
 using s2builderutil::LaxPolygonLayer;
 using s2shapeutil::ContainsBruteForce;
@@ -1226,7 +1226,7 @@
   auto index0 = MakeIndex(string{ch0});
   auto index1 = MakeIndex(string{ch1});
   auto expected = MakeIndex(result);
-  SCOPED_TRACE(absl::StrCat(
+  SCOPED_TRACE(shield_absl::StrCat(
       "\n  Operation: ", S2BooleanOperation::OpTypeToString(op_type),
       "\n  PolygonModel: ",
       S2BooleanOperation::PolygonModelToString(options.polygon_model()),
diff -u -r '--exclude=.git' a/src/s2/s2buffer_operation_test.cc b/src/s2/s2buffer_operation_test.cc
--- a/src/s2/s2buffer_operation_test.cc	2024-07-25 16:38:02.887739458 -0400
+++ b/src/s2/s2buffer_operation_test.cc	2024-07-25 16:20:50.112040525 -0400
@@ -67,7 +67,7 @@
 #include "s2/s2testing.h"
 #include "s2/s2text_format.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using std::make_unique;
 using std::max;
 using std::string;
@@ -404,7 +404,7 @@
   output.Add(DoBuffer(
       [&input](S2BufferOperation* op) { op->AddShapeIndex(input); }, options));
 
-  SCOPED_TRACE(absl::StrFormat(
+  SCOPED_TRACE(shield_absl::StrFormat(
       "\nradius = %.17g, error_fraction = %.17g\ninput = %s\noutput = %s",
       buffer_radius.radians(), error_fraction,
       s2textformat::ToString(input), s2textformat::ToString(output)));
@@ -690,7 +690,7 @@
       PolylineSide::LEFT, PolylineSide::RIGHT, PolylineSide::BOTH}) {
     for (EndCapStyle end_cap_style : {
         EndCapStyle::ROUND, EndCapStyle::FLAT}) {
-      SCOPED_TRACE(absl::StrFormat(
+      SCOPED_TRACE(shield_absl::StrFormat(
           "two_sided = %d, round = %d",
           polyline_side == PolylineSide::BOTH,
           end_cap_style == EndCapStyle::ROUND));
diff -u -r '--exclude=.git' a/src/s2/s2builder.cc b/src/s2/s2builder.cc
--- a/src/s2/s2builder.cc	2024-07-25 16:38:02.887739458 -0400
+++ b/src/s2/s2builder.cc	2024-07-25 16:20:50.152040690 -0400
@@ -119,7 +119,7 @@
 #include "s2/util/bits/bits.h"
 #include "s2/util/gtl/compact_array.h"
 
-using absl::flat_hash_set;
+using shield_absl::flat_hash_set;
 using gtl::compact_array;
 using std::make_unique;
 using std::max;
@@ -578,7 +578,7 @@
 
   if (snapping_requested_) {
     S2PointIndex<SiteId> site_index;
-    auto _ = absl::MakeCleanup([&]() { tracker_.DoneSiteIndex(site_index); });
+    auto _ = shield_absl::MakeCleanup([&]() { tracker_.DoneSiteIndex(site_index); });
     AddForcedSites(&site_index);
     ChooseInitialSites(&site_index);
     if (!tracker_.FixSiteIndexTally(site_index)) return;
@@ -697,7 +697,7 @@
   // We need to build a list of intersections and add them afterwards so that
   // we don't reallocate vertices_ during the VisitCrossings() call.
   vector<S2Point> new_vertices;
-  auto _ = absl::MakeCleanup([&]() { tracker_.Untally(new_vertices); });
+  auto _ = shield_absl::MakeCleanup([&]() { tracker_.Untally(new_vertices); });
   s2shapeutil::VisitCrossingEdgePairs(
       input_edge_index, s2shapeutil::CrossingType::INTERIOR,
       [this, &new_vertices](const s2shapeutil::ShapeEdge& a,
@@ -752,7 +752,7 @@
   // Track the memory used by SortInputVertices() before calling it.
   if (!tracker_.Tally(input_vertices_.size() * sizeof(InputVertexKey))) return;
   vector<InputVertexKey> sorted_keys = SortInputVertices();
-  auto _ = absl::MakeCleanup([&]() { tracker_.Untally(sorted_keys); });
+  auto _ = shield_absl::MakeCleanup([&]() { tracker_.Untally(sorted_keys); });
   for (const InputVertexKey& key : sorted_keys) {
     const S2Point& vertex = input_vertices_[key.second];
     S2Point site = SnapSite(vertex);
@@ -1270,7 +1270,7 @@
   vector<vector<S2Point>> layer_vertices;
   BuildLayerEdges(&layer_edges, &layer_input_edge_ids,
                   &input_edge_id_set_lexicon);
-  auto _ = absl::MakeCleanup([&]() {
+  auto _ = shield_absl::MakeCleanup([&]() {
     for (size_t i = 0; i < layers_.size(); ++i) {
       tracker_.Untally(layer_edges[i]);
       tracker_.Untally(layer_input_edge_ids[i]);
@@ -1298,7 +1298,7 @@
       // never increases storage quadratically because there can be at most
       // two filtered vertices per edge.
       if (!tracker_.TallyFilterVertices(sites_.size(), layer_edges)) return;
-      auto _ = absl::MakeCleanup([this]() { tracker_.DoneFilterVertices(); });
+      auto _ = shield_absl::MakeCleanup([this]() { tracker_.DoneFilterVertices(); });
       layer_vertices.resize(layers_.size());
       vector<Graph::VertexId> filter_tmp;  // Temporary used by FilterVertices.
       for (size_t i = 0; i < layers_.size(); ++i) {
@@ -2133,7 +2133,7 @@
   // the rightmost node at each btree level because the values are added in
   // sorted order.
   int64 delta_bytes = GetBtreeMinBytesPerEntry<
-      absl::btree_multimap<S2CellId, S2PointIndex<SiteId>::PointData>>();
+      shield_absl::btree_multimap<S2CellId, S2PointIndex<SiteId>::PointData>>();
   site_index_bytes_ += delta_bytes;
   return Tally(delta_bytes);
 }
diff -u -r '--exclude=.git' a/src/s2/s2builder_graph.cc b/src/s2/s2builder_graph.cc
--- a/src/s2/s2builder_graph.cc	2024-07-25 16:38:02.891739472 -0400
+++ b/src/s2/s2builder_graph.cc	2024-07-25 16:20:50.144040657 -0400
@@ -624,7 +624,7 @@
   int edges_left_;
   vector<bool> used_;
   // A map of (outdegree(v) - indegree(v)) considering used edges only.
-  absl::btree_map<VertexId, int> excess_used_;
+  shield_absl::btree_map<VertexId, int> excess_used_;
 };
 
 vector<Graph::EdgePolyline> Graph::GetPolylines(
diff -u -r '--exclude=.git' a/src/s2/s2builder.h b/src/s2/s2builder.h
--- a/src/s2/s2builder.h	2024-07-25 16:38:02.891739472 -0400
+++ b/src/s2/s2builder.h	2024-07-25 16:20:50.120040559 -0400
@@ -836,10 +836,10 @@
   void AddExtraSites(const MutableS2ShapeIndex& input_edge_index);
   void MaybeAddExtraSites(InputEdgeId edge_id, const std::vector<SiteId>& chain,
                           const MutableS2ShapeIndex& input_edge_index,
-                          absl::flat_hash_set<InputEdgeId>* edges_to_resnap);
+                          shield_absl::flat_hash_set<InputEdgeId>* edges_to_resnap);
   void AddExtraSite(const S2Point& new_site,
                     const MutableS2ShapeIndex& input_edge_index,
-                    absl::flat_hash_set<InputEdgeId>* edges_to_resnap);
+                    shield_absl::flat_hash_set<InputEdgeId>* edges_to_resnap);
   S2Point GetSeparationSite(const S2Point& site_to_avoid,
                             const S2Point& v0, const S2Point& v1,
                             InputEdgeId input_edge_id) const;
diff -u -r '--exclude=.git' a/src/s2/s2builder_test.cc b/src/s2/s2builder_test.cc
--- a/src/s2/s2builder_test.cc	2024-07-25 16:38:02.891739472 -0400
+++ b/src/s2/s2builder_test.cc	2024-07-25 16:20:50.108040509 -0400
@@ -70,9 +70,9 @@
 #include "s2/s2testing.h"
 #include "s2/s2text_format.h"
 
-using absl::StrAppend;
-using absl::StrCat;
-using absl::string_view;
+using shield_absl::StrAppend;
+using shield_absl::StrCat;
+using shield_absl::string_view;
 using s2builderutil::GraphClone;
 using s2builderutil::IdentitySnapFunction;
 using s2builderutil::IntLatLngSnapFunction;
@@ -265,7 +265,7 @@
   // radius is S2::kIntersectionError because split_crossing_edges() is true.
   EXPECT_EQ(builder.options().edge_snap_radius(), S2::kIntersectionError);
   S1Angle max_deviation = builder.options().max_edge_deviation();
-  const int kIters = 50 * absl::GetFlag(FLAGS_iteration_multiplier);
+  const int kIters = 50 * shield_absl::GetFlag(FLAGS_iteration_multiplier);
   auto& rnd = S2Testing::rnd;
 
   // Test cases are constructed randomly, so not all tests are effective (i.e.
@@ -807,8 +807,8 @@
   for (const auto& polyline : output) {
     output_strs.push_back(s2textformat::ToString(*polyline));
   }
-  EXPECT_EQ(absl::StrJoin(expected_strs, "; "),
-            absl::StrJoin(output_strs, "; "));
+  EXPECT_EQ(shield_absl::StrJoin(expected_strs, "; "),
+            shield_absl::StrJoin(output_strs, "; "));
 }
 
 void TestPolylineVector(
@@ -829,8 +829,8 @@
   for (const auto& polyline : output) {
     output_strs.push_back(s2textformat::ToString(*polyline));
   }
-  EXPECT_EQ(absl::StrJoin(expected_strs, "; "),
-            absl::StrJoin(output_strs, "; "));
+  EXPECT_EQ(shield_absl::StrJoin(expected_strs, "; "),
+            shield_absl::StrJoin(output_strs, "; "));
 }
 
 void TestPolylineLayersBothEdgeTypes(
@@ -1344,7 +1344,7 @@
 
   auto& rnd = S2Testing::rnd;
   int non_degenerate = 0;
-  const int kIters = 8000 * absl::GetFlag(FLAGS_iteration_multiplier);
+  const int kIters = 8000 * shield_absl::GetFlag(FLAGS_iteration_multiplier);
   for (int iter = 0; iter < kIters; ++iter) {
     rnd.Reset(iter + 1);  // Easier to reproduce a specific case.
 
@@ -1407,7 +1407,7 @@
 }
 
 TEST(S2Builder, SelfIntersectionStressTest) {
-  const int kIters = 50 * absl::GetFlag(FLAGS_iteration_multiplier);
+  const int kIters = 50 * shield_absl::GetFlag(FLAGS_iteration_multiplier);
   for (int iter = 0; iter < kIters; ++iter) {
     S2Testing::rnd.Reset(iter + 1);  // Easier to reproduce a specific case.
     CycleTimer timer;
@@ -1460,7 +1460,7 @@
 
 TEST(S2Builder, FractalStressTest) {
   const int kIters =
-      (google::DEBUG_MODE ? 100 : 1000) * absl::GetFlag(FLAGS_iteration_multiplier);
+      (google::DEBUG_MODE ? 100 : 1000) * shield_absl::GetFlag(FLAGS_iteration_multiplier);
   for (int iter = 0; iter < kIters; ++iter) {
     S2Testing::rnd.Reset(iter + 1);  // Easier to reproduce a specific case.
     S2Testing::Fractal fractal;
diff -u -r '--exclude=.git' a/src/s2/s2builderutil_closed_set_normalizer_test.cc b/src/s2/s2builderutil_closed_set_normalizer_test.cc
--- a/src/s2/s2builderutil_closed_set_normalizer_test.cc	2024-07-25 16:38:02.891739472 -0400
+++ b/src/s2/s2builderutil_closed_set_normalizer_test.cc	2024-07-25 16:20:50.108040509 -0400
@@ -41,7 +41,7 @@
 #include "s2/s2shape_index.h"
 #include "s2/s2text_format.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using std::make_unique;
 using std::string;
 using std::unique_ptr;
diff -u -r '--exclude=.git' a/src/s2/s2builderutil_find_polygon_degeneracies_test.cc b/src/s2/s2builderutil_find_polygon_degeneracies_test.cc
--- a/src/s2/s2builderutil_find_polygon_degeneracies_test.cc	2024-07-25 16:38:02.891739472 -0400
+++ b/src/s2/s2builderutil_find_polygon_degeneracies_test.cc	2024-07-25 16:20:50.144040657 -0400
@@ -40,7 +40,7 @@
 #include "s2/s2testing.h"
 #include "s2/s2text_format.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using std::make_unique;
 using std::string;
 using std::vector;
diff -u -r '--exclude=.git' a/src/s2/s2builderutil_get_snapped_winding_delta.cc b/src/s2/s2builderutil_get_snapped_winding_delta.cc
--- a/src/s2/s2builderutil_get_snapped_winding_delta.cc	2024-07-25 16:38:02.891739472 -0400
+++ b/src/s2/s2builderutil_get_snapped_winding_delta.cc	2024-07-25 16:20:50.140040641 -0400
@@ -66,7 +66,7 @@
 #include "s2/s2pointutil.h"
 #include "s2/s2shape.h"
 
-using absl::Span;
+using shield_absl::Span;
 using std::make_pair;
 using std::vector;
 
@@ -94,7 +94,7 @@
 };
 
 // A map that allows finding all the input edges that start at a given point.
-using InputVertexEdgeMap = absl::btree_multimap<S2Point, EdgeSnap>;
+using InputVertexEdgeMap = shield_absl::btree_multimap<S2Point, EdgeSnap>;
 
 // The winding number returned when a usage error is detected.
 constexpr int kErrorResult = std::numeric_limits<int>::max();
@@ -156,7 +156,7 @@
 // Returns the change in winding number along the edge AB with respect to the
 // given edge chain.  This is simply the sum of the signed edge crossings.
 int GetEdgeWindingDelta(const S2Point& a, const S2Point& b,
-                        absl::Span<const S2Point> chain) {
+                        shield_absl::Span<const S2Point> chain) {
   S2_DCHECK_GT(chain.size(), 0);
 
   int delta = 0;
@@ -213,7 +213,7 @@
 
   // First we group all the incident edges by input edge id, to handle the
   // problem that input edges can map to either one or two snapped edges.
-  absl::btree_map<InputEdgeId, EdgeSnap> input_id_edge_map;
+  shield_absl::btree_map<InputEdgeId, EdgeSnap> input_id_edge_map;
   for (EdgeId e : incident_edges) {
     Graph::Edge edge = g.edge(e);
     for (InputEdgeId input_id : g.input_edge_ids(e)) {
@@ -413,7 +413,7 @@
   // more efficiently if duplicate edges are not being merged and the mapping
   // returned by Graph::GetInputEdgeOrder() is available.  The algorithm would
   // also be much simpler if input_edge_id were known to be degenerate.
-  absl::btree_map<VertexId, int> excess_degree_map;
+  shield_absl::btree_map<VertexId, int> excess_degree_map;
   for (EdgeId e = 0; e < g.num_edges(); ++e) {
     IdSetLexicon::IdSet id_set = g.input_edge_ids(e);
     for (InputEdgeId id : id_set) {
diff -u -r '--exclude=.git' a/src/s2/s2builderutil_get_snapped_winding_delta.h b/src/s2/s2builderutil_get_snapped_winding_delta.h
--- a/src/s2/s2builderutil_get_snapped_winding_delta.h	2024-07-25 16:38:02.891739472 -0400
+++ b/src/s2/s2builderutil_get_snapped_winding_delta.h	2024-07-25 16:20:50.156040706 -0400
@@ -151,7 +151,7 @@
 // See the function above for the remaining parameters and requirements.
 int GetSnappedWindingDelta(
     const S2Point& ref_in, S2Builder::Graph::VertexId ref_v,
-    absl::Span<const S2Builder::Graph::EdgeId> incident_edges,
+    shield_absl::Span<const S2Builder::Graph::EdgeId> incident_edges,
     const InputEdgeFilter &input_edge_filter, const S2Builder& builder,
     const S2Builder::Graph& g, S2Error* error);
 
diff -u -r '--exclude=.git' a/src/s2/s2builderutil_get_snapped_winding_delta_test.cc b/src/s2/s2builderutil_get_snapped_winding_delta_test.cc
--- a/src/s2/s2builderutil_get_snapped_winding_delta_test.cc	2024-07-25 16:38:02.891739472 -0400
+++ b/src/s2/s2builderutil_get_snapped_winding_delta_test.cc	2024-07-25 16:20:50.152040690 -0400
@@ -46,8 +46,8 @@
 #include "s2/s2testing.h"
 #include "s2/s2text_format.h"
 
-using absl::StrCat;
-using absl::string_view;
+using shield_absl::StrCat;
+using shield_absl::string_view;
 using std::make_unique;
 using std::string;
 using std::vector;
@@ -322,7 +322,7 @@
 }
 
 // Used to build a histogram of winding numbers.
-using WindingTally = absl::btree_map<int, int>;
+using WindingTally = shield_absl::btree_map<int, int>;
 
 // This S2Builder::Layer checks that the change in winding number due to
 // snapping computed by s2builderutil::GetSnappedWindingDelta() is correct for
diff -u -r '--exclude=.git' a/src/s2/s2builderutil_lax_polygon_layer_test.cc b/src/s2/s2builderutil_lax_polygon_layer_test.cc
--- a/src/s2/s2builderutil_lax_polygon_layer_test.cc	2024-07-25 16:38:02.891739472 -0400
+++ b/src/s2/s2builderutil_lax_polygon_layer_test.cc	2024-07-25 16:20:50.148040674 -0400
@@ -44,9 +44,9 @@
 #include "s2/s2shape.h"
 #include "s2/s2text_format.h"
 
-using absl::btree_set;
-using absl::flat_hash_map;
-using absl::string_view;
+using shield_absl::btree_set;
+using shield_absl::flat_hash_map;
+using shield_absl::string_view;
 using s2builderutil::IndexedLaxPolygonLayer;
 using s2builderutil::LaxPolygonLayer;
 using s2textformat::MakeLaxPolygonOrDie;
diff -u -r '--exclude=.git' a/src/s2/s2builderutil_lax_polyline_layer_test.cc b/src/s2/s2builderutil_lax_polyline_layer_test.cc
--- a/src/s2/s2builderutil_lax_polyline_layer_test.cc	2024-07-25 16:38:02.891739472 -0400
+++ b/src/s2/s2builderutil_lax_polyline_layer_test.cc	2024-07-25 16:20:50.148040674 -0400
@@ -36,7 +36,7 @@
 #include "s2/s2shape.h"
 #include "s2/s2text_format.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using s2builderutil::IndexedLaxPolylineLayer;
 using s2builderutil::LaxPolylineLayer;
 using s2textformat::MakeLaxPolylineOrDie;
diff -u -r '--exclude=.git' a/src/s2/s2builderutil_s2point_vector_layer_test.cc b/src/s2/s2builderutil_s2point_vector_layer_test.cc
--- a/src/s2/s2builderutil_s2point_vector_layer_test.cc	2024-07-25 16:38:02.891739472 -0400
+++ b/src/s2/s2builderutil_s2point_vector_layer_test.cc	2024-07-25 16:20:50.108040509 -0400
@@ -38,7 +38,7 @@
 #include "s2/s2shape.h"
 #include "s2/s2text_format.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using s2builderutil::IndexedS2PointVectorLayer;
 using s2builderutil::S2PointVectorLayer;
 using s2textformat::MakePointOrDie;
diff -u -r '--exclude=.git' a/src/s2/s2builderutil_s2polygon_layer.h b/src/s2/s2builderutil_s2polygon_layer.h
--- a/src/s2/s2builderutil_s2polygon_layer.h	2024-07-25 16:38:02.891739472 -0400
+++ b/src/s2/s2builderutil_s2polygon_layer.h	2024-07-25 16:20:50.112040525 -0400
@@ -139,7 +139,7 @@
                      std::vector<std::unique_ptr<S2Loop>>* loops) const;
   void AppendEdgeLabels(const Graph& g,
                         const std::vector<Graph::EdgeLoop>& edge_loops);
-  using LoopMap = absl::btree_map<S2Loop*, std::pair<int, bool>>;
+  using LoopMap = shield_absl::btree_map<S2Loop*, std::pair<int, bool>>;
   void InitLoopMap(const std::vector<std::unique_ptr<S2Loop>>& loops,
                    LoopMap* loop_map) const;
   void ReorderEdgeLabels(const LoopMap& loop_map);
diff -u -r '--exclude=.git' a/src/s2/s2builderutil_s2polygon_layer_test.cc b/src/s2/s2builderutil_s2polygon_layer_test.cc
--- a/src/s2/s2builderutil_s2polygon_layer_test.cc	2024-07-25 16:38:02.891739472 -0400
+++ b/src/s2/s2builderutil_s2polygon_layer_test.cc	2024-07-25 16:20:50.152040690 -0400
@@ -40,9 +40,9 @@
 #include "s2/s2shape.h"
 #include "s2/s2text_format.h"
 
-using absl::flat_hash_map;
-using absl::flat_hash_set;
-using absl::string_view;
+using shield_absl::flat_hash_map;
+using shield_absl::flat_hash_set;
+using shield_absl::string_view;
 using s2builderutil::IndexedS2PolygonLayer;
 using s2builderutil::S2PolygonLayer;
 using s2textformat::MakePolylineOrDie;
diff -u -r '--exclude=.git' a/src/s2/s2builderutil_s2polyline_layer_test.cc b/src/s2/s2builderutil_s2polyline_layer_test.cc
--- a/src/s2/s2builderutil_s2polyline_layer_test.cc	2024-07-25 16:38:02.891739472 -0400
+++ b/src/s2/s2builderutil_s2polyline_layer_test.cc	2024-07-25 16:20:50.112040525 -0400
@@ -37,7 +37,7 @@
 #include "s2/s2shape.h"
 #include "s2/s2text_format.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using s2builderutil::IndexedS2PolylineLayer;
 using s2builderutil::S2PolylineLayer;
 using s2textformat::MakePolylineOrDie;
diff -u -r '--exclude=.git' a/src/s2/s2builderutil_s2polyline_vector_layer_test.cc b/src/s2/s2builderutil_s2polyline_vector_layer_test.cc
--- a/src/s2/s2builderutil_s2polyline_vector_layer_test.cc	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2builderutil_s2polyline_vector_layer_test.cc	2024-07-25 16:20:50.156040706 -0400
@@ -38,7 +38,7 @@
 #include "s2/s2shape.h"
 #include "s2/s2text_format.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using s2builderutil::IndexedS2PolylineVectorLayer;
 using s2builderutil::S2PolylineVectorLayer;
 using s2textformat::MakePolylineOrDie;
@@ -73,8 +73,8 @@
   for (const auto& polyline : output) {
     output_strs.push_back(s2textformat::ToString(*polyline));
   }
-  EXPECT_EQ(absl::StrJoin(expected_strs, "; "),
-            absl::StrJoin(output_strs, "; "));
+  EXPECT_EQ(shield_absl::StrJoin(expected_strs, "; "),
+            shield_absl::StrJoin(output_strs, "; "));
 }
 
 // Convenience function that tests both directed and undirected edges.
diff -u -r '--exclude=.git' a/src/s2/s2builderutil_snap_functions_test.cc b/src/s2/s2builderutil_snap_functions_test.cc
--- a/src/s2/s2builderutil_snap_functions_test.cc	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2builderutil_snap_functions_test.cc	2024-07-25 16:20:50.124040575 -0400
@@ -56,9 +56,9 @@
 #include "s2/s2text_format.h"
 #include "s2/util/math/mathutil.h"
 
-using absl::flat_hash_map;
-using absl::flat_hash_set;
-using absl::string_view;
+using shield_absl::flat_hash_map;
+using shield_absl::flat_hash_set;
+using shield_absl::string_view;
 using s2builderutil::IntLatLngSnapFunction;
 using s2builderutil::S2CellIdSnapFunction;
 using std::abs;
@@ -382,7 +382,7 @@
     if (--num_to_print >= 0) {
       R2Point uv = id.GetCenterUV();
       const pair<S2CellId, S2CellId>& nbrs = best_configs.find(id)->second;
-      puts(absl::StrFormat("  %s = %.15f u=%7.4f v=%7.4f %s %s %s\n", label,
+      puts(shield_absl::StrFormat("  %s = %.15f u=%7.4f v=%7.4f %s %s %s\n", label,
                            entry.first, uv[0], uv[1], id.ToToken(),
                            nbrs.first.ToToken().c_str(), nbrs.second.ToToken())
                .c_str());
@@ -673,7 +673,7 @@
     const LatLngConfig& config = entry.second;
     int64 scale = config.scale;
     if (--num_to_print >= 0) {
-      puts(absl::StrFormat("  %s = %.15f %s %s %s\n", label, entry.first,
+      puts(shield_absl::StrFormat("  %s = %.15f %s %s %s\n", label, entry.first,
                            s2textformat::ToString(ToPoint(config.ll0, scale)),
                            s2textformat::ToString(ToPoint(config.ll1, scale)),
                            s2textformat::ToString(ToPoint(config.ll2, scale)))
diff -u -r '--exclude=.git' a/src/s2/s2builderutil_testing.cc b/src/s2/s2builderutil_testing.cc
--- a/src/s2/s2builderutil_testing.cc	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2builderutil_testing.cc	2024-07-25 16:20:50.116040541 -0400
@@ -101,7 +101,7 @@
     // There may be errors in more than one dimension, so we append to the
     // existing error text.
     string label;
-    if (dimension_ >= 0) label = absl::StrFormat("Dimension %d: ", dimension_);
+    if (dimension_ >= 0) label = shield_absl::StrFormat("Dimension %d: ", dimension_);
     error->Init(S2Error::FAILED_PRECONDITION,
                 "%s%sMissing edges: %s Extra edges: %s\n",
                 error->text().c_str(), label.c_str(),
diff -u -r '--exclude=.git' a/src/s2/s2cap.cc b/src/s2/s2cap.cc
--- a/src/s2/s2cap.cc	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2cap.cc	2024-07-25 16:20:50.148040674 -0400
@@ -343,7 +343,7 @@
   center_ = S2Point(x, y, z);
   radius_ = S1ChordAngle::FromLength2(decoder->getdouble());
 
-  if (absl::GetFlag(FLAGS_s2debug)) {
+  if (shield_absl::GetFlag(FLAGS_s2debug)) {
     S2_CHECK(is_valid()) << "Invalid S2Cap: " << *this;
   }
   return true;
diff -u -r '--exclude=.git' a/src/s2/s2cell_id.cc b/src/s2/s2cell_id.cc
--- a/src/s2/s2cell_id.cc	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2cell_id.cc	2024-07-25 16:20:50.124040575 -0400
@@ -43,8 +43,8 @@
 #include "s2/s2latlng.h"
 #include "s2/s2point.h"
 
-using absl::StrCat;
-using absl::string_view;
+using shield_absl::StrCat;
+using shield_absl::string_view;
 using S2::internal::kInvertMask;
 using S2::internal::kPosToIJ;
 using S2::internal::kPosToOrientation;
@@ -110,9 +110,9 @@
   }
 }
 
-static absl::once_flag flag;
+static shield_absl::once_flag flag;
 inline static void MaybeInit() {
-  absl::call_once(flag, []{
+  shield_absl::call_once(flag, []{
     InitLookupCell(0, 0, 0, 0, 0, 0);
     InitLookupCell(0, 0, 0, kSwapMask, 0, kSwapMask);
     InitLookupCell(0, 0, 0, kInvertMask, 0, kInvertMask);
@@ -273,7 +273,7 @@
 
   // Note that this value gets shifted one bit to the left at the end
   // of the function.
-  uint64 n = absl::implicit_cast<uint64>(face) << (kPosBits - 1);
+  uint64 n = shield_absl::implicit_cast<uint64>(face) << (kPosBits - 1);
 
   // Alternating faces have opposite Hilbert curve orientations; this
   // is necessary in order for all faces to have a right-handed
@@ -606,7 +606,7 @@
 
 string S2CellId::ToString() const {
   if (!is_valid()) {
-    return StrCat("Invalid: ", absl::Hex(id(), absl::kZeroPad16));
+    return StrCat("Invalid: ", shield_absl::Hex(id(), shield_absl::kZeroPad16));
   }
   string out = StrCat(face(), "/");
   for (int current_level = 1; current_level <= level(); ++current_level) {
diff -u -r '--exclude=.git' a/src/s2/s2cell_id.h b/src/s2/s2cell_id.h
--- a/src/s2/s2cell_id.h	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2cell_id.h	2024-07-25 16:20:50.108040509 -0400
@@ -386,7 +386,7 @@
   // "x" is an invalid cell id.  All tokens are alphanumeric strings.
   // FromToken() returns S2CellId::None() for malformed inputs.
   std::string ToToken() const;
-  static S2CellId FromToken(absl::string_view token);
+  static S2CellId FromToken(shield_absl::string_view token);
 
   // Legacy coder for S2CellId that delegates to the token representation.
   // Storage is variable depending on the level of the cell.
@@ -418,7 +418,7 @@
   //
   // The method name includes "Debug" in order to avoid possible confusion
   // with FromToken() above.
-  static S2CellId FromDebugString(absl::string_view str);
+  static S2CellId FromDebugString(shield_absl::string_view str);
 
   // Return the four cells that are adjacent across the cell's four edges.
   // Neighbors are returned in the order defined by S2Cell::GetEdge.  All
@@ -472,7 +472,7 @@
   // the leaf cell with the given (i,j)-coordinates.
   static R2Rect IJLevelToBoundUV(int ij[2], int level);
 
-  // When S2CellId is used as a key in one of the absl::btree container types,
+  // When S2CellId is used as a key in one of the shield_absl::btree container types,
   // indicate that linear rather than binary search should be used.  This is
   // much faster when the comparison function is cheap.
   typedef std::true_type absl_btree_prefer_linear_node_search;
@@ -723,24 +723,24 @@
 
 // Hasher for S2CellId.
 // Does *not* need to be specified explicitly; this will be used by default for
-// absl::flat_hash_map/set.
+// shield_absl::flat_hash_map/set.
 //
 // TODO(b/259279783): Remove rotation once mixing function on 32-bit systems is
 // fixed.
 template <typename H>
 H AbslHashValue(H h, S2CellId id) {
   if (sizeof(void*) == 4) {
-    return H::combine(std::move(h), id.id(), absl::rotr(id.id(), 32));
+    return H::combine(std::move(h), id.id(), shield_absl::rotr(id.id(), 32));
   }
   return H::combine(std::move(h), id.id());
 }
 
 // Legacy hash functor for S2CellId. This only exists for backwards
 // compatibility with old hash types like std::unordered_map that don't use
-// absl::Hash natively.
+// shield_absl::Hash natively.
 struct S2CellIdHash {
   size_t operator()(S2CellId id) const {
-    return absl::Hash<S2CellId>()(id);
+    return shield_absl::Hash<S2CellId>()(id);
   }
 };
 
diff -u -r '--exclude=.git' a/src/s2/s2cell_id_test.cc b/src/s2/s2cell_id_test.cc
--- a/src/s2/s2cell_id_test.cc	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2cell_id_test.cc	2024-07-25 16:20:50.120040559 -0400
@@ -48,8 +48,8 @@
 #include "s2/s2testing.h"
 #include "s2/util/coding/coder.h"
 
-using absl::flat_hash_map;
-using absl::string_view;
+using shield_absl::flat_hash_map;
+using shield_absl::string_view;
 using S2::internal::kPosToOrientation;
 using std::fabs;
 using std::min;
diff -u -r '--exclude=.git' a/src/s2/s2cell_index.cc b/src/s2/s2cell_index.cc
--- a/src/s2/s2cell_index.cc	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2cell_index.cc	2024-07-25 16:20:50.124040575 -0400
@@ -24,7 +24,7 @@
 #include "s2/s2cell_id.h"
 #include "s2/s2cell_union.h"
 
-using absl::flat_hash_set;
+using shield_absl::flat_hash_set;
 using std::vector;
 
 using Label = S2CellIndex::Label;
diff -u -r '--exclude=.git' a/src/s2/s2cell_index.h b/src/s2/s2cell_index.h
--- a/src/s2/s2cell_index.h	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2cell_index.h	2024-07-25 16:20:50.152040690 -0400
@@ -180,13 +180,13 @@
 
   // Convenience function that returns the labels of all indexed cells that
   // intersect the given S2CellUnion "target".
-  absl::flat_hash_set<Label> GetIntersectingLabels(const S2CellUnion& target)
+  shield_absl::flat_hash_set<Label> GetIntersectingLabels(const S2CellUnion& target)
       const;
 
   // This version can be more efficient when it is called many times, since it
   // does not require allocating a new set on each call.
   void GetIntersectingLabels(const S2CellUnion& target,
-                             absl::flat_hash_set<Label>* labels) const;
+                             shield_absl::flat_hash_set<Label>* labels) const;
 
  private:
   // Represents a node in the set of non-overlapping leaf cell ranges.
diff -u -r '--exclude=.git' a/src/s2/s2cell_index_test.cc b/src/s2/s2cell_index_test.cc
--- a/src/s2/s2cell_index_test.cc	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2cell_index_test.cc	2024-07-25 16:20:50.120040559 -0400
@@ -33,8 +33,8 @@
 #include "s2/s2cell_union.h"
 #include "s2/s2testing.h"
 
-using absl::flat_hash_set;
-using absl::string_view;
+using shield_absl::flat_hash_set;
+using shield_absl::string_view;
 using std::pair;
 using std::string;
 using std::vector;
diff -u -r '--exclude=.git' a/src/s2/s2cell_iterator.h b/src/s2/s2cell_iterator.h
--- a/src/s2/s2cell_iterator.h	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2cell_iterator.h	2024-07-25 16:20:50.140040641 -0400
@@ -86,7 +86,7 @@
   //       "We require an object implementing the S2CellIterator API.");
   //   }
   template <typename T>
-  using ImplementedBy = std::is_convertible<absl::decay_t<T>*, S2CellIterator*>;
+  using ImplementedBy = std::is_convertible<shield_absl::decay_t<T>*, S2CellIterator*>;
 
   S2CellIterator() = default;
   virtual ~S2CellIterator() = default;
diff -u -r '--exclude=.git' a/src/s2/s2cell_iterator_join.h b/src/s2/s2cell_iterator_join.h
--- a/src/s2/s2cell_iterator_join.h	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2cell_iterator_join.h	2024-07-25 16:20:50.120040559 -0400
@@ -156,13 +156,13 @@
   // Returns a minimal set of cells that cover the range of the iterator to seed
   // the tolerant join set.  Returns empty list if the iterator has no cells.
   template <typename Iterator>
-  static absl::InlinedVector<S2CellId, 4> GetIteratorCovering(Iterator& iter);
+  static shield_absl::InlinedVector<S2CellId, 4> GetIteratorCovering(Iterator& iter);
 
   // Forms all possible pairs of cells from two lists.  Visits any pairs that
   // are closer to each other than the tolerance.
   template <typename Visitor>
-  bool ProcessNearbyCellPairs(absl::InlinedVector<S2CellId, 4> cells_a,
-                              absl::InlinedVector<S2CellId, 4> cells_b,
+  bool ProcessNearbyCellPairs(shield_absl::InlinedVector<S2CellId, 4> cells_a,
+                              shield_absl::InlinedVector<S2CellId, 4> cells_b,
                               Visitor& visitor);
 
   // Processes a pair of cells known to be within tolerance of each other.
@@ -269,9 +269,9 @@
 
 template <typename A, typename B>
 template <typename Iterator>
-absl::InlinedVector<S2CellId, 4> S2CellIteratorJoin<A, B>::GetIteratorCovering(
+shield_absl::InlinedVector<S2CellId, 4> S2CellIteratorJoin<A, B>::GetIteratorCovering(
     Iterator& iter) {
-  absl::InlinedVector<S2CellId, 4> cells;
+  shield_absl::InlinedVector<S2CellId, 4> cells;
 
   // Position at the front, if the iterator's empty, we're done.
   iter.Begin();
@@ -309,7 +309,7 @@
 template <typename A, typename B>
 template <typename Visitor>
 bool S2CellIteratorJoin<A, B>::TolerantJoin(Visitor& visitor) {
-  absl::InlinedVector<S2CellId, 4> cells_a, cells_b;
+  shield_absl::InlinedVector<S2CellId, 4> cells_a, cells_b;
 
   // Seed cells with a covering for iterator A, quit if empty.
   cells_a = GetIteratorCovering(iter_a_);
@@ -329,8 +329,8 @@
 template <typename A, typename B>
 template <typename Visitor>
 bool S2CellIteratorJoin<A, B>::ProcessNearbyCellPairs(
-    absl::InlinedVector<S2CellId, 4> cells_a,
-    absl::InlinedVector<S2CellId, 4> cells_b, Visitor& visitor) {
+    shield_absl::InlinedVector<S2CellId, 4> cells_a,
+    shield_absl::InlinedVector<S2CellId, 4> cells_b, Visitor& visitor) {
   for (const auto& id_a : cells_a) {
     const S2Cell cell_a(id_a);
 
@@ -389,7 +389,7 @@
   }
 
   // Too many matches, recurse on A or B, whichever's larger.
-  absl::InlinedVector<S2CellId, 4> cells_a, cells_b;
+  shield_absl::InlinedVector<S2CellId, 4> cells_a, cells_b;
 
   if (num_covered_a > num_covered_b) {
     cells_b.push_back(cell_b.id());
diff -u -r '--exclude=.git' a/src/s2/s2cell_iterator_join_test.cc b/src/s2/s2cell_iterator_join_test.cc
--- a/src/s2/s2cell_iterator_join_test.cc	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2cell_iterator_join_test.cc	2024-07-25 16:20:50.156040706 -0400
@@ -34,7 +34,7 @@
 
 namespace {
 
-using ::absl::string_view;
+using ::shield_absl::string_view;
 using std::vector;
 using ::testing::Contains;
 using ::testing::Eq;
@@ -59,8 +59,8 @@
 }
 
 // Builds a btree_map from a list of token to use with MockS2CellIterator.
-absl::btree_map<S2CellId, int> TokenMap(absl::Span<const string_view> tokens) {
-  absl::btree_map<S2CellId, int> map;
+shield_absl::btree_map<S2CellId, int> TokenMap(shield_absl::Span<const string_view> tokens) {
+  shield_absl::btree_map<S2CellId, int> map;
   int count = 0;
   for (const auto& token : tokens) {
     map.emplace(S2CellId::FromToken(token), ++count);
@@ -77,8 +77,8 @@
 }
 
 TEST(S2CellJoinIterator, ExactJoinWorks) {
-  auto cpa_map = TokenMap(absl::MakeSpan(kCentralParkATokens));
-  auto cpb_map = TokenMap(absl::MakeSpan(kCentralParkBTokens));
+  auto cpa_map = TokenMap(shield_absl::MakeSpan(kCentralParkATokens));
+  auto cpb_map = TokenMap(shield_absl::MakeSpan(kCentralParkBTokens));
 
   vector<std::pair<S2CellId, S2CellId>> rows;
   MakeS2CellIteratorJoin(MakeMockS2CellIterator(&cpa_map),
@@ -107,8 +107,8 @@
 }
 
 TEST(S2CellJoinIterator, ExactFalseJoinReturnsImmediately) {
-  auto cpa_map = TokenMap(absl::MakeSpan(kCentralParkATokens));
-  auto cpb_map = TokenMap(absl::MakeSpan(kCentralParkBTokens));
+  auto cpa_map = TokenMap(shield_absl::MakeSpan(kCentralParkATokens));
+  auto cpb_map = TokenMap(shield_absl::MakeSpan(kCentralParkBTokens));
 
   vector<std::pair<S2CellId, S2CellId>> rows;
   bool cancelled = MakeS2CellIteratorJoin(MakeMockS2CellIterator(&cpa_map),
@@ -123,8 +123,8 @@
 }
 
 TEST(S2CellJoinIterator, TolerantFalseJoinReturnsImmediately) {
-  auto cpa_map = TokenMap(absl::MakeSpan(kCentralParkATokens));
-  auto cpb_map = TokenMap(absl::MakeSpan(kCentralParkBTokens));
+  auto cpa_map = TokenMap(shield_absl::MakeSpan(kCentralParkATokens));
+  auto cpb_map = TokenMap(shield_absl::MakeSpan(kCentralParkBTokens));
 
   // Just need a non-zero tolerance to trigger tolerant join logic.
   auto dist = S1ChordAngle::Degrees(.001);
@@ -173,12 +173,12 @@
 }
 
 TEST(S2CellJoinIterator, NearJoinWorks) {
-  auto cpa_map = TokenMap(absl::MakeSpan(kCentralParkATokens));
-  auto cpb_map = TokenMap(absl::MakeSpan(kCentralParkBTokens));
+  auto cpa_map = TokenMap(shield_absl::MakeSpan(kCentralParkATokens));
+  auto cpb_map = TokenMap(shield_absl::MakeSpan(kCentralParkBTokens));
 
   const S1ChordAngle kTolerance = S1ChordAngle::Degrees(1);
 
-  absl::flat_hash_set<std::pair<S2CellId, S2CellId>> rows;
+  shield_absl::flat_hash_set<std::pair<S2CellId, S2CellId>> rows;
   MakeS2CellIteratorJoin(MakeMockS2CellIterator(&cpa_map),
                          MakeMockS2CellIterator(&cpb_map), kTolerance)
       .Join([&rows](const MockS2CellIterator<int>& iter_a,
diff -u -r '--exclude=.git' a/src/s2/s2cell_iterator_testing.h b/src/s2/s2cell_iterator_testing.h
--- a/src/s2/s2cell_iterator_testing.h	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2cell_iterator_testing.h	2024-07-25 16:20:50.144040657 -0400
@@ -19,7 +19,7 @@
 #include "absl/container/btree_map.h"
 #include "s2/s2cell_iterator.h"
 
-// A mock iterator for testing.  Iterates an absl::btree_map mapping S2CellId to
+// A mock iterator for testing.  Iterates an shield_absl::btree_map mapping S2CellId to
 // another type.  Rather than instantiating directly, consider using
 // MakeMockS2CellIterator for convenience.
 
@@ -28,7 +28,7 @@
  public:
   // We use a btree_map because we require that S2CellIterator containers be
   // ordered for fast seek and locate.
-  using CellMap = absl::btree_map<S2CellId, T>;
+  using CellMap = shield_absl::btree_map<S2CellId, T>;
 
   // Build an iterator from a btree map.  The map must persist for the lifetime
   // of the iterator, so we take it by const pointer to avoid binding
@@ -112,7 +112,7 @@
 // This function explicitly supports inferring the template parameter T.
 template <typename T>
 MockS2CellIterator<T> MakeMockS2CellIterator(
-    const absl::btree_map<S2CellId, T>* map) {
+    const shield_absl::btree_map<S2CellId, T>* map) {
   return MockS2CellIterator<T>(map);
 }
 
diff -u -r '--exclude=.git' a/src/s2/s2cell_iterator_testing_test.cc b/src/s2/s2cell_iterator_testing_test.cc
--- a/src/s2/s2cell_iterator_testing_test.cc	2024-07-25 16:37:56.183717309 -0400
+++ b/src/s2/s2cell_iterator_testing_test.cc	2024-07-25 16:20:50.132040607 -0400
@@ -25,7 +25,7 @@
 
 namespace {
 
-using ::absl::string_view;
+using ::shield_absl::string_view;
 using ::testing::Eq;
 using ::testing::Ge;
 using ::testing::IsFalse;
@@ -34,7 +34,7 @@
 S2CellId MakeId(string_view token) { return S2CellId::FromToken(token); }
 
 TEST(MockIterator, BasicFunctionality) {
-  absl::btree_map<S2CellId, int> map = {
+  shield_absl::btree_map<S2CellId, int> map = {
       std::make_pair(MakeId("89c259"), 0),
       std::make_pair(MakeId("89c2598c"), 1),
       std::make_pair(MakeId("89c2599"), 2),
diff -u -r '--exclude=.git' a/src/s2/s2cell_range_iterator.h b/src/s2/s2cell_range_iterator.h
--- a/src/s2/s2cell_range_iterator.h	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2cell_range_iterator.h	2024-07-25 16:20:50.120040559 -0400
@@ -119,7 +119,7 @@
           typename std::enable_if<S2CellIterator::ImplementedBy<Iterator>{},
                                   bool>::type = true>
 auto MakeS2CellRangeIterator(Iterator&& iter) {
-  return S2CellRangeIterator<absl::decay_t<Iterator>>(
+  return S2CellRangeIterator<shield_absl::decay_t<Iterator>>(
       std::forward<Iterator>(iter));
 }
 
diff -u -r '--exclude=.git' a/src/s2/s2cell_test.cc b/src/s2/s2cell_test.cc
--- a/src/s2/s2cell_test.cc	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2cell_test.cc	2024-07-25 16:20:50.112040525 -0400
@@ -56,9 +56,9 @@
 #include "s2/s2text_format.h"
 #include "s2/util/coding/coder.h"
 
-using absl::flat_hash_map;
-using absl::StrCat;
-using absl::string_view;
+using shield_absl::flat_hash_map;
+using shield_absl::StrCat;
+using shield_absl::string_view;
 using S2::internal::kSwapMask;
 using s2textformat::MakePointOrDie;
 using std::fabs;
@@ -322,7 +322,7 @@
   double min_error = min_value - min_metric.GetValue(level);
   double max_error = max_metric.GetValue(level) - max_value;
   double avg_error = fabs(avg_metric.GetValue(level) - avg_value);
-  puts(absl::StrFormat(
+  puts(shield_absl::StrFormat(
            "%-10s (%6.0f samples, tolerance %8.3g) - min %9.4g (%9.3g : %9.3g) "
            "max %9.4g (%9.3g : %9.3g), avg %9.4g (%9.3g : %9.3g)\n",
            label, count, tolerance, min_value, min_error / min_value,
@@ -517,7 +517,7 @@
 }
 
 TEST(S2Cell, GetDistanceToPoint) {
-  S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed));
+  S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed));
   for (int iter = 0; iter < 1000; ++iter) {
     SCOPED_TRACE(StrCat("Iteration ", iter));
     S2Cell cell(S2Testing::GetRandomCellId());
@@ -617,7 +617,7 @@
 }
 
 TEST(S2Cell, GetDistanceToEdge) {
-  S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed));
+  S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed));
   for (int iter = 0; iter < 1000; ++iter) {
     SCOPED_TRACE(StrCat("Iteration ", iter));
     S2Cell cell(S2Testing::GetRandomCellId());
diff -u -r '--exclude=.git' a/src/s2/s2cell_union.cc b/src/s2/s2cell_union.cc
--- a/src/s2/s2cell_union.cc	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2cell_union.cc	2024-07-25 16:20:50.136040625 -0400
@@ -539,7 +539,7 @@
 
   uint64 num_cells = decoder->get64();
   if (num_cells > static_cast<size_t>(
-                      absl::GetFlag(FLAGS_s2cell_union_decode_max_num_cells))) {
+                      shield_absl::GetFlag(FLAGS_s2cell_union_decode_max_num_cells))) {
     return false;
   }
 
@@ -557,7 +557,7 @@
 
 std::string S2CellUnion::ToString() const {
   static const size_t kMaxCount = 500;
-  string output = absl::StrCat("Size:", size(), " S2CellIds:");
+  string output = shield_absl::StrCat("Size:", size(), " S2CellIds:");
   for (int i = 0, limit = min(kMaxCount, size()); i < limit; ++i) {
     if (i > 0) output += ",";
     output += cell_id(i).ToToken();
diff -u -r '--exclude=.git' a/src/s2/s2cell_union.h b/src/s2/s2cell_union.h
--- a/src/s2/s2cell_union.h	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2cell_union.h	2024-07-25 16:20:50.104040492 -0400
@@ -411,7 +411,7 @@
 
 inline S2CellUnion S2CellUnion::FromVerbatim(std::vector<S2CellId> cell_ids) {
   S2CellUnion result(std::move(cell_ids), VERBATIM);
-  S2_DCHECK(!absl::GetFlag(FLAGS_s2debug) || result.IsValid());
+  S2_DCHECK(!shield_absl::GetFlag(FLAGS_s2debug) || result.IsValid());
   return result;
 }
 
@@ -449,7 +449,7 @@
 
 template <typename H>
 H AbslHashValue(H h, const S2CellUnion& u) {
-  return H::combine(std::move(h), absl::HashOf(u.cell_ids()));
+  return H::combine(std::move(h), shield_absl::HashOf(u.cell_ids()));
 }
 
 inline S2CellId S2CellUnion::Iterator::id() const {
@@ -485,7 +485,7 @@
 
 inline void S2CellUnion::Iterator::Seek(S2CellId target) {
   S2_DCHECK_NE(cell_union_, nullptr);
-  iter_ = absl::c_lower_bound(cell_union_->cell_ids_, target);
+  iter_ = shield_absl::c_lower_bound(cell_union_->cell_ids_, target);
 }
 
 inline bool S2CellUnion::Iterator::Locate(const S2Point& target) {
diff -u -r '--exclude=.git' a/src/s2/s2cell_union_test.cc b/src/s2/s2cell_union_test.cc
--- a/src/s2/s2cell_union_test.cc	2024-07-25 16:38:02.895739485 -0400
+++ b/src/s2/s2cell_union_test.cc	2024-07-25 16:20:50.124040575 -0400
@@ -51,7 +51,7 @@
 
 S2_DECLARE_bool(s2debug);
 
-using absl::StrCat;
+using shield_absl::StrCat;
 using std::max;
 using std::min;
 using std::vector;
@@ -112,12 +112,12 @@
 TEST(S2CellUnion, InvalidCellIdNotValidWithDebugFlag) {
   // Manually save and restore flag, to preserve test state in opensource
   // without gflags.
-  const bool saved_s2debug = absl::GetFlag(FLAGS_s2debug);
-  absl::SetFlag(&FLAGS_s2debug, false);
+  const bool saved_s2debug = shield_absl::GetFlag(FLAGS_s2debug);
+  shield_absl::SetFlag(&FLAGS_s2debug, false);
   ASSERT_FALSE(S2CellId::None().is_valid());
   auto cell_union = S2CellUnion::FromVerbatim({S2CellId::None()});
   EXPECT_FALSE(cell_union.IsValid());
-  absl::SetFlag(&FLAGS_s2debug, saved_s2debug);
+  shield_absl::SetFlag(&FLAGS_s2debug, saved_s2debug);
 }
 
 TEST(S2CellUnion, IsNormalized) {
@@ -568,7 +568,7 @@
 TEST(S2CellUnion, RefuseToDecode) {
   vector<S2CellId> cellids;
   S2CellId id = S2CellId::Begin(S2CellId::kMaxLevel);
-  for (int i = 0; i <= absl::GetFlag(FLAGS_s2cell_union_decode_max_num_cells);
+  for (int i = 0; i <= shield_absl::GetFlag(FLAGS_s2cell_union_decode_max_num_cells);
        ++i) {
     cellids.push_back(id);
     id = id.next();
@@ -673,7 +673,7 @@
 }
 
 TEST(S2CellUnion, SupportsAbslHash) {
-  EXPECT_TRUE(absl::VerifyTypeImplementsAbslHashCorrectly(
+  EXPECT_TRUE(shield_absl::VerifyTypeImplementsAbslHashCorrectly(
       {S2CellUnion({S2CellId::FromFace(1)}),
        S2CellUnion({S2CellId::FromFace(1), S2CellId::FromFace(2).child_begin(1),
                     S2CellId::FromFace(3).child_begin(2)})}));
diff -u -r '--exclude=.git' a/src/s2/s2closest_cell_query_base.h b/src/s2/s2closest_cell_query_base.h
--- a/src/s2/s2closest_cell_query_base.h	2024-07-25 16:38:02.899739497 -0400
+++ b/src/s2/s2closest_cell_query_base.h	2024-07-25 16:20:50.120040559 -0400
@@ -317,7 +317,7 @@
   // when result_set_ is used so that we could use a priority queue instead.
   Result result_singleton_;
   std::vector<Result> result_vector_;
-  absl::btree_set<Result> result_set_;
+  shield_absl::btree_set<Result> result_set_;
 
   // When the results are stored in a btree_set (see above), usually
   // duplicates can be removed simply by inserting candidate cells in the
@@ -337,7 +337,7 @@
   // TODO(ericv): Check whether it is faster to avoid duplicates by default
   // (even when Options::max_results() == 1), rather than just when we need to.
   bool avoid_duplicates_;
-  absl::flat_hash_set<LabelledCell> tested_cells_;
+  shield_absl::flat_hash_set<LabelledCell> tested_cells_;
 
   // The algorithm maintains a priority queue of unprocessed S2CellIds, sorted
   // in increasing order of distance from the target.
@@ -359,7 +359,7 @@
     }
   };
   using CellQueue =
-      std::priority_queue<QueueEntry, absl::InlinedVector<QueueEntry, 16>>;
+      std::priority_queue<QueueEntry, shield_absl::InlinedVector<QueueEntry, 16>>;
   CellQueue queue_;
 
   // Used to iterate over the contents of an S2CellIndex range.  It is defined
diff -u -r '--exclude=.git' a/src/s2/s2closest_cell_query_test.cc b/src/s2/s2closest_cell_query_test.cc
--- a/src/s2/s2closest_cell_query_test.cc	2024-07-25 16:38:02.899739497 -0400
+++ b/src/s2/s2closest_cell_query_test.cc	2024-07-25 16:20:50.156040706 -0400
@@ -322,7 +322,7 @@
   vector<S2Cap> index_caps;
   vector<unique_ptr<S2CellIndex>> indexes;
   for (int i = 0; i < num_indexes; ++i) {
-    S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed) + i);
+    S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed) + i);
     index_caps.push_back(S2Cap(S2Testing::RandomPoint(), kTestCapRadius));
     auto index = make_unique<S2CellIndex>();
     factory.AddCells(index_caps.back(), num_cells, index.get());
@@ -330,7 +330,7 @@
     indexes.push_back(std::move(index));
   }
   for (int i = 0; i < num_queries; ++i) {
-    S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed) + i);
+    S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed) + i);
     int i_index = S2Testing::rnd.Uniform(num_indexes);
     const S2Cap& index_cap = index_caps[i_index];
 
@@ -421,15 +421,15 @@
 }
 
 TEST(S2ClosestCellQuery, ConservativeCellDistanceIsUsed) {
-  // Don't use absl::FlagSaver, so it works in opensource without gflags.
-  const int saved_seed = absl::GetFlag(FLAGS_s2_random_seed);
+  // Don't use shield_absl::FlagSaver, so it works in opensource without gflags.
+  const int saved_seed = shield_absl::GetFlag(FLAGS_s2_random_seed);
   // These specific test cases happen to fail if max_error() is not properly
   // taken into account when measuring distances to S2ShapeIndex cells.
   for (int seed : {32, 109, 253, 342, 948, 1535, 1884, 1887, 2133}) {
-    absl::SetFlag(&FLAGS_s2_random_seed, seed);
+    shield_absl::SetFlag(&FLAGS_s2_random_seed, seed);
     TestWithIndexFactory(PointCloudCellIndexFactory(), 5, 100, 10);
   }
-  absl::SetFlag(&FLAGS_s2_random_seed, saved_seed);
+  shield_absl::SetFlag(&FLAGS_s2_random_seed, saved_seed);
 }
 
 }  // namespace
diff -u -r '--exclude=.git' a/src/s2/s2closest_edge_query_base.h b/src/s2/s2closest_edge_query_base.h
--- a/src/s2/s2closest_edge_query_base.h	2024-07-25 16:38:02.899739497 -0400
+++ b/src/s2/s2closest_edge_query_base.h	2024-07-25 16:20:50.140040641 -0400
@@ -316,7 +316,7 @@
   // The covering needs to be stored in a std::vector so that we can use
   // S2CellUnion::GetIntersection().
   std::vector<S2CellId> index_covering_;
-  absl::InlinedVector<const S2ShapeIndexCell*, 6> index_cells_;
+  shield_absl::InlinedVector<const S2ShapeIndexCell*, 6> index_cells_;
 
   // The decision about whether to use the brute force algorithm is based on
   // counting the total number of edges in the index.  However if the index
@@ -351,7 +351,7 @@
   // when result_set_ is used so that we could use a priority queue instead.
   Result result_singleton_;
   std::vector<Result> result_vector_;
-  absl::btree_set<Result> result_set_;
+  shield_absl::btree_set<Result> result_set_;
 
   // When the result edges are stored in a btree_set (see above), usually
   // duplicates can be removed simply by inserting candidate edges in the
@@ -372,7 +372,7 @@
   // (even when Options::max_results() == 1), rather than just when we need to.
   bool avoid_duplicates_;
   using ShapeEdgeId = s2shapeutil::ShapeEdgeId;
-  absl::flat_hash_set<ShapeEdgeId> tested_edges_;
+  shield_absl::flat_hash_set<ShapeEdgeId> tested_edges_;
 
   // The algorithm maintains a priority queue of unprocessed S2CellIds, sorted
   // in increasing order of distance from the target.
@@ -401,7 +401,7 @@
     }
   };
   using CellQueue =
-      std::priority_queue<QueueEntry, absl::InlinedVector<QueueEntry, 16>>;
+      std::priority_queue<QueueEntry, shield_absl::InlinedVector<QueueEntry, 16>>;
   CellQueue queue_;
 
   // Temporaries, defined here to avoid multiple allocations / initializations.
@@ -571,7 +571,7 @@
   }
 
   if (options.include_interiors()) {
-    absl::btree_set<int32> shape_ids;
+    shield_absl::btree_set<int32> shape_ids;
     (void) target->VisitContainingShapes(
         *index_, [&shape_ids, &options](S2Shape* containing_shape,
                                         const S2Point& /*target_point*/) {
diff -u -r '--exclude=.git' a/src/s2/s2closest_edge_query_test.cc b/src/s2/s2closest_edge_query_test.cc
--- a/src/s2/s2closest_edge_query_test.cc	2024-07-25 16:38:02.899739497 -0400
+++ b/src/s2/s2closest_edge_query_test.cc	2024-07-25 16:20:50.116040541 -0400
@@ -445,13 +445,13 @@
   vector<S2Cap> index_caps;
   vector<unique_ptr<MutableS2ShapeIndex>> indexes;
   for (int i = 0; i < num_indexes; ++i) {
-    S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed) + i);
+    S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed) + i);
     index_caps.push_back(S2Cap(S2Testing::RandomPoint(), kTestCapRadius));
     indexes.push_back(make_unique<MutableS2ShapeIndex>());
     factory.AddEdges(index_caps.back(), num_edges, indexes.back().get());
   }
   for (int i = 0; i < num_queries; ++i) {
-    S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed) + i);
+    S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed) + i);
     int i_index = S2Testing::rnd.Uniform(num_indexes);
     const S2Cap& index_cap = index_caps[i_index];
 
@@ -538,15 +538,15 @@
 }
 
 TEST(S2ClosestEdgeQuery, ConservativeCellDistanceIsUsed) {
-  // Don't use absl::FlagSaver, so it works in opensource without gflags.
-  const int saved_seed = absl::GetFlag(FLAGS_s2_random_seed);
+  // Don't use shield_absl::FlagSaver, so it works in opensource without gflags.
+  const int saved_seed = shield_absl::GetFlag(FLAGS_s2_random_seed);
   // These specific test cases happen to fail if max_error() is not properly
   // taken into account when measuring distances to S2ShapeIndex cells.
   for (int seed : {42, 681, 894, 1018, 1750, 1759, 2401}) {
-    absl::SetFlag(&FLAGS_s2_random_seed, seed);
+    shield_absl::SetFlag(&FLAGS_s2_random_seed, seed);
     TestWithIndexFactory(s2testing::FractalLoopShapeIndexFactory(),
                          5, 100, 10);
   }
-  absl::SetFlag(&FLAGS_s2_random_seed, saved_seed);
+  shield_absl::SetFlag(&FLAGS_s2_random_seed, saved_seed);
 }
 
diff -u -r '--exclude=.git' a/src/s2/s2closest_point_query_base.h b/src/s2/s2closest_point_query_base.h
--- a/src/s2/s2closest_point_query_base.h	2024-07-25 16:38:02.899739497 -0400
+++ b/src/s2/s2closest_point_query_base.h	2024-07-25 16:20:50.112040525 -0400
@@ -299,7 +299,7 @@
   //    have been found.
   Result result_singleton_;
   std::vector<Result> result_vector_;
-  std::priority_queue<Result, absl::InlinedVector<Result, 16>> result_set_;
+  std::priority_queue<Result, shield_absl::InlinedVector<Result, 16>> result_set_;
 
   // The algorithm maintains a priority queue of unprocessed S2CellIds, sorted
   // in increasing order of distance from the target.
@@ -321,7 +321,7 @@
     }
   };
   using CellQueue =
-      std::priority_queue<QueueEntry, absl::InlinedVector<QueueEntry, 16>>;
+      std::priority_queue<QueueEntry, shield_absl::InlinedVector<QueueEntry, 16>>;
   CellQueue queue_;
 
   // Temporaries, defined here to avoid multiple allocations / initializations.
diff -u -r '--exclude=.git' a/src/s2/s2closest_point_query_test.cc b/src/s2/s2closest_point_query_test.cc
--- a/src/s2/s2closest_point_query_test.cc	2024-07-25 16:38:02.899739497 -0400
+++ b/src/s2/s2closest_point_query_test.cc	2024-07-25 16:20:50.152040690 -0400
@@ -222,13 +222,13 @@
   vector<S2Cap> index_caps;
   vector<unique_ptr<TestIndex>> indexes;
   for (int i = 0; i < num_indexes; ++i) {
-    S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed) + i);
+    S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed) + i);
     index_caps.push_back(S2Cap(S2Testing::RandomPoint(), kTestCapRadius));
     indexes.push_back(make_unique<TestIndex>());
     factory.AddPoints(index_caps.back(), num_points, indexes.back().get());
   }
   for (int i = 0; i < num_queries; ++i) {
-    S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed) + i);
+    S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed) + i);
     int i_index = S2Testing::rnd.Uniform(num_indexes);
     const S2Cap& index_cap = index_caps[i_index];
 
@@ -315,15 +315,15 @@
 }
 
 TEST(S2ClosestPointQueryTest, ConservativeCellDistanceIsUsed) {
-  const int saved_seed = absl::GetFlag(FLAGS_s2_random_seed);
+  const int saved_seed = shield_absl::GetFlag(FLAGS_s2_random_seed);
   // These specific test cases happen to fail if max_error() is not properly
   // taken into account when measuring distances to S2PointIndex cells.  They
   // all involve S2ShapeIndexTarget, which takes advantage of max_error() to
   // optimize its distance calculation.
   for (int seed : {16, 586, 589, 822, 1959, 2298, 3155, 3490, 3723, 4953}) {
-    absl::SetFlag(&FLAGS_s2_random_seed, seed);
+    shield_absl::SetFlag(&FLAGS_s2_random_seed, seed);
     TestWithIndexFactory(FractalPointIndexFactory(), 5, 100, 10);
   }
-  absl::SetFlag(&FLAGS_s2_random_seed, saved_seed);
+  shield_absl::SetFlag(&FLAGS_s2_random_seed, saved_seed);
 }
 
diff -u -r '--exclude=.git' a/src/s2/s2coder.h b/src/s2/s2coder.h
--- a/src/s2/s2coder.h	2024-07-25 16:38:02.899739497 -0400
+++ b/src/s2/s2coder.h	2024-07-25 16:20:50.148040674 -0400
@@ -58,7 +58,7 @@
   // the first byte after the encoded representation of T.
   virtual bool Decode(Decoder&, T&, S2Error&) const = 0;
 
-  absl::Status Decode(Decoder& decoder, T& v) const {
+  shield_absl::Status Decode(Decoder& decoder, T& v) const {
     S2Error error;
     v.Init(&decoder, error);
     return ToStatus(error);
diff -u -r '--exclude=.git' a/src/s2/s2contains_vertex_query.h b/src/s2/s2contains_vertex_query.h
--- a/src/s2/s2contains_vertex_query.h	2024-07-25 16:38:02.899739497 -0400
+++ b/src/s2/s2contains_vertex_query.h	2024-07-25 16:20:50.104040492 -0400
@@ -48,7 +48,7 @@
 
  private:
   S2Point target_;
-  absl::btree_map<S2Point, int> edge_map_;
+  shield_absl::btree_map<S2Point, int> edge_map_;
 };
 
 
diff -u -r '--exclude=.git' a/src/s2/s2crossing_edge_query_test.cc b/src/s2/s2crossing_edge_query_test.cc
--- a/src/s2/s2crossing_edge_query_test.cc	2024-07-25 16:38:02.899739497 -0400
+++ b/src/s2/s2crossing_edge_query_test.cc	2024-07-25 16:20:50.144040657 -0400
@@ -53,7 +53,7 @@
 #include "s2/s2testing.h"
 #include "s2/s2text_format.h"
 
-using absl::StrCat;
+using shield_absl::StrCat;
 using s2shapeutil::ShapeEdge;
 using s2shapeutil::ShapeEdgeId;
 using s2textformat::MakePointOrDie;
@@ -134,7 +134,7 @@
   MutableS2ShapeIndex::Options options;
   options.set_max_edges_per_cell(1);
   MutableS2ShapeIndex index(options);
-  const int shape_id = index.Add(absl::WrapUnique(shape));
+  const int shape_id = index.Add(shield_absl::WrapUnique(shape));
   EXPECT_EQ(0, shape_id);
   // To check that candidates are being filtered reasonably, we count the
   // total number of candidates that the total number of edge pairs that
@@ -174,7 +174,7 @@
         ++num_nearby_pairs;
         if (!std::binary_search(candidates.begin(), candidates.end(),
                                 ShapeEdgeId{0, i})) {
-          absl::StrAppend(&missing_candidates, " ", i);
+          shield_absl::StrAppend(&missing_candidates, " ", i);
         }
       } else {
         const double kMaxDist = S2::kMaxDiag.GetValue(S2::kMaxCellLevel);
diff -u -r '--exclude=.git' a/src/s2/s2edge_clipping.h b/src/s2/s2edge_clipping.h
--- a/src/s2/s2edge_clipping.h	2024-07-25 16:38:02.899739497 -0400
+++ b/src/s2/s2edge_clipping.h	2024-07-25 16:20:50.120040559 -0400
@@ -46,7 +46,7 @@
   int face;
   R2Point a, b;
 };
-using FaceSegmentVector = absl::InlinedVector<FaceSegment, 6>;
+using FaceSegmentVector = shield_absl::InlinedVector<FaceSegment, 6>;
 
 // Subdivides the given edge AB at every point where it crosses the boundary
 // between two S2 cube faces and returns the corresponding FaceSegments.  The
diff -u -r '--exclude=.git' a/src/s2/s2edge_clipping_test.cc b/src/s2/s2edge_clipping_test.cc
--- a/src/s2/s2edge_clipping_test.cc	2024-07-25 16:38:02.899739497 -0400
+++ b/src/s2/s2edge_clipping_test.cc	2024-07-25 16:20:50.136040625 -0400
@@ -38,8 +38,8 @@
 #include "s2/s2shape.h"
 #include "s2/s2testing.h"
 
-using absl::StrCat;
-using absl::string_view;
+using shield_absl::StrCat;
+using shield_absl::string_view;
 using std::fabs;
 using std::max;
 using std::vector;
diff -u -r '--exclude=.git' a/src/s2/s2edge_crossings.cc b/src/s2/s2edge_crossings.cc
--- a/src/s2/s2edge_crossings.cc	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2edge_crossings.cc	2024-07-25 16:20:50.144040657 -0400
@@ -34,7 +34,7 @@
 
 namespace S2 {
 
-using absl::string_view;
+using shield_absl::string_view;
 using internal::GetIntersectionExact;
 using internal::intersection_method_tally_;
 using internal::IntersectionMethod;
diff -u -r '--exclude=.git' a/src/s2/s2edge_crossings_internal.h b/src/s2/s2edge_crossings_internal.h
--- a/src/s2/s2edge_crossings_internal.h	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2edge_crossings_internal.h	2024-07-25 16:20:50.148040674 -0400
@@ -78,7 +78,7 @@
   EXACT,
   NUM_METHODS
 };
-absl::string_view GetIntersectionMethodName(IntersectionMethod method);
+shield_absl::string_view GetIntersectionMethodName(IntersectionMethod method);
 
 // The following classes are used as template arguments to S2EdgeCrosserBase in
 // order to create two versions, namely S2EdgeCrosser itself (which takes
diff -u -r '--exclude=.git' a/src/s2/s2edge_crossings_test.cc b/src/s2/s2edge_crossings_test.cc
--- a/src/s2/s2edge_crossings_test.cc	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2edge_crossings_test.cc	2024-07-25 16:20:50.112040525 -0400
@@ -42,7 +42,7 @@
 #include "s2/s2testing.h"
 #include "s2/util/math/exactfloat/exactfloat.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using S2::internal::ExactCrossProd;
 using S2::internal::GetStableCrossProd;
 using S2::internal::SymbolicCrossProd;
@@ -82,10 +82,10 @@
   string result;
   int total = 0;
   for (int i = 0; i < NUM_PRECISIONS; ++i) {
-    absl::StrAppendFormat(&result, "%s=%6d, ", kPrecisionNames[i], counts_[i]);
+    shield_absl::StrAppendFormat(&result, "%s=%6d, ", kPrecisionNames[i], counts_[i]);
     total += counts_[i];
   }
-  absl::StrAppendFormat(&result, "total=%6d", total);
+  shield_absl::StrAppendFormat(&result, "total=%6d", total);
   return result;
 }
 
@@ -385,7 +385,7 @@
     for (int i = 0; i < kNumMethods; ++i) {
       if (tally_[i] == 0) continue;
       // Use `StrFormat` to handle `string_view`.
-      puts(absl::StrFormat("%10s %9d %5.1f%% %9d %5.1f%%  %5.1f%%",
+      puts(shield_absl::StrFormat("%10s %9d %5.1f%% %9d %5.1f%%  %5.1f%%",
                            S2::internal::GetIntersectionMethodName(
                                static_cast<IntersectionMethod>(i)),
                            tally_[i], 100.0 * tally_[i] / total, totals[i],
diff -u -r '--exclude=.git' a/src/s2/s2edge_distances_test.cc b/src/s2/s2edge_distances_test.cc
--- a/src/s2/s2edge_distances_test.cc	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2edge_distances_test.cc	2024-07-25 16:20:50.136040625 -0400
@@ -41,7 +41,7 @@
 #include "s2/s2testing.h"
 #include "s2/s2text_format.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using std::string;
 using std::unique_ptr;
 
diff -u -r '--exclude=.git' a/src/s2/s2edge_tessellator_test.cc b/src/s2/s2edge_tessellator_test.cc
--- a/src/s2/s2edge_tessellator_test.cc	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2edge_tessellator_test.cc	2024-07-25 16:20:50.140040641 -0400
@@ -39,7 +39,7 @@
 #include "s2/s2testing.h"
 #include "s2/s2text_format.h"
 
-using absl::StrCat;
+using shield_absl::StrCat;
 using s2textformat::ParsePointsOrDie;
 using std::cout;
 using std::endl;
diff -u -r '--exclude=.git' a/src/s2/s2edge_vector_shape_test.cc b/src/s2/s2edge_vector_shape_test.cc
--- a/src/s2/s2edge_vector_shape_test.cc	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2edge_vector_shape_test.cc	2024-07-25 16:20:50.108040509 -0400
@@ -45,7 +45,7 @@
   // to be moved.
   S2EdgeVectorShape correct;
   S2EdgeVectorShape to_move;
-  S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed));
+  S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed));
   const int kNumEdges = 100;
   for (int i = 0; i < kNumEdges; ++i) {
     const S2Point start_point = S2Testing::RandomPoint();
@@ -68,7 +68,7 @@
 
 TEST(S2EdgeVectorShape, EdgeAccess) {
   S2EdgeVectorShape shape;
-  S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed));
+  S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed));
   const int kNumEdges = 100;
   vector<std::pair<S2Point, S2Point>> edges;
   for (int i = 0; i < kNumEdges; ++i) {
diff -u -r '--exclude=.git' a/src/s2/s2error.cc b/src/s2/s2error.cc
--- a/src/s2/s2error.cc	2024-07-25 16:37:56.191717334 -0400
+++ b/src/s2/s2error.cc	2024-07-25 16:20:50.152040690 -0400
@@ -22,9 +22,9 @@
 #include "absl/strings/str_cat.h"
 #include "absl/strings/string_view.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 
-S2Error ToS2Error(const absl::Status& status) {
+S2Error ToS2Error(const shield_absl::Status& status) {
   S2Error error;
 
   if (status.ok()) {
@@ -34,35 +34,35 @@
   const string_view message = status.message();
 
   switch (status.code()) {
-    case absl::StatusCode::kCancelled:
+    case shield_absl::StatusCode::kCancelled:
       error.Init(S2Error::CANCELLED, "%s", message);
       break;
 
-    case absl::StatusCode::kInvalidArgument:
+    case shield_absl::StatusCode::kInvalidArgument:
       error.Init(S2Error::INVALID_ARGUMENT, "%s", message);
       break;
 
-    case absl::StatusCode::kDataLoss:
+    case shield_absl::StatusCode::kDataLoss:
       error.Init(S2Error::DATA_LOSS, "%s", message);
       break;
 
-    case absl::StatusCode::kResourceExhausted:
+    case shield_absl::StatusCode::kResourceExhausted:
       error.Init(S2Error::RESOURCE_EXHAUSTED, "%s", message);
       break;
 
-    case absl::StatusCode::kFailedPrecondition:
+    case shield_absl::StatusCode::kFailedPrecondition:
       error.Init(S2Error::FAILED_PRECONDITION, "%s", message);
       break;
 
-    case absl::StatusCode::kOutOfRange:
+    case shield_absl::StatusCode::kOutOfRange:
       error.Init(S2Error::OUT_OF_RANGE, "%s", message);
       break;
 
-    case absl::StatusCode::kUnimplemented:
+    case shield_absl::StatusCode::kUnimplemented:
       error.Init(S2Error::UNIMPLEMENTED, "%s", message);
       break;
 
-    case absl::StatusCode::kInternal:
+    case shield_absl::StatusCode::kInternal:
       error.Init(S2Error::INTERNAL, "%s", message);
       break;
 
@@ -73,33 +73,33 @@
   return error;
 }
 
-absl::Status ToStatus(const S2Error& error) {
+shield_absl::Status ToStatus(const S2Error& error) {
   // Map user-defined error codes to `UnknownError`.  We do this outside of
   // the `switch` so we can handle all cases explicitly without `default`.
   if (S2Error::USER_DEFINED_START <= error.code() &&
       error.code() <= S2Error::USER_DEFINED_END) {
-    return absl::UnknownError(
-        absl::StrCat(error.text(), " (", error.code(), ")"));
+    return shield_absl::UnknownError(
+        shield_absl::StrCat(error.text(), " (", error.code(), ")"));
   }
 
   switch (static_cast<S2Error::Code>(error.code())) {
     case S2Error::OK:
-      return absl::OkStatus();
+      return shield_absl::OkStatus();
 
     case S2Error::CANCELLED:
-      return absl::CancelledError(error.text());
+      return shield_absl::CancelledError(error.text());
 
     case S2Error::INVALID_ARGUMENT:
-      return absl::InvalidArgumentError(error.text());
+      return shield_absl::InvalidArgumentError(error.text());
 
     case S2Error::DATA_LOSS:
-      return absl::DataLossError(error.text());
+      return shield_absl::DataLossError(error.text());
 
     case S2Error::RESOURCE_EXHAUSTED:
-      return absl::ResourceExhaustedError(error.text());
+      return shield_absl::ResourceExhaustedError(error.text());
 
     case S2Error::FAILED_PRECONDITION:
-      return absl::FailedPreconditionError(error.text());
+      return shield_absl::FailedPreconditionError(error.text());
 
     // Custom error space are deprecated and not open-sourced, so we map
     // these to invalid argument errors.
@@ -126,24 +126,24 @@
     case S2Error::BUILDER_EDGES_DO_NOT_FORM_LOOPS:
     case S2Error::BUILDER_EDGES_DO_NOT_FORM_POLYLINE:
     case S2Error::BUILDER_IS_FULL_PREDICATE_NOT_SPECIFIED:
-      return absl::InvalidArgumentError(
-          absl::StrCat(error.text(), " (", error.code(), ")"));
+      return shield_absl::InvalidArgumentError(
+          shield_absl::StrCat(error.text(), " (", error.code(), ")"));
 
     case S2Error::OUT_OF_RANGE:
-      return absl::OutOfRangeError(error.text());
+      return shield_absl::OutOfRangeError(error.text());
 
     case S2Error::UNIMPLEMENTED:
-      return absl::UnimplementedError(error.text());
+      return shield_absl::UnimplementedError(error.text());
 
     case S2Error::INTERNAL:
-      return absl::InternalError(error.text());
+      return shield_absl::InternalError(error.text());
 
     case S2Error::UNKNOWN:
     case S2Error::USER_DEFINED_START:
     case S2Error::USER_DEFINED_END:
       // USER_DEFINED_START/END can't happen here; they're handled above.
-      return absl::UnknownError(
-          absl::StrCat(error.text(), " (", error.code(), ")"));
+      return shield_absl::UnknownError(
+          shield_absl::StrCat(error.text(), " (", error.code(), ")"));
   }
   ABSL_UNREACHABLE();
 }
diff -u -r '--exclude=.git' a/src/s2/s2error.h b/src/s2/s2error.h
--- a/src/s2/s2error.h	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2error.h	2024-07-25 16:20:50.116040541 -0400
@@ -127,7 +127,7 @@
 
   // Convenience constructor that calls Init().
   template <typename... Args>
-  S2Error(Code code, const absl::FormatSpec<Args...>& format,
+  S2Error(Code code, const shield_absl::FormatSpec<Args...>& format,
           const Args&... args) {
     Init(code, format, args...);
   }
@@ -137,10 +137,10 @@
   //
   //   error->Init(error->code(), "Loop %d: %s", j, error->text().c_str());
   template <typename... Args>
-  void Init(Code code, const absl::FormatSpec<Args...>& format,
+  void Init(Code code, const shield_absl::FormatSpec<Args...>& format,
             const Args&... args) {
     code_ = code;
-    text_ = absl::StrFormat(format, args...);
+    text_ = shield_absl::StrFormat(format, args...);
   }
 
   bool ok() const { return code_ == OK; }
@@ -155,30 +155,30 @@
   std::string text_;
 };
 
-// Converts an absl::Status object into an S2Error.
+// Converts an shield_absl::Status object into an S2Error.
 //
-// absl::Status codes with no exact equivalent in S2Error::Code are converted to
+// shield_absl::Status codes with no exact equivalent in S2Error::Code are converted to
 // S2Error::UNKNOWN.
-ABSL_MUST_USE_RESULT S2Error ToS2Error(const absl::Status& status);
+ABSL_MUST_USE_RESULT S2Error ToS2Error(const shield_absl::Status& status);
 
-// Converts an S2Error into an absl::Status.
+// Converts an S2Error into an shield_absl::Status.
 //
 // Custom error space are deprecated,
-// so S2Error codes with no exact equivalent in absl::StatusCode are converted
+// so S2Error codes with no exact equivalent in shield_absl::StatusCode are converted
 // as follows:
 //
 //   - errors related to S2Loop, S2Polygon, and S2Builder, as well as those that
 //     apply to more than one type of geometry are mapped to
-//     absl::StatusCode::kInvalidArgument.
+//     shield_absl::StatusCode::kInvalidArgument.
 //
 //   - client-defined errors (those in the [USER_DEFINED_START,USER_DEFINED_END]
 //     range), UNKNOWN errors, and all other errors not covered above are mapped
-//     to absl::StatusCode::kUnknown
+//     to shield_absl::StatusCode::kUnknown
 //
 // The above mapping significantly reduces the set of expressible errors.
 // Clients reacting to specific S2 errors (e.g. polygon validation errors)
 // should continue to use S2Error for its richer error API.
-absl::Status ToStatus(const S2Error& error);
+shield_absl::Status ToStatus(const S2Error& error);
 
 
 //////////////////   Implementation details follow   ////////////////////
diff -u -r '--exclude=.git' a/src/s2/s2error_test.cc b/src/s2/s2error_test.cc
--- a/src/s2/s2error_test.cc	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2error_test.cc	2024-07-25 16:20:50.148040674 -0400
@@ -42,56 +42,56 @@
 }
 
 TEST(S2Error, ToS2Error) {
-  const S2Error ok = ToS2Error(absl::Status());
+  const S2Error ok = ToS2Error(shield_absl::Status());
   EXPECT_EQ(ok.code(), S2Error::OK);
 
-  const S2Error cancelled = ToS2Error(absl::CancelledError("cancelled"));
+  const S2Error cancelled = ToS2Error(shield_absl::CancelledError("cancelled"));
   EXPECT_EQ(cancelled.code(), S2Error::CANCELLED);
   EXPECT_EQ(cancelled.text(), "cancelled");
 
   const S2Error invalid_argument =
-      ToS2Error(absl::InvalidArgumentError("invalid_argument"));
+      ToS2Error(shield_absl::InvalidArgumentError("invalid_argument"));
   EXPECT_EQ(invalid_argument.code(), S2Error::INVALID_ARGUMENT);
   EXPECT_EQ(invalid_argument.text(), "invalid_argument");
 
-  const S2Error data_loss = ToS2Error(absl::DataLossError("data_loss"));
+  const S2Error data_loss = ToS2Error(shield_absl::DataLossError("data_loss"));
   EXPECT_EQ(data_loss.code(), S2Error::DATA_LOSS);
   EXPECT_EQ(data_loss.text(), "data_loss");
 
   const S2Error resource_exhausted =
-      ToS2Error(absl::ResourceExhaustedError("resource_exhausted"));
+      ToS2Error(shield_absl::ResourceExhaustedError("resource_exhausted"));
   EXPECT_EQ(resource_exhausted.code(), S2Error::RESOURCE_EXHAUSTED);
   EXPECT_EQ(resource_exhausted.text(), "resource_exhausted");
 
   const S2Error failed_precondition =
-      ToS2Error(absl::FailedPreconditionError("failed_precondition"));
+      ToS2Error(shield_absl::FailedPreconditionError("failed_precondition"));
   EXPECT_EQ(failed_precondition.code(), S2Error::FAILED_PRECONDITION);
   EXPECT_EQ(failed_precondition.text(), "failed_precondition");
 
-  const S2Error out_of_range = ToS2Error(absl::OutOfRangeError("out_of_range"));
+  const S2Error out_of_range = ToS2Error(shield_absl::OutOfRangeError("out_of_range"));
   EXPECT_EQ(out_of_range.code(), S2Error::OUT_OF_RANGE);
   EXPECT_EQ(out_of_range.text(), "out_of_range");
 
   const S2Error unimplemented =
-      ToS2Error(absl::UnimplementedError("unimplemented"));
+      ToS2Error(shield_absl::UnimplementedError("unimplemented"));
   EXPECT_EQ(unimplemented.code(), S2Error::UNIMPLEMENTED);
   EXPECT_EQ(unimplemented.text(), "unimplemented");
 
-  const S2Error internal = ToS2Error(absl::InternalError("internal"));
+  const S2Error internal = ToS2Error(shield_absl::InternalError("internal"));
   EXPECT_EQ(internal.code(), S2Error::INTERNAL);
   EXPECT_EQ(internal.text(), "internal");
 
-  const S2Error unknown = ToS2Error(absl::UnknownError("unknown"));
+  const S2Error unknown = ToS2Error(shield_absl::UnknownError("unknown"));
   EXPECT_EQ(unknown.code(), S2Error::UNKNOWN);
   EXPECT_THAT(unknown.text(), "unknown");
 
-  // All absl::Status codes that don't have an exact mapping to S2Error codes
+  // All shield_absl::Status codes that don't have an exact mapping to S2Error codes
   // should be mapped to S2Error::UNKNOWN.
-  for (const absl::Status& status :
-       {absl::AbortedError("other"), absl::AlreadyExistsError("other"),
-        absl::DeadlineExceededError("other"), absl::NotFoundError("other"),
-        absl::PermissionDeniedError("other"),
-        absl::UnauthenticatedError("other"), absl::UnavailableError("other")}) {
+  for (const shield_absl::Status& status :
+       {shield_absl::AbortedError("other"), shield_absl::AlreadyExistsError("other"),
+        shield_absl::DeadlineExceededError("other"), shield_absl::NotFoundError("other"),
+        shield_absl::PermissionDeniedError("other"),
+        shield_absl::UnauthenticatedError("other"), shield_absl::UnavailableError("other")}) {
     const S2Error other = ToS2Error(status);
     EXPECT_EQ(other.code(), S2Error::UNKNOWN);
     EXPECT_EQ(other.text(), "other");
@@ -101,32 +101,32 @@
 TEST(S2Error, ToStatus) {
   S2Error error;
 
-  const absl::Status ok = ToStatus(error);
-  EXPECT_EQ(ok.code(), absl::StatusCode::kOk);
+  const shield_absl::Status ok = ToStatus(error);
+  EXPECT_EQ(ok.code(), shield_absl::StatusCode::kOk);
 
   error.Init(S2Error::CANCELLED, "cancelled");
-  const absl::Status cancelled = ToStatus(error);
-  EXPECT_EQ(cancelled.code(), absl::StatusCode::kCancelled);
+  const shield_absl::Status cancelled = ToStatus(error);
+  EXPECT_EQ(cancelled.code(), shield_absl::StatusCode::kCancelled);
   EXPECT_EQ(cancelled.message(), "cancelled");
 
   error.Init(S2Error::INVALID_ARGUMENT, "invalid_argument");
-  const absl::Status invalid_argument = ToStatus(error);
-  EXPECT_EQ(invalid_argument.code(), absl::StatusCode::kInvalidArgument);
+  const shield_absl::Status invalid_argument = ToStatus(error);
+  EXPECT_EQ(invalid_argument.code(), shield_absl::StatusCode::kInvalidArgument);
   EXPECT_EQ(invalid_argument.message(), "invalid_argument");
 
   error.Init(S2Error::DATA_LOSS, "data_loss");
-  const absl::Status data_loss = ToStatus(error);
-  EXPECT_EQ(data_loss.code(), absl::StatusCode::kDataLoss);
+  const shield_absl::Status data_loss = ToStatus(error);
+  EXPECT_EQ(data_loss.code(), shield_absl::StatusCode::kDataLoss);
   EXPECT_EQ(data_loss.message(), "data_loss");
 
   error.Init(S2Error::RESOURCE_EXHAUSTED, "resource_exhausted");
-  const absl::Status resource_exhausted = ToStatus(error);
-  EXPECT_EQ(resource_exhausted.code(), absl::StatusCode::kResourceExhausted);
+  const shield_absl::Status resource_exhausted = ToStatus(error);
+  EXPECT_EQ(resource_exhausted.code(), shield_absl::StatusCode::kResourceExhausted);
   EXPECT_EQ(resource_exhausted.message(), "resource_exhausted");
 
   error.Init(S2Error::FAILED_PRECONDITION, "failed_precondition");
-  const absl::Status failed_precondition = ToStatus(error);
-  EXPECT_EQ(failed_precondition.code(), absl::StatusCode::kFailedPrecondition);
+  const shield_absl::Status failed_precondition = ToStatus(error);
+  EXPECT_EQ(failed_precondition.code(), shield_absl::StatusCode::kFailedPrecondition);
   EXPECT_EQ(failed_precondition.message(), "failed_precondition");
 
   for (const S2Error::Code code :
@@ -149,40 +149,40 @@
         S2Error::BUILDER_EDGES_DO_NOT_FORM_POLYLINE,
         S2Error::BUILDER_IS_FULL_PREDICATE_NOT_SPECIFIED}) {
     error.Init(code, "other_invalid_argument");
-    const absl::Status other = ToStatus(error);
-    EXPECT_EQ(other.code(), absl::StatusCode::kInvalidArgument);
+    const shield_absl::Status other = ToStatus(error);
+    EXPECT_EQ(other.code(), shield_absl::StatusCode::kInvalidArgument);
     EXPECT_THAT(other.message(),
                 testing::HasSubstr("other_invalid_argument"));
   }
 
   error.Init(S2Error::OUT_OF_RANGE, "out_of_range");
-  const absl::Status out_of_range = ToStatus(error);
-  EXPECT_EQ(out_of_range.code(), absl::StatusCode::kOutOfRange);
+  const shield_absl::Status out_of_range = ToStatus(error);
+  EXPECT_EQ(out_of_range.code(), shield_absl::StatusCode::kOutOfRange);
   EXPECT_EQ(out_of_range.message(), "out_of_range");
 
   error.Init(S2Error::UNIMPLEMENTED, "unimplemented");
-  const absl::Status unimplemented = ToStatus(error);
-  EXPECT_EQ(unimplemented.code(), absl::StatusCode::kUnimplemented);
+  const shield_absl::Status unimplemented = ToStatus(error);
+  EXPECT_EQ(unimplemented.code(), shield_absl::StatusCode::kUnimplemented);
   EXPECT_EQ(unimplemented.message(), "unimplemented");
 
   error.Init(S2Error::INTERNAL, "internal");
-  const absl::Status internal = ToStatus(error);
-  EXPECT_EQ(internal.code(), absl::StatusCode::kInternal);
+  const shield_absl::Status internal = ToStatus(error);
+  EXPECT_EQ(internal.code(), shield_absl::StatusCode::kInternal);
   EXPECT_EQ(internal.message(), "internal");
 
   error.Init(S2Error::UNKNOWN, "unknown");
-  const absl::Status unknown = ToStatus(error);
-  EXPECT_EQ(unknown.code(), absl::StatusCode::kUnknown);
+  const shield_absl::Status unknown = ToStatus(error);
+  EXPECT_EQ(unknown.code(), shield_absl::StatusCode::kUnknown);
   EXPECT_THAT(unknown.message(), testing::HasSubstr("unknown"));
 
-  // All S2Error codes that don't have an exact mapping to absl::Status codes
-  // should be mapped to absl::StatusCode::kUnknown.
+  // All S2Error codes that don't have an exact mapping to shield_absl::Status codes
+  // should be mapped to shield_absl::StatusCode::kUnknown.
   for (const S2Error::Code code :
        {S2Error::USER_DEFINED_START,
         S2Error::USER_DEFINED_END}) {
     error.Init(code, "other");
-    const absl::Status other = ToStatus(error);
-    EXPECT_EQ(other.code(), absl::StatusCode::kUnknown);
+    const shield_absl::Status other = ToStatus(error);
+    EXPECT_EQ(other.code(), shield_absl::StatusCode::kUnknown);
     EXPECT_THAT(other.message(), testing::HasSubstr("other"));
   }
 }
diff -u -r '--exclude=.git' a/src/s2/s2furthest_edge_query_test.cc b/src/s2/s2furthest_edge_query_test.cc
--- a/src/s2/s2furthest_edge_query_test.cc	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2furthest_edge_query_test.cc	2024-07-25 16:20:50.148040674 -0400
@@ -437,14 +437,14 @@
   vector<S2Cap> index_caps;
   vector<unique_ptr<MutableS2ShapeIndex>> indexes;
   for (int i = 0; i < num_indexes; ++i) {
-    S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed) + i);
+    S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed) + i);
     index_caps.push_back(S2Cap(S2Testing::RandomPoint(), kTestCapRadius));
     indexes.emplace_back(new MutableS2ShapeIndex);
     factory.AddEdges(index_caps.back(), num_edges, indexes.back().get());
   }
 
   for (int i = 0; i < num_queries; ++i) {
-    S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed) + i);
+    S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed) + i);
     int i_index = S2Testing::rnd.Uniform(num_indexes);
     const S2Cap& index_cap = index_caps[i_index];
 
diff -u -r '--exclude=.git' a/src/s2/s2hausdorff_distance_query.cc b/src/s2/s2hausdorff_distance_query.cc
--- a/src/s2/s2hausdorff_distance_query.cc	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2hausdorff_distance_query.cc	2024-07-25 16:20:50.132040607 -0400
@@ -70,30 +70,30 @@
 
 S1ChordAngle S2HausdorffDistanceQuery::GetDistance(
     const S2ShapeIndex* target, const S2ShapeIndex* source) const {
-  absl::optional<Result> result = GetResult(target, source);
+  shield_absl::optional<Result> result = GetResult(target, source);
   return result.has_value() ? result->distance() : S1ChordAngle::Infinity();
 }
 
-absl::optional<Result> S2HausdorffDistanceQuery::GetResult(
+shield_absl::optional<Result> S2HausdorffDistanceQuery::GetResult(
     const S2ShapeIndex* target, const S2ShapeIndex* source) const {
-  absl::optional<DirectedResult> target_to_source =
+  shield_absl::optional<DirectedResult> target_to_source =
       GetDirectedResult(target, source);
   if (target_to_source) {
     return Result(*target_to_source, *GetDirectedResult(source, target));
   } else {
-    return absl::nullopt;
+    return shield_absl::nullopt;
   }
 }
 
 S1ChordAngle S2HausdorffDistanceQuery::GetDirectedDistance(
     const S2ShapeIndex* target, const S2ShapeIndex* source) const {
-  absl::optional<DirectedResult> directed_result =
+  shield_absl::optional<DirectedResult> directed_result =
       GetDirectedResult(target, source);
   return directed_result.has_value() ? directed_result->distance()
                                      : S1ChordAngle::Infinity();
 }
 
-absl::optional<DirectedResult> S2HausdorffDistanceQuery::GetDirectedResult(
+shield_absl::optional<DirectedResult> S2HausdorffDistanceQuery::GetDirectedResult(
     const S2ShapeIndex* target, const S2ShapeIndex* source) const {
   S2ClosestEdgeQuery closest_edge_query(source);
   closest_edge_query.mutable_options()->set_max_results(1);
@@ -117,7 +117,7 @@
   }
 
   if (max_distance.is_negative()) {
-    return absl::nullopt;
+    return shield_absl::nullopt;
   } else {
     return DirectedResult(max_distance, target_point);
   }
diff -u -r '--exclude=.git' a/src/s2/s2hausdorff_distance_query.h b/src/s2/s2hausdorff_distance_query.h
--- a/src/s2/s2hausdorff_distance_query.h	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2hausdorff_distance_query.h	2024-07-25 16:20:50.112040525 -0400
@@ -68,14 +68,14 @@
 //                                S1ChordAngle& undirected_distance) {
 //   S2HausdorffDistanceQuery query(S2HausdorffDistanceQuery::Options());
 //
-//   absl::optional<DirectedResult> directed_result =
+//   shield_absl::optional<DirectedResult> directed_result =
 //                  query.GetDirectedHausdorffDistance(A, B);
 //   if (!directed_result) {
 //     return false;
 //   }
 //   directed_distance = directed_result->distance();
 //
-//   absl::optional<Result> undirected_result =
+//   shield_absl::optional<Result> undirected_result =
 //                  query.GetHausdorffDistance(A, B);
 //   undirected_distance = undirected_result->distance();
 //
@@ -175,7 +175,7 @@
   // Note that directed Hausdorff distance from geometry A (as target) to
   // geometry B (as source) is not (in general case) equal to that from B (as
   // target) to A (as source).
-  absl::optional<DirectedResult> GetDirectedResult(
+  shield_absl::optional<DirectedResult> GetDirectedResult(
       const S2ShapeIndex* target, const S2ShapeIndex* source) const;
 
   // Same as the above method, only returns the actual distance, or
@@ -190,7 +190,7 @@
   // Note that the result of this query is symmetrical with respect to target
   // vs. source, i.e. if target and source indices are swapped, the
   // resulting Hausdorff distance remains unchanged.
-  absl::optional<Result> GetResult(const S2ShapeIndex* target,
+  shield_absl::optional<Result> GetResult(const S2ShapeIndex* target,
                                    const S2ShapeIndex* source) const;
 
   // Same as the above method, but only returns the maximum of forward and
diff -u -r '--exclude=.git' a/src/s2/s2hausdorff_distance_query_test.cc b/src/s2/s2hausdorff_distance_query_test.cc
--- a/src/s2/s2hausdorff_distance_query_test.cc	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2hausdorff_distance_query_test.cc	2024-07-25 16:20:50.108040509 -0400
@@ -109,23 +109,23 @@
   Options options;
   S2HausdorffDistanceQuery query(options);
 
-  absl::optional<DirectedResult> directed_empty_to_a =
+  shield_absl::optional<DirectedResult> directed_empty_to_a =
       query.GetDirectedResult(&empty_index, &a);
-  absl::optional<DirectedResult> directed_a_to_empty =
+  shield_absl::optional<DirectedResult> directed_a_to_empty =
       query.GetDirectedResult(&a, &empty_index);
   S1ChordAngle directed_a_to_empty_distance =
       query.GetDirectedDistance(&a, &empty_index);
 
-  absl::optional<DirectedResult> directed_a_to_b =
+  shield_absl::optional<DirectedResult> directed_a_to_b =
       query.GetDirectedResult(&a, &b);
-  absl::optional<DirectedResult> directed_b_to_a =
+  shield_absl::optional<DirectedResult> directed_b_to_a =
       query.GetDirectedResult(&b, &a);
   S1ChordAngle directed_a_to_b_distance = query.GetDirectedDistance(&a, &b);
 
-  absl::optional<Result> a_to_b = query.GetResult(&a, &b);
-  absl::optional<Result> b_to_a = query.GetResult(&b, &a);
+  shield_absl::optional<Result> a_to_b = query.GetResult(&a, &b);
+  shield_absl::optional<Result> b_to_a = query.GetResult(&b, &a);
   S1ChordAngle b_to_a_distance = query.GetDistance(&b, &a);
-  absl::optional<Result> bb = query.GetResult(&b, &b);
+  shield_absl::optional<Result> bb = query.GetResult(&b, &b);
 
   EXPECT_FALSE(directed_empty_to_a);
   EXPECT_FALSE(directed_a_to_empty);
@@ -182,9 +182,9 @@
   // directed Hausdorff distances.
   const S1ChordAngle expected_a_b = std::max(expected_a_to_b, expected_b_to_a);
 
-  absl::optional<DirectedResult> directed_a_to_b =
+  shield_absl::optional<DirectedResult> directed_a_to_b =
       query.GetDirectedResult(&a, &b);
-  absl::optional<DirectedResult> directed_b_to_a =
+  shield_absl::optional<DirectedResult> directed_b_to_a =
       query.GetDirectedResult(&b, &a);
   S1ChordAngle undirected_a_b = query.GetDistance(&a, &b);
 
@@ -218,12 +218,12 @@
   Options options;
   options.set_include_interiors(false);
   S2HausdorffDistanceQuery query_1(options);
-  absl::optional<DirectedResult> a_to_b_1 = query_1.GetDirectedResult(&a, &b);
+  shield_absl::optional<DirectedResult> a_to_b_1 = query_1.GetDirectedResult(&a, &b);
 
   // The second query has include_interiors set to true.
   options.set_include_interiors(true);
   S2HausdorffDistanceQuery query_2(options);
-  absl::optional<DirectedResult> a_to_b_2 = query_2.GetDirectedResult(&a, &b);
+  shield_absl::optional<DirectedResult> a_to_b_2 = query_2.GetDirectedResult(&a, &b);
 
   // Error tolerance to account for the difference between the northern edge of
   // the quadrangle, which is a geodesic line, and the parallel lat=3 connecting
diff -u -r '--exclude=.git' a/src/s2/s2latlng.cc b/src/s2/s2latlng.cc
--- a/src/s2/s2latlng.cc	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2latlng.cc	2024-07-25 16:20:50.112040525 -0400
@@ -104,7 +104,7 @@
 
 string S2LatLng::ToStringInDegrees() const {
   S2LatLng pt = Normalized();
-  return absl::StrFormat("%f,%f", pt.lat().degrees(), pt.lng().degrees());
+  return shield_absl::StrFormat("%f,%f", pt.lat().degrees(), pt.lng().degrees());
 }
 
 std::ostream& operator<<(std::ostream& os, const S2LatLng& ll) {
diff -u -r '--exclude=.git' a/src/s2/s2latlng.h b/src/s2/s2latlng.h
--- a/src/s2/s2latlng.h	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2latlng.h	2024-07-25 16:20:50.136040625 -0400
@@ -174,7 +174,7 @@
 
 // Hasher for S2LatLng.
 // Does *not* need to be specified explicitly; this will be used by default for
-// absl::flat_hash_map/set.
+// shield_absl::flat_hash_map/set.
 template <typename H>
 H AbslHashValue(H h, const S2LatLng& lat_lng) {
   return H::combine(std::move(h), lat_lng.coords().x(), lat_lng.coords().y());
@@ -182,9 +182,9 @@
 
 // Legacy hash functor for S2LatLng. This only exists for backwards
 // compatibility with old hash types like std::unordered_map that don't use
-// absl::Hash natively.
+// shield_absl::Hash natively.
 #ifndef SWIG
-using S2LatLngHash = absl::Hash<S2LatLng>;
+using S2LatLngHash = shield_absl::Hash<S2LatLng>;
 #endif
 
 //////////////////   Implementation details follow   ////////////////////
diff -u -r '--exclude=.git' a/src/s2/s2latlng_rect_bounder_test.cc b/src/s2/s2latlng_rect_bounder_test.cc
--- a/src/s2/s2latlng_rect_bounder_test.cc	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2latlng_rect_bounder_test.cc	2024-07-25 16:20:50.140040641 -0400
@@ -40,7 +40,7 @@
 #include "s2/s2predicates.h"
 #include "s2/s2testing.h"
 
-using absl::StrCat;
+using shield_absl::StrCat;
 using std::vector;
 
 S2LatLngRect GetEdgeBound(const S2Point& a, const S2Point& b) {
diff -u -r '--exclude=.git' a/src/s2/s2latlng_rect.cc b/src/s2/s2latlng_rect.cc
--- a/src/s2/s2latlng_rect.cc	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2latlng_rect.cc	2024-07-25 16:20:50.108040509 -0400
@@ -372,7 +372,7 @@
   lng_ = S1Interval(lng_lo, lng_hi);
 
   if (!is_valid()) {
-    S2_DLOG_IF(ERROR, absl::GetFlag(FLAGS_s2debug))
+    S2_DLOG_IF(ERROR, shield_absl::GetFlag(FLAGS_s2debug))
         << "Invalid result in S2LatLngRect::Decode: " << *this;
     return false;
   }
diff -u -r '--exclude=.git' a/src/s2/s2latlng_rect_test.cc b/src/s2/s2latlng_rect_test.cc
--- a/src/s2/s2latlng_rect_test.cc	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2latlng_rect_test.cc	2024-07-25 16:20:50.136040625 -0400
@@ -43,7 +43,7 @@
 #include "s2/s2testing.h"
 #include "s2/s2text_format.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using s2textformat::MakePointOrDie;
 using std::fabs;
 using std::min;
diff -u -r '--exclude=.git' a/src/s2/s2latlng_test.cc b/src/s2/s2latlng_test.cc
--- a/src/s2/s2latlng_test.cc	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2latlng_test.cc	2024-07-25 16:20:50.148040674 -0400
@@ -35,7 +35,7 @@
 #include "s2/s2pointutil.h"
 #include "s2/s2testing.h"
 
-using absl::StrCat;
+using shield_absl::StrCat;
 using std::fabs;
 using std::signbit;
 using std::string;
@@ -166,7 +166,7 @@
 }
 
 TEST(S2LatLng, TestHashCode) {
-  absl::flat_hash_map<S2LatLng, int, S2LatLngHash> map;
+  shield_absl::flat_hash_map<S2LatLng, int, S2LatLngHash> map;
   map[S2LatLng::FromDegrees(0, 10)] = 1;
   map[S2LatLng::FromDegrees(2, 12)] = 2;
   map[S2LatLng::FromDegrees(5, 15)] = 3;
diff -u -r '--exclude=.git' a/src/s2/s2lax_loop_shape.cc b/src/s2/s2lax_loop_shape.cc
--- a/src/s2/s2lax_loop_shape.cc	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2lax_loop_shape.cc	2024-07-25 16:20:50.152040690 -0400
@@ -29,18 +29,18 @@
 #include "s2/s2shape.h"
 #include "s2/s2shapeutil_get_reference_point.h"
 
-using absl::Span;
+using shield_absl::Span;
 using std::make_unique;
 using ReferencePoint = S2Shape::ReferencePoint;
 
 S2LaxLoopShape::S2LaxLoopShape(S2LaxLoopShape&& other)
     : S2Shape(std::move(other)),
-      num_vertices_(absl::exchange(other.num_vertices_, 0)),
+      num_vertices_(shield_absl::exchange(other.num_vertices_, 0)),
       vertices_(std::move(other.vertices_)) {}
 
 S2LaxLoopShape& S2LaxLoopShape::operator=(S2LaxLoopShape&& other) {
   S2Shape::operator=(static_cast<S2Shape&&>(other));
-  num_vertices_ = absl::exchange(other.num_vertices_, 0);
+  num_vertices_ = shield_absl::exchange(other.num_vertices_, 0);
   vertices_ = std::move(other.vertices_);
   return *this;
 }
@@ -92,14 +92,14 @@
 
 S2VertexIdLaxLoopShape::S2VertexIdLaxLoopShape(S2VertexIdLaxLoopShape&& other)
     : S2Shape(std::move(other)),
-      num_vertices_(absl::exchange(other.num_vertices_, 0)),
+      num_vertices_(shield_absl::exchange(other.num_vertices_, 0)),
       vertex_ids_(std::move(other.vertex_ids_)),
       vertex_array_(std::move(other.vertex_array_)) {}
 
 S2VertexIdLaxLoopShape& S2VertexIdLaxLoopShape::operator=(
     S2VertexIdLaxLoopShape&& other) {
   S2Shape::operator=(static_cast<S2Shape&&>(other));
-  num_vertices_ = absl::exchange(other.num_vertices_, 0);
+  num_vertices_ = shield_absl::exchange(other.num_vertices_, 0);
   vertex_ids_ = std::move(other.vertex_ids_);
   vertex_array_ = std::move(other.vertex_array_);
   return *this;
diff -u -r '--exclude=.git' a/src/s2/s2lax_loop_shape.h b/src/s2/s2lax_loop_shape.h
--- a/src/s2/s2lax_loop_shape.h	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2lax_loop_shape.h	2024-07-25 16:20:50.120040559 -0400
@@ -60,13 +60,13 @@
   S2LaxLoopShape& operator=(S2LaxLoopShape&& other);
 
   // Constructs an S2LaxLoopShape with the given vertices.
-  explicit S2LaxLoopShape(absl::Span<const S2Point> vertices);
+  explicit S2LaxLoopShape(shield_absl::Span<const S2Point> vertices);
 
   // Constructs an S2LaxLoopShape from the given S2Loop, by copying its data.
   explicit S2LaxLoopShape(const S2Loop& loop);
 
   // Initializes an S2LaxLoopShape with the given vertices.
-  void Init(absl::Span<const S2Point> vertices);
+  void Init(shield_absl::Span<const S2Point> vertices);
 
   // Initializes an S2LaxLoopShape from the given S2Loop, by copying its data.
   //
@@ -130,12 +130,12 @@
   //
   // ENSURES:  loop->vertex(i) == (*vertex_array)[vertex_ids[i]]
   // REQUIRES: "vertex_array" persists for the lifetime of this object.
-  explicit S2VertexIdLaxLoopShape(absl::Span<const int32> vertex_ids,
+  explicit S2VertexIdLaxLoopShape(shield_absl::Span<const int32> vertex_ids,
                                   const S2Point* vertex_array);
 
   // Initializes the shape from the given vertex array and indices.
   // "vertex_ids" is a vector of indices into "vertex_array".
-  void Init(absl::Span<const int32> vertex_ids, const S2Point* vertex_array);
+  void Init(shield_absl::Span<const int32> vertex_ids, const S2Point* vertex_array);
 
   // Returns the number of vertices in the loop.
   int num_vertices() const { return num_vertices_; }
diff -u -r '--exclude=.git' a/src/s2/s2lax_polygon_shape.cc b/src/s2/s2lax_polygon_shape.cc
--- a/src/s2/s2lax_polygon_shape.cc	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2lax_polygon_shape.cc	2024-07-25 16:20:50.156040706 -0400
@@ -39,8 +39,8 @@
 #include "s2/util/coding/coder.h"
 #include "s2/util/coding/varint.h"
 
-using absl::MakeSpan;
-using absl::Span;
+using shield_absl::MakeSpan;
+using shield_absl::Span;
 using std::make_unique;
 using std::unique_ptr;
 using std::vector;
@@ -51,7 +51,7 @@
 unique_ptr<T> make_unique_for_overwrite(size_t n) {
   // We only need to support this one variant.
   static_assert(std::is_array<T>::value, "T must be an array type");
-  return unique_ptr<T>(new typename absl::remove_extent_t<T>[n]);
+  return unique_ptr<T>(new typename shield_absl::remove_extent_t<T>[n]);
 }
 }  // namespace
 
@@ -75,9 +75,9 @@
 
 S2LaxPolygonShape::S2LaxPolygonShape(S2LaxPolygonShape&& b)
     : S2Shape(std::move(b)),
-      num_loops_(absl::exchange(b.num_loops_, 0)),
+      num_loops_(shield_absl::exchange(b.num_loops_, 0)),
       prev_loop_(b.prev_loop_.exchange(0, std::memory_order_relaxed)),
-      num_vertices_(absl::exchange(b.num_vertices_, 0)),
+      num_vertices_(shield_absl::exchange(b.num_vertices_, 0)),
       vertices_(std::move(b.vertices_)),
       loop_starts_(std::move(b.loop_starts_)) {}
 
@@ -88,10 +88,10 @@
   // move any of its private state.  This is a little odd since b is in a
   // half-moved state after calling but is ultimately safe.
   S2Shape::operator=(static_cast<S2Shape&&>(b));
-  num_loops_ = absl::exchange(b.num_loops_, 0);
+  num_loops_ = shield_absl::exchange(b.num_loops_, 0);
   prev_loop_.store(b.prev_loop_.exchange(0, memory_order_relaxed),
                    memory_order_relaxed);
-  num_vertices_ = absl::exchange(b.num_vertices_, 0);
+  num_vertices_ = shield_absl::exchange(b.num_vertices_, 0);
   vertices_ = std::move(b.vertices_);
   loop_starts_ = std::move(b.loop_starts_);
   return *this;
@@ -139,7 +139,7 @@
     // overwrite them, and it would also save 8 bytes of memory allocation
     // since "new T[]" stores its own copy of the array size.
     //
-    // Note that even absl::make_unique_for_overwrite<> and c++20's
+    // Note that even shield_absl::make_unique_for_overwrite<> and c++20's
     // std::make_unique_for_overwrite<T[]> default-construct all elements when
     // T is a class type.
     vertices_ = make_unique<S2Point[]>(num_vertices_);
@@ -255,7 +255,7 @@
 
 EncodedS2LaxPolygonShape::EncodedS2LaxPolygonShape(EncodedS2LaxPolygonShape&& b)
     : S2Shape(std::move(b)),
-      num_loops_(absl::exchange(b.num_loops_, 0)),
+      num_loops_(shield_absl::exchange(b.num_loops_, 0)),
       prev_loop_(b.prev_loop_.exchange(0, std::memory_order_relaxed)),
       vertices_(std::move(b.vertices_)),
       loop_starts_(std::move(b.loop_starts_)) {}
@@ -266,7 +266,7 @@
   // move any of its private state.  This is a little odd since b is in a
   // half-moved state after calling but is ultimately safe.
   S2Shape::operator=(static_cast<S2Shape&&>(b));
-  num_loops_ = absl::exchange(b.num_loops_, 0);
+  num_loops_ = shield_absl::exchange(b.num_loops_, 0);
   prev_loop_.store(b.prev_loop_.exchange(0, std::memory_order_relaxed),
                    std::memory_order_relaxed);
   vertices_ = std::move(b.vertices_);
diff -u -r '--exclude=.git' a/src/s2/s2lax_polygon_shape.h b/src/s2/s2lax_polygon_shape.h
--- a/src/s2/s2lax_polygon_shape.h	2024-07-25 16:38:02.903739511 -0400
+++ b/src/s2/s2lax_polygon_shape.h	2024-07-25 16:20:50.156040706 -0400
@@ -102,7 +102,7 @@
 
   // Alternative version that can be used to avoid copying all the vertex data
   // when it is stored using something other than std::vector.
-  explicit S2LaxPolygonShape(absl::Span<const absl::Span<const S2Point>> loops);
+  explicit S2LaxPolygonShape(shield_absl::Span<const shield_absl::Span<const S2Point>> loops);
 
   // Constructs an S2LaxPolygonShape from an S2Polygon, by copying its data.
   // Full and empty S2Polygons are supported.
@@ -113,7 +113,7 @@
 
   // Alternative version that can be used to avoid copying all the vertex data
   // when it is stored using something other than std::vector.
-  void Init(absl::Span<const absl::Span<const S2Point>> loops);
+  void Init(shield_absl::Span<const shield_absl::Span<const S2Point>> loops);
 
   // Initializes an S2LaxPolygonShape from an S2Polygon by copying its data.
   // Full and empty S2Polygons are supported.
diff -u -r '--exclude=.git' a/src/s2/s2lax_polygon_shape_test.cc b/src/s2/s2lax_polygon_shape_test.cc
--- a/src/s2/s2lax_polygon_shape_test.cc	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2lax_polygon_shape_test.cc	2024-07-25 16:20:50.140040641 -0400
@@ -49,7 +49,7 @@
 #include "s2/s2testing.h"
 #include "s2/s2text_format.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using s2textformat::MakePolygonOrDie;
 using std::make_unique;
 using std::unique_ptr;
diff -u -r '--exclude=.git' a/src/s2/s2lax_polyline_shape.cc b/src/s2/s2lax_polyline_shape.cc
--- a/src/s2/s2lax_polyline_shape.cc	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2lax_polyline_shape.cc	2024-07-25 16:20:50.112040525 -0400
@@ -31,18 +31,18 @@
 #include "s2/s2polyline.h"
 #include "s2/s2shape.h"
 
-using absl::MakeSpan;
-using absl::Span;
+using shield_absl::MakeSpan;
+using shield_absl::Span;
 using std::make_unique;
 
 S2LaxPolylineShape::S2LaxPolylineShape(S2LaxPolylineShape&& other)
     : S2Shape(std::move(other)),
-      num_vertices_(absl::exchange(other.num_vertices_, 0)),
+      num_vertices_(shield_absl::exchange(other.num_vertices_, 0)),
       vertices_(std::move(other.vertices_)) {}
 
 S2LaxPolylineShape& S2LaxPolylineShape::operator=(S2LaxPolylineShape&& other) {
   S2Shape::operator=(static_cast<S2Shape&&>(other));
-  num_vertices_ = absl::exchange(other.num_vertices_, 0);
+  num_vertices_ = shield_absl::exchange(other.num_vertices_, 0);
   vertices_ = std::move(other.vertices_);
   return *this;
 }
diff -u -r '--exclude=.git' a/src/s2/s2lax_polyline_shape.h b/src/s2/s2lax_polyline_shape.h
--- a/src/s2/s2lax_polyline_shape.h	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2lax_polyline_shape.h	2024-07-25 16:20:50.108040509 -0400
@@ -57,14 +57,14 @@
   S2LaxPolylineShape& operator=(S2LaxPolylineShape&& other);
 
   // Constructs an S2LaxPolylineShape with the given vertices.
-  explicit S2LaxPolylineShape(absl::Span<const S2Point> vertices);
+  explicit S2LaxPolylineShape(shield_absl::Span<const S2Point> vertices);
 
   // Constructs an S2LaxPolylineShape from the given S2Polyline, by copying
   // its data.
   explicit S2LaxPolylineShape(const S2Polyline& polyline);
 
   // Initializes an S2LaxPolylineShape with the given vertices.
-  void Init(absl::Span<const S2Point> vertices);
+  void Init(shield_absl::Span<const S2Point> vertices);
 
   // Initializes an S2LaxPolylineShape from the given S2Polyline, by copying
   // its data.
diff -u -r '--exclude=.git' a/src/s2/s2loop.cc b/src/s2/s2loop.cc
--- a/src/s2/s2loop.cc	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2loop.cc	2024-07-25 16:20:50.144040657 -0400
@@ -71,9 +71,9 @@
 #include "s2/util/coding/coder.h"
 #include "s2/util/math/matrix3x3.h"
 
-using absl::flat_hash_set;
-using absl::MakeSpan;
-using absl::Span;
+using shield_absl::flat_hash_set;
+using shield_absl::MakeSpan;
+using shield_absl::Span;
 using std::make_unique;
 using std::pair;
 using std::unique_ptr;
@@ -110,8 +110,8 @@
 #ifndef SWIG
 S2Loop::S2Loop(S2Loop&& b)
     : S2Region(std::move(b)),
-      depth_(absl::exchange(b.depth_, 0)),
-      num_vertices_(absl::exchange(b.num_vertices_, 0)),
+      depth_(shield_absl::exchange(b.depth_, 0)),
+      num_vertices_(shield_absl::exchange(b.num_vertices_, 0)),
       vertices_(std::move(b.vertices_)),
       s2debug_override_(std::move(b.s2debug_override_)),
       origin_inside_(std::move(b.origin_inside_)),
@@ -129,8 +129,8 @@
 
 S2Loop& S2Loop::operator=(S2Loop&& b) {
   S2Region::operator=(static_cast<S2Region&&>(b));
-  depth_ = absl::exchange(b.depth_, 0);
-  num_vertices_ = absl::exchange(b.num_vertices_, 0);
+  depth_ = shield_absl::exchange(b.depth_, 0);
+  num_vertices_ = shield_absl::exchange(b.num_vertices_, 0);
   vertices_ = std::move(b.vertices_);
   s2debug_override_ = std::move(b.s2debug_override_);
   origin_inside_ = std::move(b.origin_inside_);
@@ -183,7 +183,7 @@
 bool S2Loop::IsValid() const {
   S2Error error;
   if (FindValidationError(&error)) {
-    S2_LOG_IF(ERROR, absl::GetFlag(FLAGS_s2debug)) << error;
+    S2_LOG_IF(ERROR, shield_absl::GetFlag(FLAGS_s2debug)) << error;
     return false;
   }
   return true;
@@ -325,10 +325,10 @@
 
 void S2Loop::InitIndex() {
   index_.Add(make_unique<Shape>(this));
-  if (!absl::GetFlag(FLAGS_s2loop_lazy_indexing)) {
+  if (!shield_absl::GetFlag(FLAGS_s2loop_lazy_indexing)) {
     index_.ForceBuild();
   }
-  if (absl::GetFlag(FLAGS_s2debug) && s2debug_override_ == S2Debug::ALLOW) {
+  if (shield_absl::GetFlag(FLAGS_s2debug) && s2debug_override_ == S2Debug::ALLOW) {
     // Note that FLAGS_s2debug is false in optimized builds (by default).
     S2_CHECK(IsValid());
   }
@@ -655,7 +655,7 @@
   // and such loops encode and decode properly.
   if (decoder->avail() < sizeof(uint32)) return false;
   const uint32 num_vertices = decoder->get32();
-  if (num_vertices > static_cast<uint32>(absl::GetFlag(
+  if (num_vertices > static_cast<uint32>(shield_absl::GetFlag(
                          FLAGS_s2polygon_decode_max_num_vertices))) {
     return false;
   }
@@ -1416,7 +1416,7 @@
     return false;
   }
   if (unsigned_num_vertices == 0 ||
-      unsigned_num_vertices > static_cast<uint32>(absl::GetFlag(
+      unsigned_num_vertices > static_cast<uint32>(shield_absl::GetFlag(
                                   FLAGS_s2polygon_decode_max_num_vertices))) {
     return false;
   }
diff -u -r '--exclude=.git' a/src/s2/s2loop.h b/src/s2/s2loop.h
--- a/src/s2/s2loop.h	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2loop.h	2024-07-25 16:20:50.112040525 -0400
@@ -104,7 +104,7 @@
 #endif
 
   // Convenience constructor that calls Init() with the given vertices.
-  explicit S2Loop(absl::Span<const S2Point> vertices);
+  explicit S2Loop(shield_absl::Span<const S2Point> vertices);
 
   // Convenience constructor to disable the automatic validity checking
   // controlled by the --s2debug flag.  Example:
@@ -119,13 +119,13 @@
   //
   // The main reason to use this constructor is if you intend to call
   // IsValid() explicitly.  See set_s2debug_override() for details.
-  S2Loop(absl::Span<const S2Point> vertices, S2Debug override);
+  S2Loop(shield_absl::Span<const S2Point> vertices, S2Debug override);
 
   // Initialize a loop with given vertices.  The last vertex is implicitly
   // connected to the first.  All points should be unit length.  Loops must
   // have at least 3 vertices (except for the empty and full loops, see
   // kEmpty and kFull).  This method may be called multiple times.
-  void Init(absl::Span<const S2Point> vertices);
+  void Init(shield_absl::Span<const S2Point> vertices);
 
   // A special vertex chain of length 1 that creates an empty loop (i.e., a
   // loop with no edges that contains no points).  Example usage:
diff -u -r '--exclude=.git' a/src/s2/s2loop_measures.cc b/src/s2/s2loop_measures.cc
--- a/src/s2/s2loop_measures.cc	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2loop_measures.cc	2024-07-25 16:20:50.104040492 -0400
@@ -305,7 +305,7 @@
   int n = loop.size();
   if (n == 0) return LoopOrder(0, 1);
 
-  absl::InlinedVector<int, 4> min_indices;
+  shield_absl::InlinedVector<int, 4> min_indices;
   min_indices.push_back(0);
   for (int i = 1; i < n; ++i) {
     if (loop[i] <= loop[min_indices[0]]) {
diff -u -r '--exclude=.git' a/src/s2/s2loop_measures_test.cc b/src/s2/s2loop_measures_test.cc
--- a/src/s2/s2loop_measures_test.cc	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2loop_measures_test.cc	2024-07-25 16:20:50.144040657 -0400
@@ -39,7 +39,7 @@
 #include "s2/s2text_format.h"
 #include "s2/util/math/mathutil.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using s2textformat::ParsePointsOrDie;
 using std::fabs;
 using std::min;
@@ -87,7 +87,7 @@
 std::string BruteForceQuadraticCyclicallyCanonicalize(const string_view s) {
   std::string answer;
   for (int i = 0; i < s.size(); ++i) {
-    const std::string candidate = absl::StrCat(s.substr(i), s.substr(0, i));
+    const std::string candidate = shield_absl::StrCat(s.substr(i), s.substr(0, i));
     if (i == 0 || candidate < answer) answer = candidate;
   }
   return answer;
diff -u -r '--exclude=.git' a/src/s2/s2loop_test.cc b/src/s2/s2loop_test.cc
--- a/src/s2/s2loop_test.cc	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2loop_test.cc	2024-07-25 16:20:50.116040541 -0400
@@ -59,9 +59,9 @@
 #include "s2/s2text_format.h"
 #include "s2/util/math/matrix3x3.h"
 
-using absl::flat_hash_map;
-using absl::flat_hash_set;
-using absl::string_view;
+using shield_absl::flat_hash_map;
+using shield_absl::flat_hash_set;
+using shield_absl::string_view;
 using ::s2textformat::MakeLoopOrDie;
 using std::fabs;
 using std::make_unique;
@@ -235,7 +235,7 @@
   // Wrapper function that encodes "loop" into "encoder" using the private
   // EncodeCompressed() method.
   void TestEncodeCompressed(const S2Loop& loop, int level, Encoder* encoder) {
-    absl::FixedArray<S2XYZFaceSiTi> points(loop.num_vertices());
+    shield_absl::FixedArray<S2XYZFaceSiTi> points(loop.num_vertices());
     loop.GetXYZFaceSiTiVertices(points.data());
     loop.EncodeCompressed(encoder, points.data(), level);
   }
diff -u -r '--exclude=.git' a/src/s2/s2max_distance_targets_test.cc b/src/s2/s2max_distance_targets_test.cc
--- a/src/s2/s2max_distance_targets_test.cc	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2max_distance_targets_test.cc	2024-07-25 16:20:50.140040641 -0400
@@ -257,7 +257,7 @@
 
 vector<int> GetContainingShapes(S2MaxDistanceTarget* target,
                                 const S2ShapeIndex& index, int max_shapes) {
-  absl::btree_set<int32> shape_ids;
+  shield_absl::btree_set<int32> shape_ids;
   (void)target->VisitContainingShapes(
       index, [&shape_ids, max_shapes](S2Shape* containing_shape,
                                       const S2Point& target_point) {
diff -u -r '--exclude=.git' a/src/s2/s2memory_tracker.h b/src/s2/s2memory_tracker.h
--- a/src/s2/s2memory_tracker.h	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2memory_tracker.h	2024-07-25 16:20:50.136040625 -0400
@@ -146,7 +146,7 @@
   // where the error occurred (which will free all its memory and restore the
   // S2MemoryTracker to an accurate state).
   template <typename... Args>
-  void SetError(S2Error::Code code, const absl::FormatSpec<Args...>& format,
+  void SetError(S2Error::Code code, const shield_absl::FormatSpec<Args...>& format,
                 const Args&... args);
 
   // This version of SetError copies an existing error.
@@ -317,7 +317,7 @@
     static int64 GetCompactArrayAllocBytes(const gtl::compact_array<T>& array);
 
     // Returns the estimated minimum number of allocated bytes for each
-    // additional entry in an absl::btree_* container (e.g. absl::btree_map)
+    // additional entry in an shield_absl::btree_* container (e.g. shield_absl::btree_map)
     // including overhead.  This estimate is accurate only if the container
     // nodes are nearly full, as when elements are added in sorted order.
     // Otherwise nodes are expected to be 75% full on average.
@@ -354,7 +354,7 @@
 
 template <typename... Args>
 void S2MemoryTracker::SetError(S2Error::Code code,
-                               const absl::FormatSpec<Args...>& format,
+                               const shield_absl::FormatSpec<Args...>& format,
                                const Args&... args) {
   error_.Init(code, format, args...);
 }
diff -u -r '--exclude=.git' a/src/s2/s2min_distance_targets_test.cc b/src/s2/s2min_distance_targets_test.cc
--- a/src/s2/s2min_distance_targets_test.cc	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2min_distance_targets_test.cc	2024-07-25 16:20:50.148040674 -0400
@@ -130,7 +130,7 @@
 
 vector<int> GetContainingShapes(S2MinDistanceTarget* target,
                                 const S2ShapeIndex& index, int max_shapes) {
-  absl::btree_set<int32> shape_ids;
+  shield_absl::btree_set<int32> shape_ids;
   (void) target->VisitContainingShapes(
       index, [&shape_ids, max_shapes](S2Shape* containing_shape,
                                       const S2Point& target_point) {
diff -u -r '--exclude=.git' a/src/s2/s2point_compression.cc b/src/s2/s2point_compression.cc
--- a/src/s2/s2point_compression.cc	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2point_compression.cc	2024-07-25 16:20:50.120040559 -0400
@@ -35,7 +35,7 @@
 #include "s2/util/coding/transforms.h"
 #include "s2/util/endian/endian.h"
 
-using absl::Span;
+using shield_absl::Span;
 using std::pair;
 using std::vector;
 
@@ -59,7 +59,7 @@
     // but since this would only help if there were more than 21 faces, it will
     // be a small overall savings, much smaller than the bound encoding.
     encoder->put_varint64(
-        S2CellId::kNumFaces * absl::implicit_cast<int64>(count) + face);
+        S2CellId::kNumFaces * shield_absl::implicit_cast<int64>(count) + face);
     S2_DCHECK_GE(encoder->avail(), 0);
   }
 
@@ -319,7 +319,7 @@
 void S2EncodePointsCompressed(Span<const S2XYZFaceSiTi> points,
                               int level,
                               Encoder* encoder) {
-  absl::FixedArray<pair<int, int>> vertices_pi_qi(points.size());
+  shield_absl::FixedArray<pair<int, int>> vertices_pi_qi(points.size());
   vector<int> off_center;
   Faces faces;
   for (size_t i = 0; i < points.size(); ++i) {
diff -u -r '--exclude=.git' a/src/s2/s2point_compression.h b/src/s2/s2point_compression.h
--- a/src/s2/s2point_compression.h	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2point_compression.h	2024-07-25 16:20:50.144040657 -0400
@@ -68,13 +68,13 @@
 // Encode the points in the encoder, using an optimized compressed format for
 // points at the center of a cell at 'level', plus 3 double values for the
 // others.
-void S2EncodePointsCompressed(absl::Span<const S2XYZFaceSiTi> points,
+void S2EncodePointsCompressed(shield_absl::Span<const S2XYZFaceSiTi> points,
                               int level, Encoder* encoder);
 
 // Decode points encoded with S2EncodePointsCompressed. Requires that the
 // level is the level that was used in S2EncodePointsCompressed. Ensures
 // that the decoded points equal the encoded points. Returns true on success.
 bool S2DecodePointsCompressed(Decoder* decoder, int level,
-                              absl::Span<S2Point> points);
+                              shield_absl::Span<S2Point> points);
 
 #endif  // S2_S2POINT_COMPRESSION_H_
diff -u -r '--exclude=.git' a/src/s2/s2point_compression_test.cc b/src/s2/s2point_compression_test.cc
--- a/src/s2/s2point_compression_test.cc	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2point_compression_test.cc	2024-07-25 16:20:50.132040607 -0400
@@ -34,9 +34,9 @@
 #include "s2/s2text_format.h"
 #include "s2/util/coding/coder.h"
 
-using absl::FixedArray;
-using absl::MakeSpan;
-using absl::Span;
+using shield_absl::FixedArray;
+using shield_absl::MakeSpan;
+using shield_absl::Span;
 using std::vector;
 
 S2_DEFINE_int32(s2point_compression_bm_level, 30,
@@ -46,9 +46,9 @@
 
 namespace {
 
-using ::absl::FixedArray;
-using ::absl::MakeSpan;
-using ::absl::Span;
+using ::shield_absl::FixedArray;
+using ::shield_absl::MakeSpan;
+using ::shield_absl::Span;
 using ::std::vector;
 
 S2Point SnapPointToLevel(const S2Point& point, int level) {
diff -u -r '--exclude=.git' a/src/s2/s2point.h b/src/s2/s2point.h
--- a/src/s2/s2point.h	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2point.h	2024-07-25 16:20:50.128040591 -0400
@@ -171,6 +171,6 @@
 // nonstandard extensions (e.g., hash_map).  It is defined such that if two
 // S2Points compare equal to each other, they have the same hash.  (This
 // requires that positive and negative zero hash to the same value.)
-using S2PointHash = absl::Hash<S2Point>;
+using S2PointHash = shield_absl::Hash<S2Point>;
 
 #endif  // S2_S2POINT_H_
diff -u -r '--exclude=.git' a/src/s2/s2point_index.h b/src/s2/s2point_index.h
--- a/src/s2/s2point_index.h	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2point_index.h	2024-07-25 16:20:50.140040641 -0400
@@ -33,7 +33,7 @@
 namespace s2internal {
 // Hack to expose bytes_used.
 template <typename Key, typename Value>
-class BTreeMultimap : public absl::btree_multimap<Key, Value> {
+class BTreeMultimap : public shield_absl::btree_multimap<Key, Value> {
  public:
   size_t bytes_used() const { return this->tree_.bytes_used(); }
 };
diff -u -r '--exclude=.git' a/src/s2/s2point_span.h b/src/s2/s2point_span.h
--- a/src/s2/s2point_span.h	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2point_span.h	2024-07-25 16:20:50.108040509 -0400
@@ -31,7 +31,7 @@
 // NOTE: S2PointSpan has an implicit constructor from any container type with
 // data() and size() methods (such as std::vector and std::array).  Therefore
 // you can use such containers as arguments for any S2PointSpan parameter.
-using S2PointSpan = absl::Span<const S2Point>;
+using S2PointSpan = shield_absl::Span<const S2Point>;
 
 // Like S2PointSpan, except that operator[] maps index values in the range
 // [n, 2*n-1] to the range [0, n-1] by subtracting n (where n == size()).
@@ -44,7 +44,7 @@
 class S2PointLoopSpan : public S2PointSpan {
  public:
   // Inherit all constructors.
-  using absl::Span<const S2Point>::Span;
+  using shield_absl::Span<const S2Point>::Span;
 
   // Like operator[], but allows index values in the range [0, 2*size()-1]
   // where each index i >= size() is mapped to i - size().
diff -u -r '--exclude=.git' a/src/s2/s2point_test.cc b/src/s2/s2point_test.cc
--- a/src/s2/s2point_test.cc	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2point_test.cc	2024-07-25 16:20:50.148040674 -0400
@@ -35,8 +35,8 @@
 
 TEST(S2Point, HashSpreads) {
   int kTestPoints = 1 << 16;
-  absl::flat_hash_set<size_t> set;
-  absl::flat_hash_set<S2Point, S2PointHash> points;
+  shield_absl::flat_hash_set<size_t> set;
+  shield_absl::flat_hash_set<S2Point, S2PointHash> points;
   S2PointHash hasher;
   S2Point base = S2Point(1, 1, 1);
   for (int i = 0; i < kTestPoints; ++i) {
@@ -61,7 +61,7 @@
 }
 
 TEST(S2Point, CoderWorks) {
-  S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed));
+  S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed));
 
   S2Point point = S2Testing::RandomPoint();
   S2Error error;
diff -u -r '--exclude=.git' a/src/s2/s2point_vector_shape_test.cc b/src/s2/s2point_vector_shape_test.cc
--- a/src/s2/s2point_vector_shape_test.cc	2024-07-25 16:38:02.907739524 -0400
+++ b/src/s2/s2point_vector_shape_test.cc	2024-07-25 16:20:50.120040559 -0400
@@ -44,7 +44,7 @@
 
 TEST(S2PointVectorShape, ConstructionAndAccess) {
   vector<S2Point> points;
-  S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed));
+  S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed));
   const int kNumPoints = 100;
   for (int i = 0; i < kNumPoints; ++i) {
     points.push_back(S2Testing::RandomPoint());
diff -u -r '--exclude=.git' a/src/s2/s2polygon.cc b/src/s2/s2polygon.cc
--- a/src/s2/s2polygon.cc	2024-07-25 16:38:02.911739538 -0400
+++ b/src/s2/s2polygon.cc	2024-07-25 16:20:50.136040625 -0400
@@ -78,7 +78,7 @@
 #include "s2/s2shapeutil_visit_crossing_edge_pairs.h"
 #include "s2/util/coding/coder.h"
 
-using absl::flat_hash_set;
+using shield_absl::flat_hash_set;
 using s2builderutil::IdentitySnapFunction;
 using s2builderutil::S2CellIdSnapFunction;
 using s2builderutil::S2PolygonLayer;
@@ -138,8 +138,8 @@
       loops_(std::move(b.loops_)),
       s2debug_override_(std::move(b.s2debug_override_)),
       error_inconsistent_loop_orientations_(
-          absl::exchange(b.error_inconsistent_loop_orientations_, 0)),
-      num_vertices_(absl::exchange(b.num_vertices_, 0)),
+          shield_absl::exchange(b.error_inconsistent_loop_orientations_, 0)),
+      num_vertices_(shield_absl::exchange(b.num_vertices_, 0)),
       unindexed_contains_calls_(
           b.unindexed_contains_calls_.exchange(0, std::memory_order_relaxed)),
       bound_(std::move(b.bound_)),
@@ -161,8 +161,8 @@
   loops_ = std::move(b.loops_);
   s2debug_override_ = std::move(b.s2debug_override_);
   error_inconsistent_loop_orientations_ =
-      absl::exchange(b.error_inconsistent_loop_orientations_, 0);
-  num_vertices_ = absl::exchange(b.num_vertices_, 0);
+      shield_absl::exchange(b.error_inconsistent_loop_orientations_, 0);
+  num_vertices_ = shield_absl::exchange(b.num_vertices_, 0);
   unindexed_contains_calls_.store(
       b.unindexed_contains_calls_.exchange(0, std::memory_order_relaxed),
       std::memory_order_relaxed);
@@ -233,7 +233,7 @@
 bool S2Polygon::IsValid() const {
   S2Error error;
   if (FindValidationError(&error)) {
-    S2_LOG_IF(ERROR, absl::GetFlag(FLAGS_s2debug)) << error;
+    S2_LOG_IF(ERROR, shield_absl::GetFlag(FLAGS_s2debug)) << error;
     return false;
   }
   return true;
@@ -365,10 +365,10 @@
 void S2Polygon::InitIndex() {
   S2_DCHECK_EQ(0, index_.num_shape_ids());
   index_.Add(make_unique<Shape>(this));
-  if (!absl::GetFlag(FLAGS_s2polygon_lazy_indexing)) {
+  if (!shield_absl::GetFlag(FLAGS_s2polygon_lazy_indexing)) {
     index_.ForceBuild();
   }
-  if (absl::GetFlag(FLAGS_s2debug) && s2debug_override_ == S2Debug::ALLOW) {
+  if (shield_absl::GetFlag(FLAGS_s2debug) && s2debug_override_ == S2Debug::ALLOW) {
     // Note that FLAGS_s2debug is false in optimized builds (by default).
     S2_CHECK(IsValid());
   }
@@ -503,7 +503,7 @@
       // property of the entire set of loops.  In general there is no way to
       // determine which ones are incorrect.
       error_inconsistent_loop_orientations_ = true;
-      if (absl::GetFlag(FLAGS_s2debug) && s2debug_override_ == S2Debug::ALLOW) {
+      if (shield_absl::GetFlag(FLAGS_s2debug) && s2debug_override_ == S2Debug::ALLOW) {
         // The FLAGS_s2debug validity checking usually happens in InitIndex(),
         // but this error is detected too late for that.
         S2_CHECK(IsValid());  // Always fails.
@@ -732,7 +732,7 @@
     return;
   }
   // Converts all the polygon vertices to S2XYZFaceSiTi format.
-  absl::FixedArray<S2XYZFaceSiTi> all_vertices(num_vertices_);
+  shield_absl::FixedArray<S2XYZFaceSiTi> all_vertices(num_vertices_);
   S2XYZFaceSiTi* current_loop_vertices = all_vertices.data();
   for (const unique_ptr<S2Loop>& loop : loops_) {
     loop->GetXYZFaceSiTiVertices(current_loop_vertices);
@@ -817,7 +817,7 @@
   // polygon has zero loops and such polygons encode and decode properly.
   const uint32 num_loops = decoder->get32();
   if (num_loops > static_cast<uint32>(
-                      absl::GetFlag(FLAGS_s2polygon_decode_max_num_loops)))
+                      shield_absl::GetFlag(FLAGS_s2polygon_decode_max_num_loops)))
     return false;
   loops_.reserve(num_loops);
   num_vertices_ = 0;
@@ -1062,7 +1062,7 @@
   R2Point uv;
   if (S2::FaceXYZtoUV(cell.face(), p, &uv)) {
     R2Rect bound = cell.GetBoundUV();
-    if (absl::GetFlag(FLAGS_s2debug))
+    if (shield_absl::GetFlag(FLAGS_s2debug))
       S2_DCHECK(bound.Expanded(tolerance_uv).Contains(uv));
     if (fabs(uv[1] - bound[1][0]) <= tolerance_uv) mask |= 1;
     if (fabs(uv[0] - bound[0][1]) <= tolerance_uv) mask |= 2;
@@ -1498,7 +1498,7 @@
   uint32 num_loops;
   if (!decoder->get_varint32(&num_loops)) return false;
   if (num_loops > static_cast<uint32>(
-                      absl::GetFlag(FLAGS_s2polygon_decode_max_num_loops)))
+                      shield_absl::GetFlag(FLAGS_s2polygon_decode_max_num_loops)))
     return false;
   loops_.reserve(num_loops);
   for (size_t i = 0; i < num_loops; ++i) {
diff -u -r '--exclude=.git' a/src/s2/s2polygon.h b/src/s2/s2polygon.h
--- a/src/s2/s2polygon.h	2024-07-25 16:38:02.911739538 -0400
+++ b/src/s2/s2polygon.h	2024-07-25 16:20:50.124040575 -0400
@@ -829,7 +829,7 @@
   // A map from each loop to its immediate children with respect to nesting.
   // This map is built during initialization of multi-loop polygons to
   // determine which are shells and which are holes, and then discarded.
-  typedef absl::flat_hash_map<S2Loop*, std::vector<S2Loop*> > LoopMap;
+  typedef shield_absl::flat_hash_map<S2Loop*, std::vector<S2Loop*> > LoopMap;
 
   void InsertLoop(S2Loop* new_loop, S2Loop* parent, LoopMap* loop_map);
   void InitLoops(LoopMap* loop_map);
diff -u -r '--exclude=.git' a/src/s2/s2polygon_test.cc b/src/s2/s2polygon_test.cc
--- a/src/s2/s2polygon_test.cc	2024-07-25 16:38:02.911739538 -0400
+++ b/src/s2/s2polygon_test.cc	2024-07-25 16:20:50.116040541 -0400
@@ -79,9 +79,9 @@
 #include "s2/util/gtl/legacy_random_shuffle.h"
 #include "s2/util/math/matrix3x3.h"
 
-using absl::flat_hash_set;
-using absl::StrCat;
-using absl::string_view;
+using shield_absl::flat_hash_set;
+using shield_absl::StrCat;
+using shield_absl::string_view;
 using s2builderutil::IntLatLngSnapFunction;
 using s2builderutil::S2PolygonLayer;
 using std::make_unique;
@@ -402,7 +402,7 @@
   // tests should fail, while all of the approximate test should succeed.
   const int kIters = 1000;
   int exact_contains = 0, exact_disjoint = 0;
-  S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed));
+  S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed));
   for (int iter = 0; iter < kIters; ++iter) {
     S2CellId id = S2Testing::GetRandomCellId(10);
     S2Polygon parent_polygon((S2Cell(id)));
@@ -2250,7 +2250,7 @@
     init_oriented_ = false;
     modify_polygon_hook_ = nullptr;
     rnd_ = &S2Testing::rnd;
-    rnd_->Reset(absl::GetFlag(FLAGS_s2_random_seed));
+    rnd_->Reset(shield_absl::GetFlag(FLAGS_s2_random_seed));
   }
 
   ~IsValidTest() override { Reset(); }
@@ -2883,7 +2883,7 @@
   const int kMaxBytes = 256;
 
   // The data array.
-  absl::FixedArray<int8> data_array_;
+  shield_absl::FixedArray<int8> data_array_;
 
   // Encoder that is used to put data into the array.
   Encoder encoder_;
diff -u -r '--exclude=.git' a/src/s2/s2polyline.cc b/src/s2/s2polyline.cc
--- a/src/s2/s2polyline.cc	2024-07-25 16:38:02.911739538 -0400
+++ b/src/s2/s2polyline.cc	2024-07-25 16:20:50.148040674 -0400
@@ -61,8 +61,8 @@
 #include "s2/util/coding/coder.h"
 #include "s2/util/math/matrix3x3.h"
 
-using absl::flat_hash_set;
-using absl::Span;
+using shield_absl::flat_hash_set;
+using shield_absl::Span;
 using s2builderutil::S2CellIdSnapFunction;
 using s2builderutil::S2PolylineLayer;
 using std::make_unique;
@@ -78,13 +78,13 @@
 
 S2Polyline::S2Polyline(S2Polyline&& other)
   : s2debug_override_(other.s2debug_override_),
-    num_vertices_(absl::exchange(other.num_vertices_, 0)),
+    num_vertices_(shield_absl::exchange(other.num_vertices_, 0)),
     vertices_(std::move(other.vertices_)) {
 }
 
 S2Polyline& S2Polyline::operator=(S2Polyline&& other) {
   s2debug_override_ = other.s2debug_override_;
-  num_vertices_ = absl::exchange(other.num_vertices_, 0);
+  num_vertices_ = shield_absl::exchange(other.num_vertices_, 0);
   vertices_ = std::move(other.vertices_);
   return *this;
 }
@@ -120,7 +120,7 @@
   num_vertices_ = vertices.size();
   vertices_ = make_unique<S2Point[]>(num_vertices_);
   std::copy(vertices.begin(), vertices.end(), &vertices_[0]);
-  if (absl::GetFlag(FLAGS_s2debug) && s2debug_override_ == S2Debug::ALLOW) {
+  if (shield_absl::GetFlag(FLAGS_s2debug) && s2debug_override_ == S2Debug::ALLOW) {
     S2_CHECK(IsValid());
   }
 }
@@ -131,7 +131,7 @@
   for (int i = 0; i < num_vertices_; ++i) {
     vertices_[i] = vertices[i].ToPoint();
   }
-  if (absl::GetFlag(FLAGS_s2debug) && s2debug_override_ == S2Debug::ALLOW) {
+  if (shield_absl::GetFlag(FLAGS_s2debug) && s2debug_override_ == S2Debug::ALLOW) {
     S2_CHECK(IsValid());
   }
 }
@@ -160,7 +160,7 @@
 bool S2Polyline::IsValid() const {
   S2Error error;
   if (FindValidationError(&error)) {
-    S2_LOG_IF(ERROR, absl::GetFlag(FLAGS_s2debug)) << error;
+    S2_LOG_IF(ERROR, shield_absl::GetFlag(FLAGS_s2debug)) << error;
     return false;
   }
   return true;
@@ -456,7 +456,7 @@
   vertices_ = make_unique<S2Point[]>(num_vertices_);
   decoder->getn(&vertices_[0], num_vertices_ * sizeof(vertices_[0]));
 
-  if (absl::GetFlag(FLAGS_s2debug) && s2debug_override_ == S2Debug::ALLOW) {
+  if (shield_absl::GetFlag(FLAGS_s2debug) && s2debug_override_ == S2Debug::ALLOW) {
     S2_CHECK(IsValid());
   }
   return true;
@@ -468,7 +468,7 @@
     return;
   }
   // Convert S2Points to (face, si, ti) representation.
-  absl::FixedArray<S2XYZFaceSiTi> all_vertices(num_vertices_);
+  shield_absl::FixedArray<S2XYZFaceSiTi> all_vertices(num_vertices_);
   for (int i = 0; i < num_vertices_; ++i) {
     all_vertices[i].xyz = vertices_[i];
     all_vertices[i].cell_level =
@@ -513,7 +513,7 @@
 }
 
 void S2Polyline::EncodeCompressed(Encoder* encoder,
-                                  absl::Span<const S2XYZFaceSiTi> all_vertices,
+                                  shield_absl::Span<const S2XYZFaceSiTi> all_vertices,
                                   int snap_level) const {
   // Set version number.
   encoder->Ensure(2 + Encoder::kVarintMax32);
@@ -541,7 +541,7 @@
   // This is more complicated due to the compressed encoding, but perhaps the
   // minimum required size can be bounded.
   points.resize(num_vertices);
-  if (!S2DecodePointsCompressed(decoder, snap_level, absl::MakeSpan(points))) {
+  if (!S2DecodePointsCompressed(decoder, snap_level, shield_absl::MakeSpan(points))) {
     return false;
   }
   Init(points);
diff -u -r '--exclude=.git' a/src/s2/s2polyline.h b/src/s2/s2polyline.h
--- a/src/s2/s2polyline.h	2024-07-25 16:38:02.911739538 -0400
+++ b/src/s2/s2polyline.h	2024-07-25 16:20:50.108040509 -0400
@@ -71,8 +71,8 @@
 #endif  // SWIG
 
   // Convenience constructors that call Init() with the given vertices.
-  explicit S2Polyline(absl::Span<const S2Point> vertices);
-  explicit S2Polyline(absl::Span<const S2LatLng> vertices);
+  explicit S2Polyline(shield_absl::Span<const S2Point> vertices);
+  explicit S2Polyline(shield_absl::Span<const S2LatLng> vertices);
 
   // Convenience constructors to disable the automatic validity checking
   // controlled by the --s2debug flag.  Example:
@@ -87,17 +87,17 @@
   //
   // The main reason to use this constructors is if you intend to call
   // IsValid() explicitly.  See set_s2debug_override() for details.
-  S2Polyline(absl::Span<const S2Point> vertices, S2Debug override);
-  S2Polyline(absl::Span<const S2LatLng> vertices, S2Debug override);
+  S2Polyline(shield_absl::Span<const S2Point> vertices, S2Debug override);
+  S2Polyline(shield_absl::Span<const S2LatLng> vertices, S2Debug override);
 
   // Initialize a polyline that connects the given vertices. Empty polylines are
   // allowed.  Adjacent vertices should not be identical or antipodal.  All
   // vertices should be unit length.
-  void Init(absl::Span<const S2Point> vertices);
+  void Init(shield_absl::Span<const S2Point> vertices);
 
   // Convenience initialization function that accepts latitude-longitude
   // coordinates rather than S2Points.
-  void Init(absl::Span<const S2LatLng> vertices);
+  void Init(shield_absl::Span<const S2LatLng> vertices);
 
   ~S2Polyline() override;
 
@@ -427,7 +427,7 @@
 
   // Encode the polylines's vertices using S2EncodePointsCompressed().
   void EncodeCompressed(Encoder* encoder,
-                        absl::Span<const S2XYZFaceSiTi> all_vertices,
+                        shield_absl::Span<const S2XYZFaceSiTi> all_vertices,
                         int snap_level) const;
 
   // Decode a polyline encoded with EncodeUncompressed(). Used by the Decode
diff -u -r '--exclude=.git' a/src/s2/s2polyline_simplifier_test.cc b/src/s2/s2polyline_simplifier_test.cc
--- a/src/s2/s2polyline_simplifier_test.cc	2024-07-25 16:38:02.911739538 -0400
+++ b/src/s2/s2polyline_simplifier_test.cc	2024-07-25 16:20:50.136040625 -0400
@@ -31,7 +31,7 @@
 #include "s2/s2testing.h"
 #include "s2/s2text_format.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using std::vector;
 
 void CheckSimplify(string_view src, string_view dst, string_view target,
diff -u -r '--exclude=.git' a/src/s2/s2polyline_test.cc b/src/s2/s2polyline_test.cc
--- a/src/s2/s2polyline_test.cc	2024-07-25 16:38:02.911739538 -0400
+++ b/src/s2/s2polyline_test.cc	2024-07-25 16:20:50.140040641 -0400
@@ -46,8 +46,8 @@
 #include "s2/s2text_format.h"
 #include "s2/util/coding/coder.h"
 
-using absl::StrCat;
-using absl::string_view;
+using shield_absl::StrCat;
+using shield_absl::string_view;
 using s2builderutil::S2CellIdSnapFunction;
 using std::fabs;
 using std::make_unique;
@@ -104,7 +104,7 @@
 
 TEST(S2Polyline, NoDataClone) {
   S2Polyline poly;
-  const auto cloned_poly = absl::WrapUnique(poly.Clone());
+  const auto cloned_poly = shield_absl::WrapUnique(poly.Clone());
   ASSERT_NE(cloned_poly, nullptr);
 }
 
@@ -373,10 +373,10 @@
   string result;
   int n = ints.size();
   for (int i = 0; i + 1 < n; ++i) {
-    absl::StrAppend(&result, ints[i], ",");
+    shield_absl::StrAppend(&result, ints[i], ",");
   }
   if (n > 0) {
-    absl::StrAppend(&result, ints[n - 1]);
+    shield_absl::StrAppend(&result, ints[n - 1]);
   }
   return result;
 }
diff -u -r '--exclude=.git' a/src/s2/s2predicates.h b/src/s2/s2predicates.h
--- a/src/s2/s2predicates.h	2024-07-25 16:38:02.911739538 -0400
+++ b/src/s2/s2predicates.h	2024-07-25 16:20:50.104040492 -0400
@@ -397,7 +397,7 @@
   double det = a_cross_b.DotProd(c);
 
   // Double-check borderline cases in debug mode.
-  S2_DCHECK(!absl::GetFlag(FLAGS_s2debug) || std::fabs(det) <= kMaxDetError ||
+  S2_DCHECK(!shield_absl::GetFlag(FLAGS_s2debug) || std::fabs(det) <= kMaxDetError ||
          std::fabs(det) >= 100 * kMaxDetError ||
          det * ExpensiveSign(a, b, c) > 0);
 
diff -u -r '--exclude=.git' a/src/s2/s2predicates_internal.h b/src/s2/s2predicates_internal.h
--- a/src/s2/s2predicates_internal.h	2024-07-25 16:37:56.203717375 -0400
+++ b/src/s2/s2predicates_internal.h	2024-07-25 16:20:50.120040559 -0400
@@ -65,7 +65,7 @@
 }
 
 inline static long double ToLD(double x) {
-  return absl::implicit_cast<long double>(x);
+  return shield_absl::implicit_cast<long double>(x);
 }
 
 inline static Vector3_xf ToExact(const S2Point& x) {
diff -u -r '--exclude=.git' a/src/s2/s2predicates_test.cc b/src/s2/s2predicates_test.cc
--- a/src/s2/s2predicates_test.cc	2024-07-25 16:38:02.911739538 -0400
+++ b/src/s2/s2predicates_test.cc	2024-07-25 16:20:50.152040690 -0400
@@ -48,7 +48,7 @@
 S2_DEFINE_int32(consistency_iters, 5000,
              "Number of iterations for precision consistency tests");
 
-using absl::string_view;
+using shield_absl::string_view;
 using std::back_inserter;
 using std::min;
 using std::numeric_limits;
@@ -521,10 +521,10 @@
   string result;
   int total = 0;
   for (int i = 0; i < NUM_PRECISIONS; ++i) {
-    absl::StrAppendFormat(&result, "%s=%6d, ", kPrecisionNames[i], counts_[i]);
+    shield_absl::StrAppendFormat(&result, "%s=%6d, ", kPrecisionNames[i], counts_[i]);
     total += counts_[i];
   }
-  absl::StrAppendFormat(&result, "total=%6d", total);
+  shield_absl::StrAppendFormat(&result, "total=%6d", total);
   return result;
 }
 
@@ -710,7 +710,7 @@
       S2Point(1, 0, 0), S2Point(0, -1, 0), S2Point(0, 1, 0));
   auto& rnd = S2Testing::rnd;
   PrecisionStats sin2_stats, cos_stats, minus_sin2_stats;
-  for (int iter = 0; iter < absl::GetFlag(FLAGS_consistency_iters); ++iter) {
+  for (int iter = 0; iter < shield_absl::GetFlag(FLAGS_consistency_iters); ++iter) {
     rnd.Reset(iter + 1);  // Easier to reproduce a specific case.
     S2Point x = ChoosePoint();
     S2Point dir = ChoosePoint();
@@ -750,7 +750,7 @@
  public:
   template <class T>
   static int Triage(const Vector3<T>& x, const Vector3<T>& y, S1ChordAngle r) {
-    return TriageCompareSin2Distance(x, y, absl::implicit_cast<T>(r.length2()));
+    return TriageCompareSin2Distance(x, y, shield_absl::implicit_cast<T>(r.length2()));
   }
 };
 
@@ -758,7 +758,7 @@
  public:
   template <class T>
   static int Triage(const Vector3<T>& x, const Vector3<T>& y, S1ChordAngle r) {
-    return TriageCompareCosDistance(x, y, absl::implicit_cast<T>(r.length2()));
+    return TriageCompareCosDistance(x, y, shield_absl::implicit_cast<T>(r.length2()));
   }
 };
 
@@ -861,7 +861,7 @@
   // comments in the CompareDistances consistency test.
   auto& rnd = S2Testing::rnd;
   PrecisionStats sin2_stats, cos_stats;
-  for (int iter = 0; iter < absl::GetFlag(FLAGS_consistency_iters); ++iter) {
+  for (int iter = 0; iter < shield_absl::GetFlag(FLAGS_consistency_iters); ++iter) {
     rnd.Reset(iter + 1);  // Easier to reproduce a specific case.
     S2Point x = ChoosePoint();
     S2Point dir = ChoosePoint();
@@ -996,7 +996,7 @@
   // See also the comments in the CompareDistances consistency test.
   auto& rnd = S2Testing::rnd;
   PrecisionStats stats;
-  for (int iter = 0; iter < absl::GetFlag(FLAGS_consistency_iters); ++iter) {
+  for (int iter = 0; iter < shield_absl::GetFlag(FLAGS_consistency_iters); ++iter) {
     rnd.Reset(iter + 1);  // Easier to reproduce a specific case.
     S2Point a0 = ChoosePoint();
     S1Angle len = S1Angle::Radians(M_PI * pow(1e-20, rnd.RandDouble()));
@@ -1336,7 +1336,7 @@
   // precision.  See also the comments in the CompareDistances test.
   auto& rnd = S2Testing::rnd;
   PrecisionStats stats;
-  for (int iter = 0; iter < absl::GetFlag(FLAGS_consistency_iters); ++iter) {
+  for (int iter = 0; iter < shield_absl::GetFlag(FLAGS_consistency_iters); ++iter) {
     rnd.Reset(iter + 1);  // Easier to reproduce a specific case.
     S2Point a0 = ChoosePoint();
     S1Angle a_len = S1Angle::Radians(M_PI * pow(1e-20, rnd.RandDouble()));
@@ -1482,7 +1482,7 @@
   // at the next higher level of precision.
   auto& rnd = S2Testing::rnd;
   PrecisionStats stats;
-  for (int iter = 0; iter < absl::GetFlag(FLAGS_consistency_iters); ++iter) {
+  for (int iter = 0; iter < shield_absl::GetFlag(FLAGS_consistency_iters); ++iter) {
     rnd.Reset(iter + 1);  // Easier to reproduce a specific case.
     S2Point x0 = ChoosePoint();
     S2Point x1 = ChoosePoint();
@@ -1731,7 +1731,7 @@
   // is consistent with the answer given at higher levels of precision.
   auto& rnd = S2Testing::rnd;
   PrecisionStats stats;
-  for (int iter = 0; iter < absl::GetFlag(FLAGS_consistency_iters); ++iter) {
+  for (int iter = 0; iter < shield_absl::GetFlag(FLAGS_consistency_iters); ++iter) {
     rnd.Reset(iter + 1);  // Easier to reproduce a specific case.
     S2Point x0 = ChoosePoint();
     S2Point x1 = ChoosePoint();
diff -u -r '--exclude=.git' a/src/s2/s2r2rect_test.cc b/src/s2/s2r2rect_test.cc
--- a/src/s2/s2r2rect_test.cc	2024-07-25 16:38:02.915739550 -0400
+++ b/src/s2/s2r2rect_test.cc	2024-07-25 16:20:50.112040525 -0400
@@ -40,8 +40,8 @@
 #include "s2/s2predicates.h"
 #include "s2/s2testing.h"
 
-using absl::StrCat;
-using absl::string_view;
+using shield_absl::StrCat;
+using shield_absl::string_view;
 
 static void TestIntervalOps(const S2R2Rect& x, const S2R2Rect& y,
                             string_view expected_rexion,
diff -u -r '--exclude=.git' a/src/s2/s2region_coverer.cc b/src/s2/s2region_coverer.cc
--- a/src/s2/s2region_coverer.cc	2024-07-25 16:38:02.915739550 -0400
+++ b/src/s2/s2region_coverer.cc	2024-07-25 16:20:50.112040525 -0400
@@ -483,7 +483,7 @@
 
 void S2RegionCoverer::FloodFill(const S2Region& region, S2CellId start,
                                 vector<S2CellId>* output) {
-  absl::flat_hash_set<S2CellId, S2CellIdHash> all;
+  shield_absl::flat_hash_set<S2CellId, S2CellIdHash> all;
   vector<S2CellId> frontier;
   output->clear();
   all.insert(start);
diff -u -r '--exclude=.git' a/src/s2/s2region_coverer.h b/src/s2/s2region_coverer.h
--- a/src/s2/s2region_coverer.h	2024-07-25 16:38:02.915739550 -0400
+++ b/src/s2/s2region_coverer.h	2024-07-25 16:20:50.148040674 -0400
@@ -262,7 +262,7 @@
     Candidate(const S2Cell& cell, const std::size_t max_children)
         : cell(cell), is_terminal(max_children == 0) {
       std::fill_n(&children[0], max_children,
-                  absl::implicit_cast<Candidate*>(nullptr));
+                  shield_absl::implicit_cast<Candidate*>(nullptr));
     }
 
     // Default destructor is fine; Candidate* is trivially destructible.
diff -u -r '--exclude=.git' a/src/s2/s2region_coverer_test.cc b/src/s2/s2region_coverer_test.cc
--- a/src/s2/s2region_coverer_test.cc	2024-07-25 16:38:02.915739550 -0400
+++ b/src/s2/s2region_coverer_test.cc	2024-07-25 16:20:50.112040525 -0400
@@ -51,7 +51,7 @@
 #include "s2/s2region.h"
 #include "s2/s2testing.h"
 
-using absl::StrCat;
+using shield_absl::StrCat;
 using std::max;
 using std::min;
 using std::priority_queue;
@@ -67,8 +67,8 @@
 
 namespace {
 
-using absl::flat_hash_map;
-using absl::StrCat;
+using shield_absl::flat_hash_map;
+using shield_absl::StrCat;
 using std::max;
 using std::min;
 using std::priority_queue;
@@ -215,7 +215,7 @@
   for (int method = 0; method < kNumMethods; ++method) {
     min_ratio[method] = 1e20;
   }
-  for (int i = 0; i < absl::GetFlag(FLAGS_iters); ++i) {
+  for (int i = 0; i < shield_absl::GetFlag(FLAGS_iters); ++i) {
     // Choose the log of the cap area to be uniformly distributed over
     // the allowable range.  Don't try to approximate regions that are so
     // small they can't use the given maximum number of cells efficiently.
@@ -266,9 +266,9 @@
     printf("\nMax cells %d, method %d:\n", max_cells, method);
     printf(
         "  Average cells: %.4f\n",
-        cell_total[method] / static_cast<double>(absl::GetFlag(FLAGS_iters)));
+        cell_total[method] / static_cast<double>(shield_absl::GetFlag(FLAGS_iters)));
     printf("  Average area ratio: %.4f\n",
-           ratio_total[method] / absl::GetFlag(FLAGS_iters));
+           ratio_total[method] / shield_absl::GetFlag(FLAGS_iters));
     vector<double>& mratios = ratios[method];
     std::sort(mratios.begin(), mratios.end());
     printf("  Median ratio: %.4f\n", mratios[mratios.size() / 2]);
@@ -277,10 +277,10 @@
     if (kNumMethods > 1) {
       printf("  Cell winner probability: %.4f\n",
              cell_winner_tally[method] /
-                 static_cast<double>(absl::GetFlag(FLAGS_iters)));
+                 static_cast<double>(shield_absl::GetFlag(FLAGS_iters)));
       printf("  Area winner probability: %.4f\n",
              area_winner_tally[method] /
-                 static_cast<double>(absl::GetFlag(FLAGS_iters)));
+                 static_cast<double>(shield_absl::GetFlag(FLAGS_iters)));
     }
     printf("  Caps with the worst approximation ratios:\n");
     for (; !worst_caps[method].empty(); worst_caps[method].pop()) {
@@ -297,9 +297,9 @@
 
 TEST(S2RegionCoverer, Accuracy) {
   for (auto max_cells_str :
-       absl::StrSplit(absl::GetFlag(FLAGS_max_cells), ',', absl::SkipEmpty())) {
+       shield_absl::StrSplit(shield_absl::GetFlag(FLAGS_max_cells), ',', shield_absl::SkipEmpty())) {
     int max_cells;
-    S2_CHECK(absl::SimpleAtoi(max_cells_str, &max_cells));
+    S2_CHECK(shield_absl::SimpleAtoi(max_cells_str, &max_cells));
     TestAccuracy(max_cells);
   }
 }
diff -u -r '--exclude=.git' a/src/s2/s2region_term_indexer.cc b/src/s2/s2region_term_indexer.cc
--- a/src/s2/s2region_term_indexer.cc	2024-07-25 16:38:02.915739550 -0400
+++ b/src/s2/s2region_term_indexer.cc	2024-07-25 16:20:50.152040690 -0400
@@ -88,7 +88,7 @@
 #include "s2/s2region.h"
 #include "s2/s2region_coverer.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using std::string;
 using std::vector;
 
@@ -101,7 +101,7 @@
 }
 
 void S2RegionTermIndexer::Options::set_marker_character(char ch) {
-  S2_DCHECK(!absl::ascii_isalnum(ch));
+  S2_DCHECK(!shield_absl::ascii_isalnum(ch));
   marker_ = ch;
 }
 
@@ -121,9 +121,9 @@
   // There are generally more ancestor terms than covering terms, so we add
   // the extra "marker" character to the covering terms to distinguish them.
   if (term_type == TermType::ANCESTOR) {
-    return absl::StrCat(prefix, id.ToToken());
+    return shield_absl::StrCat(prefix, id.ToToken());
   } else {
-    return absl::StrCat(prefix, options_.marker(), id.ToToken());
+    return shield_absl::StrCat(prefix, options_.marker(), id.ToToken());
   }
 }
 
diff -u -r '--exclude=.git' a/src/s2/s2region_term_indexer.h b/src/s2/s2region_term_indexer.h
--- a/src/s2/s2region_term_indexer.h	2024-07-25 16:38:02.915739550 -0400
+++ b/src/s2/s2region_term_indexer.h	2024-07-25 16:20:50.124040575 -0400
@@ -218,7 +218,7 @@
     //
     // REQUIRES: "ch" is non-alphanumeric.
     // DEFAULT: '$'
-    absl::string_view marker() const { return absl::string_view(&marker_, 1); }
+    shield_absl::string_view marker() const { return shield_absl::string_view(&marker_, 1); }
     char marker_character() const { return marker_; }
     void set_marker_character(char ch);
 
@@ -254,7 +254,7 @@
   // parking lots, etc).  The prefix should be kept short since it is
   // prepended to every term.
   std::vector<std::string> GetIndexTerms(const S2Region& region,
-                                         absl::string_view prefix);
+                                         shield_absl::string_view prefix);
 
   // Converts a given query region into a set of terms.  If you compute the
   // union of all the documents associated with these terms, the result will
@@ -262,7 +262,7 @@
   //
   // "prefix" should match the corresponding value used when indexing.
   std::vector<std::string> GetQueryTerms(const S2Region& region,
-                                         absl::string_view prefix);
+                                         shield_absl::string_view prefix);
 
   // Convenience methods that accept an S2Point rather than S2Region.  (These
   // methods are also faster.)
@@ -270,9 +270,9 @@
   // Note that you can index an S2LatLng by converting it to an S2Point first:
   //     auto terms = GetIndexTerms(S2Point(latlng), ...);
   std::vector<std::string> GetIndexTerms(const S2Point& point,
-                                         absl::string_view prefix);
+                                         shield_absl::string_view prefix);
   std::vector<std::string> GetQueryTerms(const S2Point& point,
-                                         absl::string_view prefix);
+                                         shield_absl::string_view prefix);
 
   // Low-level methods that accept an S2CellUnion covering of the region to be
   // indexed or queried.
@@ -285,15 +285,15 @@
   // type of S2Region), or "canonicalize" the covering first by calling
   // S2RegionCoverer::CanonicalizeCovering() with the same options.
   std::vector<std::string> GetIndexTermsForCanonicalCovering(
-      const S2CellUnion& covering, absl::string_view prefix);
+      const S2CellUnion& covering, shield_absl::string_view prefix);
   std::vector<std::string> GetQueryTermsForCanonicalCovering(
-      const S2CellUnion& covering, absl::string_view prefix);
+      const S2CellUnion& covering, shield_absl::string_view prefix);
 
  private:
   enum TermType { ANCESTOR, COVERING };
 
   std::string GetTerm(TermType term_type, const S2CellId id,
-                      absl::string_view prefix) const;
+                      shield_absl::string_view prefix) const;
 
   Options options_;
   S2RegionCoverer coverer_;
diff -u -r '--exclude=.git' a/src/s2/s2region_term_indexer_test.cc b/src/s2/s2region_term_indexer_test.cc
--- a/src/s2/s2region_term_indexer_test.cc	2024-07-25 16:38:02.915739550 -0400
+++ b/src/s2/s2region_term_indexer_test.cc	2024-07-25 16:20:50.132040607 -0400
@@ -58,9 +58,9 @@
   S2RegionCoverer coverer(options);
   vector<S2Cap> caps;
   vector<S2CellUnion> coverings;
-  absl::flat_hash_map<string, vector<int>> index;
+  shield_absl::flat_hash_map<string, vector<int>> index;
   int index_terms = 0, query_terms = 0;
-  for (int i = 0; i < absl::GetFlag(FLAGS_iters); ++i) {
+  for (int i = 0; i < shield_absl::GetFlag(FLAGS_iters); ++i) {
     // Choose the region to be indexed: either a single point or a cap
     // of random size (up to a full sphere).
     S2Cap cap;
@@ -81,7 +81,7 @@
     }
     index_terms += terms.size();
   }
-  for (int i = 0; i < absl::GetFlag(FLAGS_iters); ++i) {
+  for (int i = 0; i < shield_absl::GetFlag(FLAGS_iters); ++i) {
     // Choose the region to be queried: either a random point or a cap of
     // random size.
     S2Cap cap;
@@ -97,7 +97,7 @@
     }
     // Compute the expected results of the S2Cell query by brute force.
     S2CellUnion covering = coverer.GetCovering(cap);
-    absl::flat_hash_set<int> expected, actual;
+    shield_absl::flat_hash_set<int> expected, actual;
     for (int j = 0; j < caps.size(); ++j) {
       if (covering.Intersects(coverings[j])) {
         expected.insert(j);
@@ -110,8 +110,8 @@
     query_terms += terms.size();
   }
   printf("Index terms/doc: %.2f,  Query terms/doc: %.2f\n",
-         static_cast<double>(index_terms) / absl::GetFlag(FLAGS_iters),
-         static_cast<double>(query_terms) / absl::GetFlag(FLAGS_iters));
+         static_cast<double>(index_terms) / shield_absl::GetFlag(FLAGS_iters),
+         static_cast<double>(query_terms) / shield_absl::GetFlag(FLAGS_iters));
 }
 
 // We run one test case for each combination of space vs. time optimization,
diff -u -r '--exclude=.git' a/src/s2/s2region_test.cc b/src/s2/s2region_test.cc
--- a/src/s2/s2region_test.cc	2024-07-25 16:37:56.203717375 -0400
+++ b/src/s2/s2region_test.cc	2024-07-25 16:20:50.152040690 -0400
@@ -41,7 +41,7 @@
 #include "s2/s2text_format.h"
 #include "s2/util/coding/coder.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using std::string;
 using std::unique_ptr;
 using std::vector;
diff -u -r '--exclude=.git' a/src/s2/s2shape_index_buffered_region_test.cc b/src/s2/s2shape_index_buffered_region_test.cc
--- a/src/s2/s2shape_index_buffered_region_test.cc	2024-07-25 16:38:02.915739550 -0400
+++ b/src/s2/s2shape_index_buffered_region_test.cc	2024-07-25 16:20:50.140040641 -0400
@@ -40,7 +40,7 @@
 #include "s2/s2testing.h"
 #include "s2/s2text_format.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using s2textformat::MakeIndexOrDie;
 using s2textformat::MakePointOrDie;
 using std::cout;
diff -u -r '--exclude=.git' a/src/s2/s2shape_index.h b/src/s2/s2shape_index.h
--- a/src/s2/s2shape_index.h	2024-07-25 16:38:02.915739550 -0400
+++ b/src/s2/s2shape_index.h	2024-07-25 16:20:50.116040541 -0400
@@ -154,7 +154,7 @@
   const S2ClippedShape* find_clipped(int shape_id) const;
 
   // Returns a read-only span over the clipped shapes in the cell.
-  absl::Span<const S2ClippedShape> clipped_shapes() const {
+  shield_absl::Span<const S2ClippedShape> clipped_shapes() const {
     if (!shapes_.empty()) {
       return {shapes_.begin(), shapes_.size()};
     }
@@ -305,7 +305,7 @@
   //       "We require an S2ShapeIndex.");
   //   }
   template <typename T>
-  using ImplementedBy = std::is_convertible<absl::decay_t<T>*, S2ShapeIndex*>;
+  using ImplementedBy = std::is_convertible<shield_absl::decay_t<T>*, S2ShapeIndex*>;
 
   virtual ~S2ShapeIndex() = default;
 
diff -u -r '--exclude=.git' a/src/s2/s2shape_index_region.h b/src/s2/s2shape_index_region.h
--- a/src/s2/s2shape_index_region.h	2024-07-25 16:38:02.915739550 -0400
+++ b/src/s2/s2shape_index_region.h	2024-07-25 16:20:50.144040657 -0400
@@ -358,7 +358,7 @@
       // it contains the center of all cells, and it has no edges in any cell.
       // It is easier to keep track of whether a shape does *not* contain the
       // target cell because boolean values default to false.
-      absl::flat_hash_map<int, bool> shape_not_contains;
+      shield_absl::flat_hash_map<int, bool> shape_not_contains;
       for (const S2CellId max = target.id().range_max();
            !iter_.done() && iter_.id() <= max; iter_.Next()) {
         const S2ShapeIndexCell& cell = iter_.cell();
diff -u -r '--exclude=.git' a/src/s2/s2shape_index_region_test.cc b/src/s2/s2shape_index_region_test.cc
--- a/src/s2/s2shape_index_region_test.cc	2024-07-25 16:38:02.915739550 -0400
+++ b/src/s2/s2shape_index_region_test.cc	2024-07-25 16:20:50.144040657 -0400
@@ -45,8 +45,8 @@
 #include "s2/s2testing.h"
 #include "s2/s2wrapped_shape.h"
 
-using absl::flat_hash_map;
-using absl::string_view;
+using shield_absl::flat_hash_map;
+using shield_absl::string_view;
 using std::make_unique;
 using std::string;
 using std::unique_ptr;
diff -u -r '--exclude=.git' a/src/s2/s2shape_nesting_query.cc b/src/s2/s2shape_nesting_query.cc
--- a/src/s2/s2shape_nesting_query.cc	2024-07-25 16:38:02.915739550 -0400
+++ b/src/s2/s2shape_nesting_query.cc	2024-07-25 16:20:50.148040674 -0400
@@ -106,8 +106,8 @@
   }
 
   // Sets to track possible parents and possible children for each chain.
-  absl::FixedArray<Bitmap64> parents(num_chains, Bitmap64(num_chains, false));
-  absl::FixedArray<Bitmap64> children(num_chains, Bitmap64(num_chains, false));
+  shield_absl::FixedArray<Bitmap64> parents(num_chains, Bitmap64(num_chains, false));
+  shield_absl::FixedArray<Bitmap64> children(num_chains, Bitmap64(num_chains, false));
 
   // We'll compute edge crossings along a line segment from the datum shell to a
   // random point on the other chains.  This choice is arbitrary, so we'll use
@@ -187,7 +187,7 @@
   if (S2_VLOG_IS_ON(2)) {
     S2_LOG(INFO) << "Current parent set";
     for (int chain = 0; chain < num_chains; ++chain) {
-      S2_LOG(INFO) << "  " << absl::StrFormat("%2d", chain) << ": "
+      S2_LOG(INFO) << "  " << shield_absl::StrFormat("%2d", chain) << ": "
                 << parents[chain].ToString(8);
     }
   }
@@ -221,7 +221,7 @@
     if (S2_VLOG_IS_ON(2)) {
       S2_LOG(INFO) << "  Parent set now:";
       for (int chain = 0; chain < num_chains; ++chain) {
-        S2_LOG(INFO) << "  " << absl::StrFormat("%2d", chain) << ": "
+        S2_LOG(INFO) << "  " << shield_absl::StrFormat("%2d", chain) << ": "
                   << parents[chain].ToString(8);
       }
     }
diff -u -r '--exclude=.git' a/src/s2/s2shape_nesting_query.h b/src/s2/s2shape_nesting_query.h
--- a/src/s2/s2shape_nesting_query.h	2024-07-25 16:38:02.915739550 -0400
+++ b/src/s2/s2shape_nesting_query.h	2024-07-25 16:20:50.124040575 -0400
@@ -99,7 +99,7 @@
   class ChainRelation {
    public:
     // Builds a `ChainRelation` that's a shell with given holes.
-    static ChainRelation MakeShell(absl::Span<const int32> holes = {}) {
+    static ChainRelation MakeShell(shield_absl::Span<const int32> holes = {}) {
       ChainRelation relation;
       for (int32 chain : holes) {
         relation.AddHole(chain);
@@ -130,15 +130,15 @@
     int num_holes() const { return holes_.size(); }
 
     // Returns a read only view over the hole ids for the associated chain.
-    absl::Span<const int32> holes() const {
-      return absl::Span<const int32>(holes_);
+    shield_absl::Span<const int32> holes() const {
+      return shield_absl::Span<const int32>(holes_);
     }
 
    private:
     // So that the query can add Holes to the chain before returning it.
     friend class S2ShapeNestingQuery;
 
-    // `absl::InlinedVector` doesn't round up its inline capacity even if it
+    // `shield_absl::InlinedVector` doesn't round up its inline capacity even if it
     // wouldn't make the object any bigger.  It stores a pointer and a capacity
     // with the inlined data in a union:
     //
@@ -149,7 +149,7 @@
     // sense for us to scale the number of reserved elements to take advantage
     // of this.  On 64-bit systems we can store 4 int32s and on 32-bit we can
     // store 2.
-    absl::InlinedVector<int32, 2 * sizeof(void*) / sizeof(int32)> holes_;
+    shield_absl::InlinedVector<int32, 2 * sizeof(void*) / sizeof(int32)> holes_;
     int32 parent_;
 
     // Adds the given id as a hole of this chain.  The `ParentId` of the hole
diff -u -r '--exclude=.git' a/src/s2/s2shape_nesting_query_test.cc b/src/s2/s2shape_nesting_query_test.cc
--- a/src/s2/s2shape_nesting_query_test.cc	2024-07-25 16:38:02.915739550 -0400
+++ b/src/s2/s2shape_nesting_query_test.cc	2024-07-25 16:20:50.124040575 -0400
@@ -37,7 +37,7 @@
 #include "s2/s2testing.h"
 #include "s2/util/gtl/legacy_random_shuffle.h"
 
-using absl::Span;
+using shield_absl::Span;
 using std::make_unique;
 using std::unique_ptr;
 using std::vector;
@@ -397,7 +397,7 @@
   }
 
   if (test_case.shuffle) {
-    S2Testing::rnd.Reset(absl::GetFlag(FLAGS_s2_random_seed));
+    S2Testing::rnd.Reset(shield_absl::GetFlag(FLAGS_s2_random_seed));
     gtl::legacy_random_shuffle(rings.begin() + 1, rings.end(), S2Testing::rnd);
   }
 
@@ -447,7 +447,7 @@
     if (relations[chain].is_hole()) {
       num_holes++;
       int parent = relations[chain].parent_id();
-      absl::Span<const int32> holes = relations[parent].holes();
+      shield_absl::Span<const int32> holes = relations[parent].holes();
       EXPECT_NE(std::find(holes.begin(), holes.end(), chain), holes.end());
     }
   }
diff -u -r '--exclude=.git' a/src/s2/s2shapeutil_build_polygon_boundaries.cc b/src/s2/s2shapeutil_build_polygon_boundaries.cc
--- a/src/s2/s2shapeutil_build_polygon_boundaries.cc	2024-07-25 16:38:02.915739550 -0400
+++ b/src/s2/s2shapeutil_build_polygon_boundaries.cc	2024-07-25 16:20:50.156040706 -0400
@@ -28,7 +28,7 @@
 #include "s2/s2shape.h"
 #include "s2/s2shapeutil_contains_brute_force.h"
 
-using absl::WrapUnique;
+using shield_absl::WrapUnique;
 using std::vector;
 
 namespace s2shapeutil {
@@ -88,7 +88,7 @@
   }
   // Assign each outer loop to the component whose depth is one less.
   // Components at depth 0 become a single face.
-  absl::btree_map<S2Shape*, vector<S2Shape*>> children;
+  shield_absl::btree_map<S2Shape*, vector<S2Shape*>> children;
   for (size_t i = 0; i < outer_loops.size(); ++i) {
     S2Shape* ancestor = nullptr;
     size_t depth = ancestors[i].size();
diff -u -r '--exclude=.git' a/src/s2/s2shapeutil_build_polygon_boundaries_test.cc b/src/s2/s2shapeutil_build_polygon_boundaries_test.cc
--- a/src/s2/s2shapeutil_build_polygon_boundaries_test.cc	2024-07-25 16:37:56.207717387 -0400
+++ b/src/s2/s2shapeutil_build_polygon_boundaries_test.cc	2024-07-25 16:20:50.140040641 -0400
@@ -28,7 +28,7 @@
 #include "s2/s2shape.h"
 #include "s2/s2text_format.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using std::string;
 using std::vector;
 
diff -u -r '--exclude=.git' a/src/s2/s2shapeutil_coding_test.cc b/src/s2/s2shapeutil_coding_test.cc
--- a/src/s2/s2shapeutil_coding_test.cc	2024-07-25 16:37:56.207717387 -0400
+++ b/src/s2/s2shapeutil_coding_test.cc	2024-07-25 16:20:50.136040625 -0400
@@ -71,7 +71,7 @@
   auto polyline = s2textformat::MakePolylineOrDie("1:1, 1:2, 1:3");
   index->Add(make_unique<S2LaxPolylineShape>(*polyline));
   index->Add(make_unique<S2LaxPolygonShape>(*polygon));
-  string bytes = absl::HexStringToBytes(
+  string bytes = shield_absl::HexStringToBytes(
       "2932007C00E4002E0192010310000000000000F03F000000000000000000000000000000"
       "008AAFF597C0FEEF3F1EDD892B0BDF913F00000000000000000418B4825F3C81FDEF3F27"
       "DCF7C958DE913F1EDD892B0BDF913FD44A8442C3F9EF3FCE5B5A6FA6DDA13F1EDD892B0B"
diff -u -r '--exclude=.git' a/src/s2/s2shapeutil_edge_iterator.cc b/src/s2/s2shapeutil_edge_iterator.cc
--- a/src/s2/s2shapeutil_edge_iterator.cc	2024-07-25 16:38:02.915739550 -0400
+++ b/src/s2/s2shapeutil_edge_iterator.cc	2024-07-25 16:20:50.132040607 -0400
@@ -45,7 +45,7 @@
 }
 
 string EdgeIterator::DebugString() const {
-  return absl::StrCat("(shape=", shape_id_, ", edge=", edge_id_, ")");
+  return shield_absl::StrCat("(shape=", shape_id_, ", edge=", edge_id_, ")");
 }
 
 }  // namespace s2shapeutil
diff -u -r '--exclude=.git' a/src/s2/s2shapeutil_visit_crossing_edge_pairs.cc b/src/s2/s2shapeutil_visit_crossing_edge_pairs.cc
--- a/src/s2/s2shapeutil_visit_crossing_edge_pairs.cc	2024-07-25 16:38:02.919739565 -0400
+++ b/src/s2/s2shapeutil_visit_crossing_edge_pairs.cc	2024-07-25 16:20:50.152040690 -0400
@@ -43,7 +43,7 @@
 
 // Ensure that we don't usually need to allocate memory when collecting the
 // edges in an S2ShapeIndex cell (which by default have about 10 edges).
-using ShapeEdgeVector = absl::InlinedVector<ShapeEdge, 16>;
+using ShapeEdgeVector = shield_absl::InlinedVector<ShapeEdge, 16>;
 
 // Appends all edges in the given S2ShapeIndexCell to the given vector.
 static void AppendShapeEdges(const S2ShapeIndex& index,
@@ -375,7 +375,7 @@
 // Helper function that formats a loop error message.  If the loop belongs to
 // a multi-loop polygon, adds a prefix indicating which loop is affected.
 static void InitLoopError(S2Error::Code code,
-                          const absl::FormatSpec<int, int>& format,
+                          const shield_absl::FormatSpec<int, int>& format,
                           ChainPosition ap, ChainPosition bp,
                           bool is_polygon, S2Error* error) {
   error->Init(code, format, ap.offset, bp.offset);
diff -u -r '--exclude=.git' a/src/s2/s2shapeutil_visit_crossing_edge_pairs_test.cc b/src/s2/s2shapeutil_visit_crossing_edge_pairs_test.cc
--- a/src/s2/s2shapeutil_visit_crossing_edge_pairs_test.cc	2024-07-25 16:38:02.919739565 -0400
+++ b/src/s2/s2shapeutil_visit_crossing_edge_pairs_test.cc	2024-07-25 16:20:50.116040541 -0400
@@ -46,7 +46,7 @@
 #include "s2/s2shapeutil_shape_edge_id.h"
 #include "s2/s2text_format.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using std::make_unique;
 using std::string;
 using std::unique_ptr;
diff -u -r '--exclude=.git' a/src/s2/s2testing.cc b/src/s2/s2testing.cc
--- a/src/s2/s2testing.cc	2024-07-25 16:38:02.919739565 -0400
+++ b/src/s2/s2testing.cc	2024-07-25 16:20:50.124040575 -0400
@@ -55,7 +55,7 @@
 #include "s2/s2text_format.h"
 #include "s2/util/math/matrix3x3.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using std::make_unique;
 using std::max;
 using std::string;
@@ -178,7 +178,7 @@
 // The Dump*() functions are for use within a debugger.  They are similar to
 // the corresponding s2textformat::ToString() functions except that they
 // prefix their output with a label and they don't require default arguments
-// or constructing absl::Span objects (which gdb doesn't know how to do).
+// or constructing shield_absl::Span objects (which gdb doesn't know how to do).
 void Dump(const S2Point& p) {
   std::cout << "S2Point: " << s2textformat::ToString(p) << std::endl;
 }
diff -u -r '--exclude=.git' a/src/s2/s2testing.h b/src/s2/s2testing.h
--- a/src/s2/s2testing.h	2024-07-25 16:38:02.919739565 -0400
+++ b/src/s2/s2testing.h	2024-07-25 16:20:50.124040575 -0400
@@ -326,7 +326,7 @@
                     int max_size, Distance max_distance,
                     typename Distance::Delta max_error,
                     typename Distance::Delta max_pruning_error,
-                    absl::string_view label) {
+                    shield_absl::string_view label) {
   using Result = std::pair<Distance, Id>;
   // Results should be sorted by distance, but not necessarily then by Id.
   EXPECT_TRUE(std::is_sorted(x.begin(), x.end(),
diff -u -r '--exclude=.git' a/src/s2/s2text_format.cc b/src/s2/s2text_format.cc
--- a/src/s2/s2text_format.cc	2024-07-25 16:38:02.919739565 -0400
+++ b/src/s2/s2text_format.cc	2024-07-25 16:20:50.120040559 -0400
@@ -45,8 +45,8 @@
 #include "s2/s2shape.h"
 #include "s2/s2shape_index.h"
 
-using absl::Span;
-using absl::string_view;
+using shield_absl::Span;
+using shield_absl::string_view;
 using std::make_unique;
 using std::pair;
 using std::string;
@@ -57,9 +57,9 @@
 
 static vector<string_view> SplitString(string_view str, char separator) {
   vector<string_view> result =
-      absl::StrSplit(str, separator, absl::SkipWhitespace());
+      shield_absl::StrSplit(str, separator, shield_absl::SkipWhitespace());
   for (auto& e : result) {
-    e = absl::StripAsciiWhitespace(e);
+    e = shield_absl::StripAsciiWhitespace(e);
   }
   return result;
 }
@@ -72,12 +72,12 @@
 
 bool ParseLatLngs(string_view str, vector<S2LatLng>* latlngs) {
   for (const string_view lat_lng_str :
-       absl::StrSplit(str, ',', absl::SkipEmpty())) {
-    const vector<string_view> lat_lng = absl::StrSplit(lat_lng_str, ':');
+       shield_absl::StrSplit(str, ',', shield_absl::SkipEmpty())) {
+    const vector<string_view> lat_lng = shield_absl::StrSplit(lat_lng_str, ':');
     if (lat_lng.size() != 2) return false;
     double lat, lng;
-    if (!absl::SimpleAtod(lat_lng[0], &lat)) return false;
-    if (!absl::SimpleAtod(lat_lng[1], &lng)) return false;
+    if (!shield_absl::SimpleAtod(lat_lng[0], &lat)) return false;
+    if (!shield_absl::SimpleAtod(lat_lng[1], &lng)) return false;
     latlngs->push_back(S2LatLng::FromDegrees(lat, lng));
   }
   return true;
@@ -291,7 +291,7 @@
 }
 
 bool MakeIndex(string_view str, unique_ptr<MutableS2ShapeIndex>* index) {
-  vector<string_view> strs = absl::StrSplit(str, '#');
+  vector<string_view> strs = shield_absl::StrSplit(str, '#');
   S2_DCHECK_EQ(3, strs.size()) << "Must contain two # characters: " << str;
 
   vector<S2Point> points;
@@ -319,10 +319,10 @@
 static void AppendVertex(const S2LatLng& ll, string* out,
                          bool roundtrip_precision = false) {
   if (roundtrip_precision) {
-    absl::StrAppendFormat(out, "%.17g:%.17g", ll.lat().degrees(),
+    shield_absl::StrAppendFormat(out, "%.17g:%.17g", ll.lat().degrees(),
                           ll.lng().degrees());
   } else {
-    absl::StrAppendFormat(out, "%.15g:%.15g", ll.lat().degrees(),
+    shield_absl::StrAppendFormat(out, "%.15g:%.15g", ll.lat().degrees(),
                           ll.lng().degrees());
   }
 }
@@ -402,7 +402,7 @@
   }
   string out;
   for (int i = 0; i < polygon.num_loops(); ++i) {
-    if (i > 0) absl::StrAppend(&out, loop_separator);
+    if (i > 0) shield_absl::StrAppend(&out, loop_separator);
     const S2Loop& loop = *polygon.loop(i);
     AppendVertices(&loop.vertex(0), loop.num_vertices(), &out);
   }
@@ -460,7 +460,7 @@
 string ToString(const S2LaxPolygonShape& polygon, string_view loop_separator) {
   string out;
   for (int i = 0; i < polygon.num_loops(); ++i) {
-    if (i > 0) absl::StrAppend(&out, loop_separator);
+    if (i > 0) shield_absl::StrAppend(&out, loop_separator);
     int n = polygon.num_loop_vertices(i);
     if (n == 0) {
       out += "full";
diff -u -r '--exclude=.git' a/src/s2/s2text_format.h b/src/s2/s2text_format.h
--- a/src/s2/s2text_format.h	2024-07-25 16:38:02.919739565 -0400
+++ b/src/s2/s2text_format.h	2024-07-25 16:20:50.136040625 -0400
@@ -59,14 +59,14 @@
 // Returns an S2Point corresponding to the given a latitude-longitude
 // coordinate in degrees.  Example of the input format:
 //     "-20:150"
-S2Point MakePointOrDie(absl::string_view str);
+S2Point MakePointOrDie(shield_absl::string_view str);
 
 // As above, but do not S2_CHECK-fail on invalid input. Returns true if conversion
 // is successful.
-ABSL_MUST_USE_RESULT bool MakePoint(absl::string_view str, S2Point* point);
+ABSL_MUST_USE_RESULT bool MakePoint(shield_absl::string_view str, S2Point* point);
 
 ABSL_DEPRECATED("Inline the implementation")
-inline S2Point MakePoint(absl::string_view str) { return MakePointOrDie(str); }
+inline S2Point MakePoint(shield_absl::string_view str) { return MakePointOrDie(str); }
 
 // Parses a string of one or more latitude-longitude coordinates in degrees,
 // and return the corresponding vector of S2LatLng points.
@@ -74,44 +74,44 @@
 //     ""                            // no points
 //     "-20:150"                     // one point
 //     "-20:150, -20:151, -19:150"   // three points
-std::vector<S2LatLng> ParseLatLngsOrDie(absl::string_view str);
+std::vector<S2LatLng> ParseLatLngsOrDie(shield_absl::string_view str);
 
 // As above, but does not S2_CHECK-fail on invalid input. Returns true if
 // conversion is successful.
-ABSL_MUST_USE_RESULT bool ParseLatLngs(absl::string_view str,
+ABSL_MUST_USE_RESULT bool ParseLatLngs(shield_absl::string_view str,
                                 std::vector<S2LatLng>* latlngs);
 
 ABSL_DEPRECATED("Inline the implementation")
-inline std::vector<S2LatLng> ParseLatLngs(absl::string_view str) {
+inline std::vector<S2LatLng> ParseLatLngs(shield_absl::string_view str) {
   return ParseLatLngsOrDie(str);
 }
 
 // Parses a string in the same format as ParseLatLngs, and return the
 // corresponding vector of S2Point values.
-std::vector<S2Point> ParsePointsOrDie(absl::string_view str);
+std::vector<S2Point> ParsePointsOrDie(shield_absl::string_view str);
 
 // As above, but does not S2_CHECK-fail on invalid input. Returns true if
 // conversion is successful.
-ABSL_MUST_USE_RESULT bool ParsePoints(absl::string_view str,
+ABSL_MUST_USE_RESULT bool ParsePoints(shield_absl::string_view str,
                                std::vector<S2Point>* vertices);
 
 // Given a string in the same format as ParseLatLngs, returns a single S2LatLng.
-S2LatLng MakeLatLngOrDie(absl::string_view str);
+S2LatLng MakeLatLngOrDie(shield_absl::string_view str);
 
 // As above, but does not S2_CHECK-fail on invalid input. Returns true if
 // conversion is successful.
-ABSL_MUST_USE_RESULT bool MakeLatLng(absl::string_view str, S2LatLng* latlng);
+ABSL_MUST_USE_RESULT bool MakeLatLng(shield_absl::string_view str, S2LatLng* latlng);
 
 // Given a string in the same format as ParseLatLngs, returns the minimal
 // bounding S2LatLngRect that contains the coordinates.
-S2LatLngRect MakeLatLngRectOrDie(absl::string_view str);
+S2LatLngRect MakeLatLngRectOrDie(shield_absl::string_view str);
 
 // As above, but does not S2_CHECK-fail on invalid input. Returns true if
 // conversion is successful.
-ABSL_MUST_USE_RESULT bool MakeLatLngRect(absl::string_view str, S2LatLngRect* rect);
+ABSL_MUST_USE_RESULT bool MakeLatLngRect(shield_absl::string_view str, S2LatLngRect* rect);
 
 ABSL_DEPRECATED("Inline the implementation")
-inline S2LatLngRect MakeLatLngRect(absl::string_view str) {
+inline S2LatLngRect MakeLatLngRect(shield_absl::string_view str) {
   return MakeLatLngRectOrDie(str);
 }
 
@@ -124,54 +124,54 @@
 // S2CellId::FromFace(3).child(0).child(2).
 //
 // This function is a wrapper for S2CellId::FromDebugString().
-S2CellId MakeCellIdOrDie(absl::string_view str);
+S2CellId MakeCellIdOrDie(shield_absl::string_view str);
 
 // As above, but does not S2_CHECK-fail on invalid input. Returns true if
 // conversion is successful.
-ABSL_MUST_USE_RESULT bool MakeCellId(absl::string_view str, S2CellId* cell_id);
+ABSL_MUST_USE_RESULT bool MakeCellId(shield_absl::string_view str, S2CellId* cell_id);
 
 // Parses a comma-separated list of S2CellIds in the format above, and returns
 // the corresponding S2CellUnion.  (Note that S2CellUnions are automatically
 // normalized by sorting, removing duplicates, and replacing groups of 4 child
 // cells by their parent cell.)
-S2CellUnion MakeCellUnionOrDie(absl::string_view str);
+S2CellUnion MakeCellUnionOrDie(shield_absl::string_view str);
 
 // As above, but does not S2_CHECK-fail on invalid input. Returns true if
 // conversion is successful.
-ABSL_MUST_USE_RESULT bool MakeCellUnion(absl::string_view str,
+ABSL_MUST_USE_RESULT bool MakeCellUnion(shield_absl::string_view str,
                                  S2CellUnion* cell_union);
 
 // Given a string of latitude-longitude coordinates in degrees,
 // returns a newly allocated loop.  Example of the input format:
 //     "-20:150, 10:-120, 0.123:-170.652"
 // The strings "empty" or "full" create an empty or full loop respectively.
-std::unique_ptr<S2Loop> MakeLoopOrDie(absl::string_view str,
+std::unique_ptr<S2Loop> MakeLoopOrDie(shield_absl::string_view str,
                                       S2Debug debug_override = S2Debug::ALLOW);
 
 // As above, but does not S2_CHECK-fail on invalid input. Returns true if
 // conversion is successful.
-ABSL_MUST_USE_RESULT bool MakeLoop(absl::string_view str,
+ABSL_MUST_USE_RESULT bool MakeLoop(shield_absl::string_view str,
                             std::unique_ptr<S2Loop>* loop,
                             S2Debug debug_override = S2Debug::ALLOW);
 
 // Similar to MakeLoop(), but returns an S2Polyline rather than an S2Loop.
 std::unique_ptr<S2Polyline> MakePolylineOrDie(
-    absl::string_view str,
+    shield_absl::string_view str,
     S2Debug debug_override = S2Debug::ALLOW);
 
 // As above, but does not S2_CHECK-fail on invalid input. Returns true if
 // conversion is successful.
-ABSL_MUST_USE_RESULT bool MakePolyline(absl::string_view str,
+ABSL_MUST_USE_RESULT bool MakePolyline(shield_absl::string_view str,
                                 std::unique_ptr<S2Polyline>* polyline,
                                 S2Debug debug_override = S2Debug::ALLOW);
 
 // Like MakePolyline, but returns an S2LaxPolylineShape instead.
-std::unique_ptr<S2LaxPolylineShape> MakeLaxPolylineOrDie(absl::string_view str);
+std::unique_ptr<S2LaxPolylineShape> MakeLaxPolylineOrDie(shield_absl::string_view str);
 
 // As above, but does not S2_CHECK-fail on invalid input. Returns true if
 // conversion is successful.
 ABSL_MUST_USE_RESULT bool MakeLaxPolyline(
-    absl::string_view str, std::unique_ptr<S2LaxPolylineShape>* lax_polyline);
+    shield_absl::string_view str, std::unique_ptr<S2LaxPolylineShape>* lax_polyline);
 
 // Given a sequence of loops separated by semicolons, returns a newly
 // allocated polygon.  Loops are automatically normalized by inverting them
@@ -187,40 +187,40 @@
 //     "empty"  // the empty polygon (consisting of no loops)
 //     "full"   // the full polygon (consisting of one full loop).
 std::unique_ptr<S2Polygon> MakePolygonOrDie(
-    absl::string_view str,
+    shield_absl::string_view str,
     S2Debug debug_override = S2Debug::ALLOW);
 
 // As above, but does not S2_CHECK-fail on invalid input. Returns true if
 // conversion is successful.
-ABSL_MUST_USE_RESULT bool MakePolygon(absl::string_view str,
+ABSL_MUST_USE_RESULT bool MakePolygon(shield_absl::string_view str,
                                std::unique_ptr<S2Polygon>* polygon,
                                S2Debug debug_override = S2Debug::ALLOW);
 
 ABSL_DEPRECATED("Inline the implementation")
 inline std::unique_ptr<S2Polygon> MakePolygon(
-    absl::string_view str, S2Debug debug_override = S2Debug::ALLOW) {
+    shield_absl::string_view str, S2Debug debug_override = S2Debug::ALLOW) {
   return MakePolygonOrDie(str, debug_override);
 }
 
 // Like MakePolygon(), except that it does not normalize loops (i.e., it
 // gives you exactly what you asked for).
-std::unique_ptr<S2Polygon> MakeVerbatimPolygonOrDie(absl::string_view str);
+std::unique_ptr<S2Polygon> MakeVerbatimPolygonOrDie(shield_absl::string_view str);
 
 // As above, but does not S2_CHECK-fail on invalid input. Returns true if
 // conversion is successful.
-ABSL_MUST_USE_RESULT bool MakeVerbatimPolygon(absl::string_view str,
+ABSL_MUST_USE_RESULT bool MakeVerbatimPolygon(shield_absl::string_view str,
                                        std::unique_ptr<S2Polygon>* polygon);
 
 // Parses a string in the same format as MakePolygon, except that loops must
 // be oriented so that the interior of the loop is always on the left, and
 // polygons with degeneracies are supported.  As with MakePolygon, "full" and
 // denotes the full polygon and "" or "empty" denote the empty polygon.
-std::unique_ptr<S2LaxPolygonShape> MakeLaxPolygonOrDie(absl::string_view str);
+std::unique_ptr<S2LaxPolygonShape> MakeLaxPolygonOrDie(shield_absl::string_view str);
 
 // As above, but does not S2_CHECK-fail on invalid input. Returns true if
 // conversion is successful.
 ABSL_MUST_USE_RESULT bool MakeLaxPolygon(
-    absl::string_view str, std::unique_ptr<S2LaxPolygonShape>* lax_polygon);
+    shield_absl::string_view str, std::unique_ptr<S2LaxPolygonShape>* lax_polygon);
 
 // Returns a MutableS2ShapeIndex containing the points, polylines, and loops
 // (in the form of one polygon for each group of loops) described by the
@@ -246,11 +246,11 @@
 //
 // CAVEAT: Because whitespace is ignored, empty polygons must be specified
 //         as the string "empty" rather than as the empty string ("").
-std::unique_ptr<MutableS2ShapeIndex> MakeIndexOrDie(absl::string_view str);
+std::unique_ptr<MutableS2ShapeIndex> MakeIndexOrDie(shield_absl::string_view str);
 
 // As above, but does not S2_CHECK-fail on invalid input. Returns true if
 // conversion is successful.
-ABSL_MUST_USE_RESULT bool MakeIndex(absl::string_view str,
+ABSL_MUST_USE_RESULT bool MakeIndex(shield_absl::string_view str,
                              std::unique_ptr<MutableS2ShapeIndex>* index);
 
 // Convert an S2Point, S2LatLng, S2LatLngRect, S2CellId, S2CellUnion, loop,
@@ -263,12 +263,12 @@
 std::string ToString(const S2Loop& loop);
 std::string ToString(const S2Polyline& polyline);
 std::string ToString(const S2Polygon& polygon,
-                     absl::string_view loop_separator = ";\n");
-std::string ToString(absl::Span<const S2Point> points);
-std::string ToString(absl::Span<const S2LatLng> latlngs);
+                     shield_absl::string_view loop_separator = ";\n");
+std::string ToString(shield_absl::Span<const S2Point> points);
+std::string ToString(shield_absl::Span<const S2LatLng> latlngs);
 std::string ToString(const S2LaxPolylineShape& polyline);
 std::string ToString(const S2LaxPolygonShape& polygon,
-                     absl::string_view loop_separator = ";\n");
+                     shield_absl::string_view loop_separator = ";\n");
 
 // Convert any S2Shape to the string format above.
 std::string ToString(const S2Shape& shape);
diff -u -r '--exclude=.git' a/src/s2/s2text_format_test.cc b/src/s2/s2text_format_test.cc
--- a/src/s2/s2text_format_test.cc	2024-07-25 16:38:02.919739565 -0400
+++ b/src/s2/s2text_format_test.cc	2024-07-25 16:20:50.148040674 -0400
@@ -44,7 +44,7 @@
 #include "s2/s2testing.h"
 #include "s2/util/math/mathutil.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using std::make_unique;
 using std::string;
 using std::unique_ptr;
@@ -58,11 +58,11 @@
 // "max_digits" after the decimal point and has no trailing zeros.
 void ExpectMaxDigits(const S2LatLng& ll, int max_digits) {
   string result = s2textformat::ToString(ll);
-  vector<string> values = absl::StrSplit(result, ':', absl::SkipEmpty());
+  vector<string> values = shield_absl::StrSplit(result, ':', shield_absl::SkipEmpty());
   EXPECT_EQ(2, values.size()) << result;
   for (const auto& value : values) {
     int num_digits = 0;
-    if (absl::StrContains(value, '.')) {
+    if (shield_absl::StrContains(value, '.')) {
       num_digits = value.size() - value.find('.') - 1;
       EXPECT_NE('0', value.back());
     }
diff -u -r '--exclude=.git' a/src/s2/s2winding_operation_test.cc b/src/s2/s2winding_operation_test.cc
--- a/src/s2/s2winding_operation_test.cc	2024-07-25 16:38:02.919739565 -0400
+++ b/src/s2/s2winding_operation_test.cc	2024-07-25 16:20:50.108040509 -0400
@@ -44,7 +44,7 @@
 #include "s2/s2testing.h"
 #include "s2/s2text_format.h"
 
-using absl::string_view;
+using shield_absl::string_view;
 using s2builderutil::IdentitySnapFunction;
 using s2builderutil::IntLatLngSnapFunction;
 using std::make_unique;
diff -u -r '--exclude=.git' a/src/s2/sequence_lexicon.h b/src/s2/sequence_lexicon.h
--- a/src/s2/sequence_lexicon.h	2024-07-25 16:38:02.919739565 -0400
+++ b/src/s2/sequence_lexicon.h	2024-07-25 16:20:50.120040559 -0400
@@ -164,7 +164,7 @@
 template <class T, class Hasher, class KeyEqual>
 size_t SequenceLexicon<T, Hasher, KeyEqual>::IdHasher::operator()(
     uint32 id) const {
-  // TODO(user,b/205929456): Is there a way to use absl::Hash instead?
+  // TODO(user,b/205929456): Is there a way to use shield_absl::Hash instead?
   HashMix mix;
   for (const auto& value : lexicon_->sequence(id)) {
     mix.Mix(hasher_(value));
diff -u -r '--exclude=.git' a/src/s2/thread_testing.h b/src/s2/thread_testing.h
--- a/src/s2/thread_testing.h	2024-07-25 16:37:56.207717387 -0400
+++ b/src/s2/thread_testing.h	2024-07-25 16:20:50.116040541 -0400
@@ -48,16 +48,16 @@
   void ReaderLoop();  // The main loop of each reader thread.
 
   // The following fields are guarded by lock_.
-  absl::Mutex lock_;
+  shield_absl::Mutex lock_;
   int num_writes_ = 0;
   int num_readers_left_ = 0;
 
   // TODO(ericv): Consider removing these condition variables now that the
 
   // Signalled when a new write is ready to be processed.
-  absl::CondVar write_ready_;
+  shield_absl::CondVar write_ready_;
   // Signalled when all readers have processed the latest write.
-  absl::CondVar all_readers_done_;
+  shield_absl::CondVar all_readers_done_;
 };
 
 }  // namespace s2testing
diff -u -r '--exclude=.git' a/src/s2/util/bitmap/bitmap.h b/src/s2/util/bitmap/bitmap.h
--- a/src/s2/util/bitmap/bitmap.h	2024-07-25 16:38:02.919739565 -0400
+++ b/src/s2/util/bitmap/bitmap.h	2024-07-25 16:20:50.132040607 -0400
@@ -77,7 +77,7 @@
   explicit BasicBitmap(size_type size) : BasicBitmap(size, false) {}
 
   // Creates a BasicBitmap with the given sequence of boolean values as bits.
-  explicit BasicBitmap(const absl::Span<const bool> values)
+  explicit BasicBitmap(const shield_absl::Span<const bool> values)
       : BasicBitmap(values.size()) {
     int index = 0;
     for (bool value : values) {
@@ -475,7 +475,7 @@
 
   // Returns an efficient hash of the bitmap.
   // The algorithm used is subject to change.
-  size_t HashCode() const { return absl::Hash<BasicBitmap>{}(*this); }
+  size_t HashCode() const { return shield_absl::Hash<BasicBitmap>{}(*this); }
   template <typename H>
   friend H AbslHashValue(H state, const BasicBitmap& bitmap) {
     const size_t complete_map_entries = bitmap.size_ / bitmap.kIntBits;
@@ -902,16 +902,16 @@
 
   ArithmeticWord endmask = (kAllOnesWord >> ((end_word + 1) * kIntBits - end));
   if (end_word == start_word) {  // Only one word?
-    return absl::popcount(static_cast<Word>(c & endmask));
+    return shield_absl::popcount(static_cast<Word>(c & endmask));
   }
 
-  size_type sum = absl::popcount(static_cast<Word>(c));
+  size_type sum = shield_absl::popcount(static_cast<Word>(c));
 
   for (++p; p < map_ + end_word; ++p) {
-    sum += absl::popcount(*p);
+    sum += shield_absl::popcount(*p);
   }
 
-  return sum + absl::popcount(static_cast<Word>(
+  return sum + shield_absl::popcount(static_cast<Word>(
                    static_cast<ArithmeticWord>(*p) & endmask));
 }
 
diff -u -r '--exclude=.git' a/src/s2/util/bits/bits.h b/src/s2/util/bits/bits.h
--- a/src/s2/util/bits/bits.h	2024-07-25 16:38:02.919739565 -0400
+++ b/src/s2/util/bits/bits.h	2024-07-25 16:20:50.128040591 -0400
@@ -39,29 +39,29 @@
 
 inline int FindLSBSetNonZero(uint32 n) {
   S2_ASSUME(n != 0);
-  return absl::countr_zero(n);
+  return shield_absl::countr_zero(n);
 }
 
 inline int FindLSBSetNonZero64(uint64 n) {
   S2_ASSUME(n != 0);
-  return absl::countr_zero(n);
+  return shield_absl::countr_zero(n);
 }
 
 inline int Log2FloorNonZero(uint32 n) {
   S2_ASSUME(n != 0);
-  return absl::bit_width(n) - 1;
+  return shield_absl::bit_width(n) - 1;
 }
 
 inline int Log2FloorNonZero64(uint64 n) {
   S2_ASSUME(n != 0);
-  return absl::bit_width(n) - 1;
+  return shield_absl::bit_width(n) - 1;
 }
 
 inline int FindMSBSetNonZero(uint32 n) { return Log2FloorNonZero(n); }
 inline int FindMSBSetNonZero64(uint64 n) { return Log2FloorNonZero64(n); }
 
 inline int Log2Ceiling(uint32 n) {
-  int floor = absl::bit_width(n) - 1;
+  int floor = shield_absl::bit_width(n) - 1;
   if ((n & (n - 1)) == 0) {  // zero or a power of two
     return floor;
   } else {
diff -u -r '--exclude=.git' a/src/s2/util/coding/coder.cc b/src/s2/util/coding/coder.cc
--- a/src/s2/util/coding/coder.cc	2024-07-25 16:38:02.919739565 -0400
+++ b/src/s2/util/coding/coder.cc	2024-07-25 16:20:50.128040591 -0400
@@ -32,18 +32,18 @@
 #include "s2/base/port.h"
 
 Encoder::Encoder(Encoder&& other)
-    : buf_(absl::exchange(other.buf_, nullptr)),
-      limit_(absl::exchange(other.limit_, nullptr)),
-      underlying_buffer_(absl::exchange(other.underlying_buffer_, nullptr)),
-      orig_(absl::exchange(other.orig_, nullptr)) {}
+    : buf_(shield_absl::exchange(other.buf_, nullptr)),
+      limit_(shield_absl::exchange(other.limit_, nullptr)),
+      underlying_buffer_(shield_absl::exchange(other.underlying_buffer_, nullptr)),
+      orig_(shield_absl::exchange(other.orig_, nullptr)) {}
 
 Encoder& Encoder::operator=(Encoder&& other) {
   if (this == &other) return *this;
   if (ensure_allowed()) DeleteBuffer(underlying_buffer_, capacity());
-  buf_ = absl::exchange(other.buf_, nullptr);
-  limit_ = absl::exchange(other.limit_, nullptr);
-  underlying_buffer_ = absl::exchange(other.underlying_buffer_, nullptr);
-  orig_ = absl::exchange(other.orig_, nullptr);
+  buf_ = shield_absl::exchange(other.buf_, nullptr);
+  limit_ = shield_absl::exchange(other.limit_, nullptr);
+  underlying_buffer_ = shield_absl::exchange(other.underlying_buffer_, nullptr);
+  orig_ = shield_absl::exchange(other.orig_, nullptr);
   return *this;
 }
 
diff -u -r '--exclude=.git' a/src/s2/util/coding/coder.h b/src/s2/util/coding/coder.h
--- a/src/s2/util/coding/coder.h	2024-07-25 16:38:02.919739565 -0400
+++ b/src/s2/util/coding/coder.h	2024-07-25 16:20:50.128040591 -0400
@@ -65,7 +65,7 @@
   void put16(uint16 v) { writer().put16(v); }
   void put32(uint32 v) { writer().put32(v); }
   void put64(uint64 v) { writer().put64(v); }
-  void put128(absl::uint128 v) { writer().put128(v); }
+  void put128(shield_absl::uint128 v) { writer().put128(v); }
   void putn(const void* mem, size_t n) { writer().putn(mem, n); }
 
   // Put no more than n bytes, stopping when c is put.
@@ -75,8 +75,8 @@
   void puts(const void* mem) { writer().puts(mem); }
   // put a c-string without \0.
   void puts_without_null(const char* mem) { writer().puts_no_null(mem); }
-  void putfloat(float f) { writer().put32(absl::bit_cast<uint32>(f)); }
-  void putdouble(double d) { writer().put64(absl::bit_cast<uint64>(d)); }
+  void putfloat(float f) { writer().put32(shield_absl::bit_cast<uint32>(f)); }
+  void putdouble(double d) { writer().put64(shield_absl::bit_cast<uint64>(d)); }
 
   // Support for variable length encoding with 7 bits per byte
   // (these are just simple wrappers around the Varint module)
@@ -158,13 +158,13 @@
       S2_DCHECK_LE(enc->buf_, enc->limit_);
     }
 
-    char* skip(ptrdiff_t N) { return absl::exchange(p, p + N); }
+    char* skip(ptrdiff_t N) { return shield_absl::exchange(p, p + N); }
 
     void put8(unsigned char v) { *p++ = v; }
     void put16(uint16 v) { LittleEndian::Store16(skip(2), v); }
     void put32(uint32 v) { LittleEndian::Store32(skip(4), v); }
     void put64(uint64 v) { LittleEndian::Store64(skip(8), v); }
-    void put128(absl::uint128 v) { LittleEndian::Store128(skip(16), v); }
+    void put128(shield_absl::uint128 v) { LittleEndian::Store128(skip(16), v); }
     void putn(const void* src, size_t n) { memcpy(skip(n), src, n); }
     void putcn(const void* src, int c, size_t n) {
       auto* o = p;
@@ -237,7 +237,7 @@
   uint16 get16();
   uint32 get32();
   uint64 get64();
-  absl::uint128 get128();
+  shield_absl::uint128 get128();
   float  getfloat();
   double getdouble();
   void   getn(void* mem, size_t n);
@@ -490,11 +490,11 @@
   // This is an optimization based on the fact that Decoder(nullptr, 0) sets all
   // structure bytes to 0. This is valid because Decoder is TriviallyCopyable
   // (https://en.cppreference.com/w/cpp/named_req/TriviallyCopyable).
-  static_assert(absl::is_trivially_copy_constructible<Decoder>::value,
+  static_assert(shield_absl::is_trivially_copy_constructible<Decoder>::value,
                 "Decoder must be trivially copy-constructible");
-  static_assert(absl::is_trivially_copy_assignable<Decoder>::value,
+  static_assert(shield_absl::is_trivially_copy_assignable<Decoder>::value,
                 "Decoder must be trivially copy-assignable");
-  static_assert(absl::is_trivially_destructible<Decoder>::value,
+  static_assert(shield_absl::is_trivially_destructible<Decoder>::value,
                 "Decoder must be trivially destructible");
   std::memset(array, 0, num_decoders * sizeof(Decoder));
 }
@@ -523,18 +523,18 @@
   return v;
 }
 
-inline absl::uint128 Decoder::get128() {
-  const absl::uint128 v = LittleEndian::Load128(buf_);
+inline shield_absl::uint128 Decoder::get128() {
+  const shield_absl::uint128 v = LittleEndian::Load128(buf_);
   buf_ += sizeof(v);
   return v;
 }
 
 inline float Decoder::getfloat() {
-  return absl::bit_cast<float>(get32());
+  return shield_absl::bit_cast<float>(get32());
 }
 
 inline double Decoder::getdouble() {
-  return absl::bit_cast<double>(get64());
+  return shield_absl::bit_cast<double>(get64());
 }
 
 inline bool Decoder::get_varint32(uint32* v) {
diff -u -r '--exclude=.git' a/src/s2/util/endian/endian.h b/src/s2/util/endian/endian.h
--- a/src/s2/util/endian/endian.h	2024-07-25 16:38:02.919739565 -0400
+++ b/src/s2/util/endian/endian.h	2024-07-25 16:20:50.128040591 -0400
@@ -40,20 +40,20 @@
 #include "absl/numeric/int128.h"
 
 inline uint64 gbswap_64(uint64 host_int) {
-  return absl::gbswap_64(host_int);
+  return shield_absl::gbswap_64(host_int);
 }
 
 inline uint32 gbswap_32(uint32 host_int) {
-  return absl::gbswap_32(host_int);
+  return shield_absl::gbswap_32(host_int);
 }
 
 inline uint16 gbswap_16(uint16 host_int) {
-  return absl::gbswap_16(host_int);
+  return shield_absl::gbswap_16(host_int);
 }
 
-inline absl::uint128 gbswap_128(absl::uint128 host_int) {
-  return absl::MakeUint128(gbswap_64(absl::Uint128Low64(host_int)),
-                           gbswap_64(absl::Uint128High64(host_int)));
+inline shield_absl::uint128 gbswap_128(shield_absl::uint128 host_int) {
+  return shield_absl::MakeUint128(gbswap_64(shield_absl::Uint128Low64(host_int)),
+                           gbswap_64(shield_absl::Uint128High64(host_int)));
 }
 
 #ifdef IS_LITTLE_ENDIAN
@@ -155,8 +155,8 @@
   static uint64 FromHost64(uint64 x) { return x; }
   static uint64 ToHost64(uint64 x) { return x; }
 
-  static absl::uint128 FromHost128(absl::uint128 x) { return x; }
-  static absl::uint128 ToHost128(absl::uint128 x) { return x; }
+  static shield_absl::uint128 FromHost128(shield_absl::uint128 x) { return x; }
+  static shield_absl::uint128 ToHost128(shield_absl::uint128 x) { return x; }
 
   static constexpr bool IsLittleEndian() { return true; }
 
@@ -171,8 +171,8 @@
   static uint64 FromHost64(uint64 x) { return gbswap_64(x); }
   static uint64 ToHost64(uint64 x) { return gbswap_64(x); }
 
-  static absl::uint128 FromHost128(absl::uint128 x) { return gbswap_128(x); }
-  static absl::uint128 ToHost128(absl::uint128 x) { return gbswap_128(x); }
+  static shield_absl::uint128 FromHost128(shield_absl::uint128 x) { return gbswap_128(x); }
+  static shield_absl::uint128 ToHost128(shield_absl::uint128 x) { return gbswap_128(x); }
 
   static constexpr bool IsLittleEndian() { return false; }
 
@@ -274,27 +274,27 @@
     UNALIGNED_STORE64(p, FromHost64(v));
   }
 
-  static absl::uint128 Load128(const void* p) {
-    return absl::MakeUint128(
+  static shield_absl::uint128 Load128(const void* p) {
+    return shield_absl::MakeUint128(
         ToHost64(UNALIGNED_LOAD64(reinterpret_cast<const uint64*>(p) + 1)),
         ToHost64(UNALIGNED_LOAD64(p)));
   }
 
-  static void Store128(void* p, const absl::uint128 v) {
-    UNALIGNED_STORE64(p, FromHost64(absl::Uint128Low64(v)));
+  static void Store128(void* p, const shield_absl::uint128 v) {
+    UNALIGNED_STORE64(p, FromHost64(shield_absl::Uint128Low64(v)));
     UNALIGNED_STORE64(reinterpret_cast<uint64*>(p) + 1,
-                      FromHost64(absl::Uint128High64(v)));
+                      FromHost64(shield_absl::Uint128High64(v)));
   }
 
   // Build a uint128 from 1-16 bytes.
   // 8 * len least significant bits are loaded from the memory with
   // LittleEndian order. The 128 - 8 * len most significant bits are
   // set all to 0.
-  static absl::uint128 Load128VariableLength(const void* p, int len) {
+  static shield_absl::uint128 Load128VariableLength(const void* p, int len) {
     if (len <= 8) {
-      return absl::uint128(Load64VariableLength(p, len));
+      return shield_absl::uint128(Load64VariableLength(p, len));
     } else {
-      return absl::MakeUint128(
+      return shield_absl::MakeUint128(
           Load64VariableLength(static_cast<const char*>(p) + 8, len - 8),
           Load64(p));
     }
@@ -355,8 +355,8 @@
   static uint64 FromHost64(uint64 x) { return gbswap_64(x); }
   static uint64 ToHost64(uint64 x) { return gbswap_64(x); }
 
-  static absl::uint128 FromHost128(absl::uint128 x) { return gbswap_128(x); }
-  static absl::uint128 ToHost128(absl::uint128 x) { return gbswap_128(x); }
+  static shield_absl::uint128 FromHost128(shield_absl::uint128 x) { return gbswap_128(x); }
+  static shield_absl::uint128 ToHost128(shield_absl::uint128 x) { return gbswap_128(x); }
 
   static constexpr bool IsLittleEndian() { return true; }
 
@@ -371,8 +371,8 @@
   static uint64 FromHost64(uint64 x) { return x; }
   static uint64 ToHost64(uint64 x) { return x; }
 
-  static absl::uint128 FromHost128(absl::uint128 x) { return x; }
-  static absl::uint128 ToHost128(absl::uint128 x) { return x; }
+  static shield_absl::uint128 FromHost128(shield_absl::uint128 x) { return x; }
+  static shield_absl::uint128 ToHost128(shield_absl::uint128 x) { return x; }
 
   static constexpr bool IsLittleEndian() { return false; }
 
@@ -465,31 +465,31 @@
     UNALIGNED_STORE64(p, FromHost64(v));
   }
 
-  static absl::uint128 Load128(const void* p) {
-    return absl::MakeUint128(
+  static shield_absl::uint128 Load128(const void* p) {
+    return shield_absl::MakeUint128(
         ToHost64(UNALIGNED_LOAD64(p)),
         ToHost64(UNALIGNED_LOAD64(reinterpret_cast<const uint64*>(p) + 1)));
   }
 
-  static void Store128(void* p, const absl::uint128 v) {
-    UNALIGNED_STORE64(p, FromHost64(absl::Uint128High64(v)));
+  static void Store128(void* p, const shield_absl::uint128 v) {
+    UNALIGNED_STORE64(p, FromHost64(shield_absl::Uint128High64(v)));
     UNALIGNED_STORE64(reinterpret_cast<uint64*>(p) + 1,
-                      FromHost64(absl::Uint128Low64(v)));
+                      FromHost64(shield_absl::Uint128Low64(v)));
   }
 
   // Build a uint128 from 1-16 bytes.
   // 8 * len least significant bits are loaded from the memory with
   // BigEndian order. The 128 - 8 * len most significant bits are
   // set all to 0.
-  static absl::uint128 Load128VariableLength(const void* p, int len) {
+  static shield_absl::uint128 Load128VariableLength(const void* p, int len) {
     if (len <= 8) {
-      return absl::uint128(
+      return shield_absl::uint128(
           Load64VariableLength(static_cast<const char*>(p), len));
     } else if (len < 16) {
-      return absl::MakeUint128(Load64VariableLength(p, len - 8),
+      return shield_absl::MakeUint128(Load64VariableLength(p, len - 8),
                                Load64(static_cast<const char*>(p) + len - 8));
     } else {
-      return absl::MakeUint128(Load64(static_cast<const char*>(p)),
+      return shield_absl::MakeUint128(Load64(static_cast<const char*>(p)),
                                Load64(static_cast<const char*>(p) + 8));
     }
   }
@@ -562,7 +562,7 @@
 FROMHOST_TYPE_MAP(uint32, float);
 FROMHOST_TYPE_MAP(uint64, double);
 FROMHOST_TYPE_MAP(uint8, bool);
-FROMHOST_TYPE_MAP(absl::uint128, absl::uint128);
+FROMHOST_TYPE_MAP(shield_absl::uint128, shield_absl::uint128);
 #undef FROMHOST_TYPE_MAP
 
 // Default implementation for the unified FromHost(ValueType) API, which
@@ -622,12 +622,12 @@
  public:
   static typename tofromhost_value_type_traits<float>::int_type FromHost(
       float v) {
-    return EndianClass::FromHost32(absl::bit_cast<uint32>(v));
+    return EndianClass::FromHost32(shield_absl::bit_cast<uint32>(v));
   }
   static typename tofromhost_value_type_traits<float>::int_type ToHost(
       float v) {
-    return absl::bit_cast<float>(
-        EndianClass::ToHost32(absl::bit_cast<uint32>(v)));
+    return shield_absl::bit_cast<float>(
+        EndianClass::ToHost32(shield_absl::bit_cast<uint32>(v)));
   }
 };
 
@@ -638,26 +638,26 @@
  public:
   static typename tofromhost_value_type_traits<double>::int_type FromHost(
       double v) {
-    return EndianClass::FromHost64(absl::bit_cast<uint64>(v));
+    return EndianClass::FromHost64(shield_absl::bit_cast<uint64>(v));
   }
   static typename tofromhost_value_type_traits<double>::int_type ToHost(
       double v) {
-    return absl::bit_cast<double>(
-        EndianClass::ToHost64(absl::bit_cast<uint64>(v)));
+    return shield_absl::bit_cast<double>(
+        EndianClass::ToHost64(shield_absl::bit_cast<uint64>(v)));
   }
 };
 
 // Specialization of the unified FromHost(ValueType) API, which handles
 // uint128 types (ValueType is uint128).
 template <class EndianClass>
-class GeneralFormatConverter<EndianClass, absl::uint128> {
+class GeneralFormatConverter<EndianClass, shield_absl::uint128> {
  public:
-  static typename tofromhost_value_type_traits<absl::uint128>::int_type
-  FromHost(absl::uint128 v) {
+  static typename tofromhost_value_type_traits<shield_absl::uint128>::int_type
+  FromHost(shield_absl::uint128 v) {
     return EndianClass::FromHost128(v);
   }
-  static typename tofromhost_value_type_traits<absl::uint128>::int_type ToHost(
-      absl::uint128 v) {
+  static typename tofromhost_value_type_traits<shield_absl::uint128>::int_type ToHost(
+      shield_absl::uint128 v) {
     return EndianClass::ToHost128(v);
   }
 };
@@ -704,24 +704,24 @@
 
 template<typename EndianClass>
 inline float LoadFloat(const char* p) {
-  return absl::bit_cast<float>(EndianClass::ToHost32(UNALIGNED_LOAD32(p)));
+  return shield_absl::bit_cast<float>(EndianClass::ToHost32(UNALIGNED_LOAD32(p)));
 }
 
 template<typename EndianClass>
 inline void StoreFloat(float value, char* p) {
   UNALIGNED_STORE32(p,
-                    EndianClass::FromHost32(absl::bit_cast<uint32>(value)));
+                    EndianClass::FromHost32(shield_absl::bit_cast<uint32>(value)));
 }
 
 template<typename EndianClass>
 inline double LoadDouble(const char* p) {
-  return absl::bit_cast<double>(EndianClass::ToHost64(UNALIGNED_LOAD64(p)));
+  return shield_absl::bit_cast<double>(EndianClass::ToHost64(UNALIGNED_LOAD64(p)));
 }
 
 template<typename EndianClass>
 inline void StoreDouble(double value, char* p) {
   UNALIGNED_STORE64(p,
-                    EndianClass::FromHost64(absl::bit_cast<uint64>(value)));
+                    EndianClass::FromHost64(shield_absl::bit_cast<uint64>(value)));
 }
 
 }  // namespace endian_internal
@@ -821,22 +821,22 @@
 // Load/Store for uint128.
 
 template <>
-inline absl::uint128 LittleEndian::Load<absl::uint128>(const char* p) {
+inline shield_absl::uint128 LittleEndian::Load<shield_absl::uint128>(const char* p) {
   return LittleEndian::Load128(p);
 }
 
 template <>
-inline void LittleEndian::Store<absl::uint128>(absl::uint128 value, char* p) {
+inline void LittleEndian::Store<shield_absl::uint128>(shield_absl::uint128 value, char* p) {
   LittleEndian::Store128(p, value);
 }
 
 template <>
-inline absl::uint128 BigEndian::Load<absl::uint128>(const char* p) {
+inline shield_absl::uint128 BigEndian::Load<shield_absl::uint128>(const char* p) {
   return BigEndian::Load128(p);
 }
 
 template <>
-inline void BigEndian::Store<absl::uint128>(absl::uint128 value, char* p) {
+inline void BigEndian::Store<shield_absl::uint128>(shield_absl::uint128 value, char* p) {
   BigEndian::Store128(p, value);
 }
 
diff -u -r '--exclude=.git' a/src/s2/util/gtl/compact_array.h b/src/s2/util/gtl/compact_array.h
--- a/src/s2/util/gtl/compact_array.h	2024-07-25 16:38:02.919739565 -0400
+++ b/src/s2/util/gtl/compact_array.h	2024-07-25 16:20:50.128040591 -0400
@@ -358,7 +358,7 @@
     // Destroying elements on shrinking resize isn't a concern, since the
     // value_type must be trivially destructible.
     if (n > size() &&
-        !absl::is_trivially_default_constructible<value_type>::value) {
+        !shield_absl::is_trivially_default_constructible<value_type>::value) {
       // Increasing size would expose unconstructed elements.
       value_type *new_end = Array() + n;
       for (value_type *p = Array() + size(); p != new_end; ++p)
@@ -519,9 +519,9 @@
     typedef typename std::iterator_traits<Iterator>::iterator_category Cat;
     range_insert(p, first, last, Cat());
   }
-  static_assert(absl::is_trivially_copy_constructible<value_type>::value &&
-                absl::is_trivially_copy_assignable<value_type>::value &&
-                absl::is_trivially_destructible<value_type>::value,
+  static_assert(shield_absl::is_trivially_copy_constructible<value_type>::value &&
+                shield_absl::is_trivially_copy_assignable<value_type>::value &&
+                shield_absl::is_trivially_destructible<value_type>::value,
                 "Requires trivial copy, assignment, and destructor.");
 };
 
diff -u -r '--exclude=.git' a/src/s2/util/gtl/dense_hash_set.h b/src/s2/util/gtl/dense_hash_set.h
--- a/src/s2/util/gtl/dense_hash_set.h	2024-07-25 16:38:02.919739565 -0400
+++ b/src/s2/util/gtl/dense_hash_set.h	2024-07-25 16:20:50.128040591 -0400
@@ -183,12 +183,12 @@
   // These come from tr1's unordered_set. For us, a bucket has 0 or 1 elements.
   ABSL_DEPRECATED(
       "This method is slated for removal.  Please migrate to "
-      "absl::flat_hash_set.")
+      "shield_absl::flat_hash_set.")
   local_iterator begin(size_type i) const { return rep.begin(i); }
 
   ABSL_DEPRECATED(
       "This method is slated for removal.  Please migrate to "
-      "absl::flat_hash_set.")
+      "shield_absl::flat_hash_set.")
   local_iterator end(size_type i) const   { return rep.end(i); }
 
 
@@ -239,17 +239,17 @@
 
   ABSL_DEPRECATED(
       "This method is slated for removal.  Please migrate to "
-      "absl::flat_hash_set.")
+      "shield_absl::flat_hash_set.")
   size_type max_bucket_count() const  { return rep.max_bucket_count(); }
 
   // These are tr1 methods.  bucket() is the bucket the key is or would be in.
   ABSL_DEPRECATED(
       "This method is slated for removal.  Please migrate to "
-      "absl::flat_hash_set.")
+      "shield_absl::flat_hash_set.")
   size_type bucket_size(size_type i) const    { return rep.bucket_size(i); }
   ABSL_DEPRECATED(
       "This method is slated for removal.  Please migrate to "
-      "absl::flat_hash_set.")
+      "shield_absl::flat_hash_set.")
   size_type bucket(const key_type& key) const { return rep.bucket(key); }
   float load_factor() const {
     return size() * 1.0f / bucket_count();
@@ -267,7 +267,7 @@
   // These aren't tr1 methods but perhaps ought to be.
   ABSL_DEPRECATED(
       "This method is slated for removal.  Please migrate to "
-      "absl::flat_hash_set.")
+      "shield_absl::flat_hash_set.")
   float min_load_factor() const {
     float shrink, grow;
     rep.get_resizing_parameters(&shrink, &grow);
diff -u -r '--exclude=.git' a/src/s2/util/math/exactfloat/exactfloat.cc b/src/s2/util/math/exactfloat/exactfloat.cc
--- a/src/s2/util/math/exactfloat/exactfloat.cc	2024-07-25 16:38:02.923739577 -0400
+++ b/src/s2/util/math/exactfloat/exactfloat.cc	2024-07-25 16:20:50.132040607 -0400
@@ -156,7 +156,7 @@
   // In OpenSSL >= 1.1, BIGNUM is an opaque type, so d and top
   // cannot be accessed.  The bytes must be copied out at a ~25%
   // performance penalty.
-  absl::FixedArray<unsigned char> bytes(BN_num_bytes(bn));
+  shield_absl::FixedArray<unsigned char> bytes(BN_num_bytes(bn));
   // "le" indicates little endian.
   S2_CHECK_EQ(BN_bn2lebinpad(bn, bytes.data(), bytes.size()), bytes.size());
 
diff -u -r '--exclude=.git' a/src/s2/util/math/vector.h b/src/s2/util/math/vector.h
--- a/src/s2/util/math/vector.h	2024-07-25 16:38:02.923739577 -0400
+++ b/src/s2/util/math/vector.h	2024-07-25 16:20:50.132040607 -0400
@@ -59,7 +59,7 @@
   typedef typename std::conditional<std::is_integral<T>::value, double, T>::type
       FloatType;
 
-  using IdxSeqN = typename absl::make_index_sequence<N>;
+  using IdxSeqN = typename shield_absl::make_index_sequence<N>;
 
   template <std::size_t I, typename F, typename... As>
   static auto Reduce(F f, As*... as) -> decltype(f(as[I]...)) {
@@ -67,7 +67,7 @@
   }
 
   template <typename R = D, std::size_t... Is, typename F, typename... As>
-  static R GenerateEach(absl::index_sequence<Is...>, F f, As*... as) {
+  static R GenerateEach(shield_absl::index_sequence<Is...>, F f, As*... as) {
     return R(Reduce<Is>(std::move(f), as...)...);
   }
 
@@ -308,28 +308,28 @@
   static void Ignore(std::initializer_list<bool>) {}
 
   template <std::size_t... Is>
-  static T Dot(T sum, const T* a, const T* b, absl::index_sequence<Is...>) {
+  static T Dot(T sum, const T* a, const T* b, shield_absl::index_sequence<Is...>) {
     Ignore({(sum += a[Is] * b[Is], true)...});
     return sum;
   }
 
   template <std::size_t... Is>
-  static void PlusEq(T* a, const T* b, absl::index_sequence<Is...>) {
+  static void PlusEq(T* a, const T* b, shield_absl::index_sequence<Is...>) {
     Ignore({(a[Is] += b[Is], true)...});
   }
 
   template <std::size_t... Is>
-  static void MinusEq(T* a, const T* b, absl::index_sequence<Is...>) {
+  static void MinusEq(T* a, const T* b, shield_absl::index_sequence<Is...>) {
     Ignore({(a[Is] -= b[Is], true)...});
   }
 
   template <std::size_t... Is>
-  static void MulEq(T* a, T b, absl::index_sequence<Is...>) {
+  static void MulEq(T* a, T b, shield_absl::index_sequence<Is...>) {
     Ignore({(a[Is] *= b, true)...});
   }
 
   template <std::size_t... Is>
-  static void DivEq(T* a, T b, absl::index_sequence<Is...>) {
+  static void DivEq(T* a, T b, shield_absl::index_sequence<Is...>) {
     Ignore({(a[Is] /= b, true)...});
   }
 };

